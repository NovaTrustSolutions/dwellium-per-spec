# Handoff v08 — Phase 3b Step 11 + 11.5 (LibraryPreview + Wiki sub-tab) — session bridge

**To:** the next Claude session
**From:** prior session 2026-05-08 (shipped Smart Paste, System/ folder structure, Editor color customization, plus two north-star arch-v2 sections — none of which changed Step 11's spec)
**You are starting:** Step 11 — same scope as `HANDOFF_v07_step-11.md`. **This handoff is a thin update on top of v07; read v07 for the implementation spec.**

**Naming convention:** handoffs live in `docs/` as `HANDOFF_vNN_step-X.md`, monotonic counter. Predecessors: `v01_step-4.md`, `v02_step-5.md`, `v03_step-3.md`, `v04_step-7.md`, `v05_step-6.md`, `v06_step-10.md`, `v07_step-11.md`. When you finish, write `HANDOFF_v09_step-X.md` for whatever comes next.

---

## Read order

1. **`docs/STATUS.md`** — TL;DR is current as of 2026-05-08.
2. **`docs/HANDOFF_v07_step-11.md`** — **the canonical Step 11 + 11.5 spec.** Files to add, IPC contracts, Option C scoping, persistence pattern, things-settled list. **Don't re-derive any of it; v07 is the implementation brief.**
3. **`docs/architecture-v3.md` §"Read/Write Surface Split"** — the architectural decision driving this step.
4. **`docs/gotcha.md`** — debugging discipline + accumulated priors.

---

## What shipped between v07 and v08 (that you should know exists)

None of these change Step 11's scope, but the codebase is meaningfully different. Quick survey so you don't trip over recent additions:

| Commit | Area | What | Why it matters for Step 11 |
|---|---|---|---|
| `99497cc` | Editor | Smart Paste (`Cmd+Shift+V`) + Raw Paste (`Cmd+Shift+Opt+V`) | Doesn't affect Step 11. Verified working. |
| `44f2d96` | RAG | Wiki compile prompt now surfaces contradictions in `## Open questions / tensions` section + `Planned: Audit Pass` arch-v2 spec | Future Step 11 work won't touch ragWiki's prompt; safe. |
| `4470caa` | Docs | Read/Write Surface Split arch + Step 11/11.5 Option C rescope | **This is your spec.** Go read arch-v2 §"Read/Write Surface Split" before designing components. |
| `d15416e` `0033067` | Diag | `[ChatDiag]` logs on the chat request path (4 sites) | **Don't remove the diag logs** in `threadActions.ts`, `useLMStream.ts`, `ipc.ts` (`lm:start`), `honcho.ts` (`getMessages`). Step 11 is renderer + IPC work that shouldn't touch any of these, but flagging defensively. |
| `176b3d4` | Docs | Thread Memory Management + Editor Enhancements arch sections | Both deferred until after Phase 3b. No Step 11 impact. |
| `866d4f9` | Thread | New threads get a `System/` subfolder for auto-generated files (BD/Notes/Comments/Memory) | **No Step 11 impact.** But if you're testing end-to-end: a new thread will have `System/` with `Memory/` inside. Old threads keep their flat structure. The change is invisible to the LibraryPreview surface — wiki pages still live at `_Library/Wiki/` regardless. |
| `1248d7a` | Editor | Markdown syntax color customization (Settings → Editor tab) | **Possible interaction:** the LibraryPreview component you're building renders markdown content. If you reuse `getMarkdownExtensions()` from `markdownConfig.ts` for the preview's read-only render, you automatically get the user's editor color theme applied. That's almost certainly what you want — same theme the user picked applies in the preview. Verify by switching themes and re-opening a preview. |
| `a331a64` | Docs | Component Library + Phase 5 Finalization north-star sections | No Step 11 impact. Read for context if curious. |

---

## What's already done (Phase 3b cumulative)

| Step | Commit(s) | Status |
|---|---|---|
| 10 Wiki compilation pipeline | `00819b9` | ✓ Verified end-to-end — 6 pages in DB + on disk, auto-compile fires, re-ingest loop closes |
| Reference regex + right-click Re-ingest | `d1f07da` | ✓ Verified |
| Watcher: usePolling + awaitWriteFinish + depth | `779e947`, `16440b4` | ✓ Verified |
| Watcher: thread-switch no longer clobbers root | `2399f6e` | ✓ The actual fix |
| Smart Paste / Raw Paste | `99497cc` | ✓ Verified by Andy |
| System/ folder for new threads | `866d4f9` | ✓ Verified by Andy |
| Editor color theme | `1248d7a` | ✓ Verified by Andy |
| Wiki contradiction prompt + Audit Pass spec | `44f2d96` | Prompt change live; audit pass spec only |

Phase 3a is fully shipped; ship gate verified end-to-end on 2026-05-07.

---

## Working tree at session start (deferred, do not touch)

```
M  src/renderer/src/themes.ts          ← Fey Phase 0 color sandbox, deferred per Andy
?? docs/Fey design.md                  ← Fey 4-phase roadmap, deferred per Andy
?? _Library/                           ← wiki disk artifacts, auto-generated by step 10
M  tsconfig.web.tsbuildinfo            ← auto-generated
```

The Fey items are intentionally uncommitted. **Do not commit them.** Andy will return to the Fey work explicitly when ready.

---

## Confirmed running infrastructure

```
Postgres   localhost:5432   pgvector/pgvector:pg15   container holocron_link-database-1
Redis      localhost:6379   redis:8.2                container holocron_link-redis-1
Honcho     localhost:8000   custom build             containers holocron_link-api-1 + deriver-1
```

`HOLOCRON_DB_URI` is in `editor/.env`. `npm run db:setup` is idempotent and applies migrations 001-003 in lex order.

Andy's data state (verify before claiming UI rendering correct):

- 4+ docs in `rag_documents`, several with tags
- 6+ wiki pages in `rag_wiki_pages` + on disk under `_Library/Wiki/<slug>.md`
- 1+ tag-shared relationship in `rag_relationships`
- All 10 RAG tables present: rag_documents, rag_tags, rag_document_tags, rag_relationships, rag_wiki_pages, rag_syntheses, rag_operations_log, rag_config, rag_namespaces, rag_wiki_page_sources

---

## Open items the next session might want to be aware of

- **Chat hallucination investigation** (`d15416e`) is open and waiting on Andy to capture `[ChatDiag]` logs from a real recurrence. Bug surfaces after extended conversation; not reliably reproducible on demand. **Don't pre-emptively fix.** If you're tempted to touch the chat code path during Step 11, don't — there's nothing in Step 11's spec that requires it.
- **Step 11.5's open-file dedup logic** (per v07): if a clicked search result is already in `editorStore.openFiles`, switch to Editor tab + select that tab regardless of relationship. This avoids two surfaces showing the same file. v07 spec calls this out; don't forget it during the Search.tsx click rewrite.
- **`useFileRelationship` hook** (per v07) is the single source of truth for the Editor-vs-Library dispatch. Don't duplicate the logic across components — every divergence is a future bug.
- **`useAsReportDraft`** must call into existing `report.ts` versioning — don't roll your own `_v1`/`_v2` filename logic; it's already correct there.
- **alert() for v1 feedback** is the pattern (matches right-click Re-ingest). Real toast component is future cleanup; don't block on it.

---

## Definition of done

Per v07 §"Definition of done" — both Step 11 (LibraryPreview + Wiki sub-tab + Import-to-Thread + Use-as-Report-Draft) and Step 11.5 (Search reroute + useFileRelationship + badges). Both ship clean tsc + build, behavior verifiable by Andy.

---

**Current branch:** `main`. **Latest commit:** `a331a64` (north-star arch sections). **Step 10 commit:** `00819b9`. Run `git log -15` for the recent trail.

**Cleared to begin Step 11 immediately.** v07 is your spec. Good luck.
