# docs/code.md — known issues, root causes, fixes (read BEFORE fixing anything)

Append-only log. Each entry: error → root cause → fix → prevention.

## 2026-08-22 — Calendar / Inbox Zero sub-widgets dead in production (hard-coded API base)

- **Error:** Strata → Calendar → Integrations tab never shows Google Calendar status/events in production; InboxZero Cold-email blocker / Reply tracker / NIF panels fail silently.
- **Root cause:** `CalendarModule.tsx`, `SentimentModule.tsx`, `InboxZero/{ColdEmailBlocker,ReplyTracker,NifIntelligence}.tsx` each declared `const API = 'http://localhost:3000'` instead of importing `API_BASE` from `src/config.ts` (same-origin on deployed hosts, localhost only in dev). Browser on argyleholocron.netlify.app → requests to localhost:3000 → connection refused (and mixed-content blocked).
- **Fix:** import `API_BASE` from `config` in all five (commit on `fix/050-cockpit-followups`).
- **Prevention:** `grep -rn "http://localhost:3000" qualia-shell/src --include='*.ts' --include='*.tsx' | grep -v test` must only hit `config.ts`. Never declare a per-widget API base.

## 2026-08-22 — Backend `GET /api/calendar/status` did not exist

- **Error:** CalendarModule fetches `/api/calendar/status` → 404 → card stuck on "not connected" even when Google is linked.
- **Root cause:** `calendarRoutes.ts` only had `/events`; `getCalendarConnectionStatus()` existed in `calendarService` but was never routed.
- **Fix:** backend `router.get('/status')` → `getCalendarConnectionStatus()` (+ `tests/calendarRoutes.test.ts`).

## 2026-08-22 — Cloud Run secrets wiped by partial deploys (`--set-secrets`)

- **Error:** After a deploy that exported only `BRIEF_RUN_SECRET`, Gmail/Calendar OAuth (and possibly OpenAI/Trello) stopped: `GOOGLE_OAUTH_CLIENT_SECRET is missing…` (503 from `/api/google/oauth/start`).
- **Root cause:** `deploy/cloud-run.sh` built `--set-secrets` only from secrets exported in the deploying shell, and gcloud documents `--set-secrets` as "All existing secrets will be removed first". The env-file merge preserved only literal vars (it skips `valueFrom` secret refs by design), so unlisted secret refs were dropped.
- **Confirmed 2026-08-22:** serving revision `00043-xdp` had only `BRIEF_RUN_SECRET`. Secret Manager holds exactly three secrets (`dwellium-brief-run-secret`, `dwellium-google-oauth-client-secret`, `dwellium-openai-api-key`) — the Trello secrets were NEVER created, so an `--update-secrets` naming them fails the whole revision ("Secret … was not found") and leaves dangling refs in the service spec (`Ready=False`).
- **Fix:** `--set-secrets` → `--update-secrets` in the script (adds/updates, keeps the rest). Restored live with: `gcloud run services update dwellium-backend --region us-central1 --remove-secrets TRELLO_API_KEY,TRELLO_TOKEN --update-secrets OPENAI_API_KEY=dwellium-openai-api-key:latest,GOOGLE_OAUTH_CLIENT_SECRET=dwellium-google-oauth-client-secret:latest` → revision `00045-8c6`. To add Trello later: export `TRELLO_API_KEY`/`TRELLO_TOKEN` in the deploying shell once (the script creates the secrets).
- **Prevention:** never use `--set-secrets` in deploy scripts; after every deploy, `gcloud run services describe dwellium-backend --region us-central1 --format='value(spec.template.spec.containers[0].env[].name)'` and confirm the secret names are still present.

## 2026-08-22 — Gmail fetcher hard-disabled in production

- **Error:** Inbox Zero never receives Gmail mail on Cloud Run.
- **Root cause:** deploy script wrote `GMAIL_FETCHER_ENABLED: "false"` unconditionally.
- **Fix:** `GMAIL_FETCHER_ENABLED: "${GMAIL_FETCHER_ENABLED:-false}"` — deploy with `GMAIL_FETCHER_ENABLED=true` once andy@dwellium.com is linked in Control Panel → Google Accounts (account_oauth strategy). Testing-mode OAuth (Ilya's decision, plan 032) → refresh tokens expire ~7 days → reconnect when the card says "needs re-auth".

## 2026-08-22 — AppFolio (and Google/Microsoft/GitHub…) blank in the Cockpit preview iframe

- **Error:** URL loads but the preview pane stays blank.
- **Root cause:** `curl -sI https://www.appfolio.com/` → `content-security-policy: frame-ancestors 'self' *.appfolio.com …`. The site forbids being framed by other origins; no proxy/header trick fixes this legitimately.
- **Fix:** `isKnownFrameBlocked()` in `FluidOS.tsx` shows an honest "doesn't allow embedding → Open ↗" card. For AppFolio DATA inside Dwellium the path is their API (Plus/Max plan + credentials from AppFolio support), not an iframe.

## 2026-08-22 — Cockpit nav rows kicked the user back to the classic desktop

- **Root cause:** plan 049 `openInDesktop` = `openWindow` + `fluidOsStore.setOpen(false)`.
- **Fix:** widgets open as center-pane tabs (`openInCockpit`); desktop windows opened while the cockpit is up (⌘K) are adopted as tabs; "Open on desktop ↗" is the explicit exit. ⌘K pill hidden while the cockpit is open (it overlapped the header at z 4900 > 4000).

## 2026-08-22 — `git apply` of a backend patch silently applied NOTHING (exit 0)

- **Error:** `git apply plans/052-backend.patch` run from `~/dwellium-backend/ai-dashboard369-file-manager` returned success, but `git status` showed no changes and jest still reported the old counts.
- **Root cause:** the backend git root is `~/dwellium-backend` (the app lives in the `ai-dashboard369-file-manager/` sub-folder). Agent patches carry paths rooted at the app folder (`a/src/...`). When `git apply` runs inside a subdirectory it interprets paths relative to the repo root and **silently ignores paths outside the current directory** → nothing applied, exit 0.
- **Fix:** apply from the repo root with the prefix: `cd ~/dwellium-backend && git apply --check --directory=ai-dashboard369-file-manager/ <patch> && git apply --directory=ai-dashboard369-file-manager/ <patch>`; then commit/add with `ai-dashboard369-file-manager/...` paths. Always confirm with `git status --short | grep -v '^??'` that files are actually modified, and re-run the gate (tsc + jest) expecting the NEW counts.
- **Prevention:** this is now the patch convention for backend worktree agents (they cannot run git in the backend checkout): patches are `git diff` with paths rooted at the app folder; the operator applies with `--directory=`.

## 2026-08-26 — Six days of red CI, four stacked causes (first green gate since Aug 20: run 32992380365)

- **Nobody looked at CI.** Local gates (tsc/vitest/build) were green for every push since Aug 20 while the AppFolio Parity Gate failed 10× in a row. Rule: after every push, `gh run list` — a local gate is not the repo's gate.
- **Cause 1 — FirstRunCard overlay (046-F):** floats over the desktop and intercepted the axe-baseline spec's clicks → every run died as a click timeout. Fix: `e2e/helpers/auth.ts` seeds `sessionStorage['dwellium:first-run:dismissed']='1'` + ARA prefs `introSeen`.
- **Cause 2 — masked by 1:** `nested-interactive` (serious) ×18 — the sidebar row `<button>` contained a `role="button"` remove span. Fix: row is a `<div>` with SIBLING `.sidebar-widget__main` + remove `<button>`s (Sidebar.tsx + .css). Drag/drop and visuals unchanged.
- **Cause 3 — stale screenshot baselines:** plan-053 + the sidebar restructure legitimately changed all 8 module screenshots (ratio 0.04–0.05 vs 0.03 gate). Fix: dispatch `Capture Linux Playwright Baselines` (workflow_dispatch), then merge the pushed `baseline-capture/main-<run>` branch (its PR-creation step still fails on permissions — expected).
- **Cause 4 — WebCrypto settle races:** `whiteboardCollab.test.tsx` asserted after a single `setTimeout(0)` hop; Node's `crypto.subtle` completes off-thread, so CI (and full-suite load) lost the race that fast local solo runs won. Fix: poll with `waitFor(...)` after every encrypt/decrypt; negative assertions get a real ~50 ms settle window.
- **Auto-starting overlays break e2e (2026-08-30):** a NEW full-viewport overlay that auto-shows for a fresh user (walkthrough dimmer; earlier: FirstRunCard) intercepts Playwright clicks → axe-baseline/nav specs fail in CI while vitest+build are green. Fix: seed its 'done'/'dismissed' key in `e2e/helpers/auth.ts::loginAs` (alongside `default-stack`, `first-run:dismissed`). Rule: any new auto-start overlay ships with its e2e-seed suppression in the SAME change.
- **Stale local service survives a restart (3rd occurrence, 2026-08-31):** `kill` on the listener + immediate relaunch races; the old pid keeps the port, the new one dies on EADDRINUSE and the rig silently serves OLD code (symptom: some routes 404 while siblings answer). Rule: after any local restart, assert `lsof -iTCP:<port> -sTCP:LISTEN -t | wc -l` == 1, that pid's start time is NOW, and `grep -c EADDRINUSE <log>` == 0 — before trusting a single probe.
- **Shared git stash across worktrees (2026-09-05):** two swarm agents in separate worktrees both used `git stash push/pop`; the stash is ONE stack per repo, so a pop delivered the other agent's uncommitted file. Rule: swarm/worktree agents never use `git stash`; compare with `git show HEAD:<path>`; commit early; stage by exact path. Also: Strata `AccountingModule.tsx` still carries hardcoded `MOCK_*` ledgers/accounts (fabricated names and figures) — tracked as the next fake-data item.
- **Runner eviction (2026-08-28):** a run that dies mid-step with `##[error]The operation was canceled.` after ~5 min, with NO superseding run and no newer commit, is GitHub infrastructure eviction — not your code. Re-dispatch the same SHA (`gh workflow run … --ref main`); it went green unchanged.
- **CI orchestration gotchas re-confirmed:** push webhooks arrive late and out of order; the workflow's `cancel-in-progress` concurrency then cancels a manual dispatch in favor of the delayed push run (harmless when it's the same SHA — watch the survivor). `qualia-shell/e2e/**` (incl. snapshots) is OUTSIDE the parity-gate paths filter: baseline merges never auto-fire a run — dispatch manually.
- **Env Drift Check (separate):** check3 scanned only index.html+manifest assets; the Google client-id moved two hops into the chunk graph → false FAIL since ~Aug 23. Fix: bounded transitive crawl in `Scripts/verify_deploy_env.mjs` (verified: prefix present, 262 chunks). 

## 2026-09-05 — UserContext dead-session tests failed only with `VITE_ONE_SAVE=true` (swarm C bulk hydrate)

- **Error:** `src/test/UserContext.test.tsx` → "a confirmed-dead session WITH a stored identity stays mounted and flags sessionExpired" and "logging in from an expired session clears sessionExpired" failed (`sessionExpired` stayed `false`) with Ilya's gitignored `.env` (`VITE_ONE_SAVE=true`); passed with the flag off and in CI (no `.env`).
- **Root cause:** the tests queued responses by call ORDER (`mockResolvedValueOnce` ×2: `/api/auth/me` 401, `/api/auth/refresh` 401). `oneSaveSync.bootstrap()` fires on mount from `UserContext.tsx` (`user?.id` effect); swarm C's bulk `GET /api/objects?owner=&limit=500` is issued immediately, so it consumed the refresh's 401 and the refresh got `undefined` from the exhausted `vi.fn()` → treated as a network error → session not "confirmed dead". Proof: `(globalThis.fetch as any).mock.calls` = `/api/auth/me, /api/objects?owner=u1&limit=500, /api/auth/refresh, /api/objects/…, /api/objects/…`. On main the per-store hydrates land after the refresh by microtask luck.
- **Fix:** test-only — `deadSessionFetch` routes by URL (`/api/auth/me` + `/api/auth/refresh` → 401, `/api/objects*` → 404 = "no remote object", anything else throws `Unmocked:`), same shape as the existing logout test's mock. App code unchanged (a 401 on the sync call is deduped by the existing `markAuthRejected` path). Reverted the `whenSettled()` experiment in `oneSaveStore.ts` — timing hacks did not fix an ordering bug.
- **Prevention:** any test that mounts `UserProvider` with a stored identity must mock fetch BY URL, never by order — One Save, integrations and session-health all fetch on mount. Run the auth suite both ways before calling a branch green: `VITE_ONE_SAVE=true npx vitest run src/test/UserContext.test.tsx` and `VITE_ONE_SAVE=false …` (CI has no `.env`, Ilya's machine does).

## 2026-09-07 — Inbox Zero "stopped working" / Google link "gone" — the whole chain

- **Symptom:** Inbox Zero shows "All caught up" with nothing to triage; Status Check says Gmail "No credentials configured", Calendar/Drive "OAuth2 not configured" even after linking Google several times (Section Check 2026-09-04 rows 4, 6, 40, 57).
- **Root causes (four, stacked):** (1) `statusRoutes` and the Gmail client read legacy files / `GMAIL_WATCH_EMAIL` instead of the linked-account store, so a linked account that differs from the env email reads as "not configured" — fixed in backend `3e0b3c0` (`pickLinkedAccount()`, `listEnabledGoogleAccounts()`). (2) `deploy/cloud-run.sh` wrote `GMAIL_FETCHER_ENABLED: "${GMAIL_FETCHER_ENABLED:-false}"` unconditionally, so every deploy without that var exported turned the fetcher off again — fixed on `backend/ship` (written only when exported; the script's preserve-unknowns merge keeps the live value otherwise). (3) Google OAuth app is in Testing publishing status (plan 032 decision): refresh tokens expire after ~7 days, so every link dies within a week — only fixable in the Google Cloud console (OAuth consent screen → In production). (4) Data durability: the script mounts bucket `<project>-dwellium-runtime` at `/var/dwellium` and the SQLite DB lives on that gcsfuse mount (no byte-range locks; the persistence doc explains why that is unsafe); the snapshot/restore branch is merged on `backend/ship` (`27af194`, gated on `DWELLIUM_SNAPSHOT_DIR`, no behaviour change until set) — activation needs the live preflight in `docs/persistent-data-cloud-run.md` because the snapshot path must sit outside the existing mount.
- **Could not verify live:** `/api/status` needs a session (401 without one) and `gcloud` token refresh fails non-interactively ("Reauthentication failed") — the live fetcher flag, volume mount and Gmail state are unverified until Ilya runs `gcloud auth login`.
- **Fix sequence (owner):** `gcloud auth login` → deploy `backend/ship` with `GMAIL_FETCHER_ENABLED=true` exported (plus the OAuth vars the script expects) → Control Panel → Google Accounts → connect once → OAuth consent screen to "In production" so the link stops expiring weekly.
- **Prevention:** after every deploy run `gcloud run services describe dwellium-backend --region us-central1 --format='value(spec.template.spec.containers[0].env[].name)'` and confirm `GMAIL_FETCHER_ENABLED` and the three secret names are still present; never add an unconditional line with a default to the env-file heredoc — optional vars are written only when exported.

### 2026-09-08 addendum — Inbox Zero, live evidence (gcloud signed in)

- `gcloud run services describe dwellium-backend`: revision 00053-smm, `GMAIL_FETCHER_ENABLED = true`, `GMAIL_WATCH_EMAIL` unset (backend default andy@dwellium.com), bucket `dwellium-runtime` mounted at `/var/dwellium` (so the data dir persists across deploys; SQLite-on-gcsfuse caveat stands), secrets present: OPENAI_API_KEY, GOOGLE_OAUTH_CLIENT_SECRET, BRIEF_RUN_SECRET, LIBRARY_SYNC_SECRET; `TOKEN_ENCRYPTION_KEY` set.
- `gcloud run services logs read`: `[GoogleAuth] Using account-scoped OAuth2 authentication for andy@dwellium.com` → the link exists; then `[Gmail Fetcher] Fetch cycle failed: GaxiosError: invalid_grant — Token has been expired or revoked.` So the fetcher runs, finds the linked account, and Google refuses the refresh token. That is cause (3): the OAuth app is in Testing status, refresh tokens die after ~7 days. Causes (1) and (2) are not what is failing today.
- **Immediate unblock (no deploy):** Control Panel → Google Accounts → connect again (new refresh token), then publish the OAuth consent screen so it stops expiring. The backend/ship deploy still matters for Status Check accuracy, the security fixes and the Strata routes.
- **New finding:** the fetcher logs the whole GaxiosError, which includes the token request body — the refresh token itself lands in Cloud Logging. Log only `error` / `error_description` (fix on backend/ship).

## 2026-09-05 — Strata Accounting tabs rendered fabricated tenants / bank balances / GL / journal / diagnostics (hardcoded `MOCK_*` arrays)

- **Error:** Accounting → Tenant Ledger, Bank Accounts, Bank Transfers, Journal Entries, GL Accounts, Diagnostics (and the Overview "Bank Balance" card) showed the same invented data for every account — "John Basher & Erin H. Devine", $284,500 Chase Operations, JE-2026-0301, "12 tenants have outstanding balances totaling $19,050…" — regardless of the account's real data. Same bug class as the ManagerHome / ReportingModule fix in 945c6d2 (AccountingModule was out of scope there).
- **Root cause:** five module-level constants in `qualia-shell/src/components/StrataDashboard/modules/AccountingModule.tsx` (`MOCK_TENANT_LEDGER`, `MOCK_BANK_ACCOUNTS`, `MOCK_JOURNAL_ENTRIES`, `MOCK_GL_ACCOUNTS`, `MOCK_DIAGNOSTICS`) rendered unconditionally; only `/invoices` was ever fetched.
- **Fix:** Tenant Ledger, Journal Entries and Diagnostics derive from the real `/invoices` rows (receivables grouped per tenant with a running unpaid balance; the `type:'journal'` rows the module's own New Entry modal writes; overdue-AR / overdue-AP / AP-due-within-7-days warnings whose buttons switch tabs). Bank Accounts, Bank Transfers, GL Accounts and the Bank Balance card show "Not available — connect QuickBooks" + `<NotYet>` — no read route for them exists in `strataApi.static.ts`, `strataApi.backend.ts` or the backend's `dwelliumRoutes.ts`, and `quickbooksService.syncChartOfAccounts` only logs a count (persists nothing). Test: `qualia-shell/src/test/appfolioParity/accounting.module.test.tsx` (13 tests; all 13 fail against the old file).
- **Gaps found while fixing (NOT changed):** backend `invoices.type` CHECK allows only `payable|receivable` and `dwelliumRoutes.ts` has no `POST /invoices` at all, so the New Entry modal only persists in static/demo mode; `LeasingModule.tsx` still carries `MOCK_GUEST_CARDS` / `MOCK_APPLICATIONS` / `MOCK_RENEWALS` / `MOCK_AGENTS` (same bug class, next in line).
- **Prevention:** `grep -rnE "^const (MOCK|DEMO|FAKE)_" qualia-shell/src/components --include='*.tsx' | grep -v /test/` must return nothing (as of this entry it returns only the four LeasingModule lines above). A tab with no backend source renders `Not available — connect QuickBooks` + `<NotYet reason=…/>` — never invented rows — and gets a vitest that asserts the old fake strings are absent.

## 2026-09-06 — Strata Leasing tabs rendered fabricated guest cards / applications / renewals / agents (hardcoded `MOCK_*` arrays) + invented Metrics numbers

- **Error:** Leasing → Guest Cards, Rental Applications, Renewals and Metrics (Overview cards, Leasing Funnel, Agent Performance) showed the same invented data for every account — "Cullins, Kenderequs" / "Mary H. Gallogly-Schmitt" prospects with a Zumper/Website source breakdown, "Tracy W. Terry" applications with a "Screening: Approved" column, "John Basher & Erin H. Devine" $2,650 → $2,915 renewals, "Lisa M." / "Andy K." agent conversion rates, plus "23" days-to-lease, "60%" online payments and "51%" portal adoption — regardless of the account's real data. Same bug class as 945c6d2 (ManagerHome / ReportingModule) and ea6cb34 (AccountingModule), whose entry named this file "next in line".
- **Root cause:** four module-level constants in `qualia-shell/src/components/StrataDashboard/modules/LeasingModule.tsx` (`MOCK_GUEST_CARDS`, `MOCK_APPLICATIONS`, `MOCK_RENEWALS`, `MOCK_AGENTS`) plus three literal metric values rendered unconditionally; the real `/workitems?type=lease` rows the module already fetched were only shown on the pipeline board and the Leases tab.
- **Fix:** Rental Applications lists the real lease workitems still in an application stage (`metadata.stage` ∈ applied / screening / approved — what the Add Application modal and the AppFolio seed write). Renewals derives from two real feeds: offers already prepared (`stage: 'renewal_offered'` workitems written by `POST /leasing/renewals`) shown as `pending`, and occupied `/units` whose `leaseEnd` is within 90 days or already past (the same window the backend's `GET /leasing/alerts` uses) shown as `eligible`, tenant names resolved via `/entities?type=tenant`; proposed rent has no source until an offer exists, so it renders `—` and "Prepare Offer" asks for the number (`window.prompt`) instead of guessing. Guest Cards and Metrics → Agent Performance have no read route in `strataApi.static.ts`, `strataApi.backend.ts` or the backend's `dwelliumRoutes.ts` (no workitem type, no fixture; `seedAppFolioLeasing.ts` announces "6 guest cards" but inserts none) and show an honest empty state + `<NotYet>`; the plan-053 cal.com "Schedule showing" bridge survives as a generic link. Metrics Overview cards without a live feed (Avg. Days to Lease, Online Payments, Portal Adoption) read "Not available"; Active Applications / Pending Renewals count the real rows; "Leases Signed (MTD)" → "Leases Signed" (it was an all-time count); the funnel starts at Applications with a `<NotYet>` note. All reads accept a bare array or the `{ data: [] }` envelope. Test: `qualia-shell/src/test/appfolioParity/leasing.module.test.tsx` (12 tests; all 12 fail against the previous file).
- **Gaps found while fixing (NOT changed):** (1) Vacancies → "Listing Status" is a hardcoded `LISTING_STATUS.default` (`website: true`), so every vacant unit reads "Posted" with no listing source — same class, separate tab. **Fixed the same day — see the next entry.** (2) The backend has no `GET /leasing/renewals` (only POST); the static stub returns `[]`, so renewals are derived client-side as above. (3) The module subtitle "`{leases.length}` applications" counts every `type=lease` workitem (558 in the fixture, 550 of them legacy imports without a stage). (4) Seeded rows carry `metadata.appFolioStatus` ("Converting" / "Approved") — a status string, not a screening result; the old "Screening" column had no source and is gone.
- **Prevention:** `grep -rnE "^const (MOCK|DEMO|FAKE)_" qualia-shell/src/components --include='*.tsx' | grep -v /test/` now returns nothing — keep it that way. A metric with no live feed renders `Not available`, never a plausible literal; a tab whose only backend route is a POST derives its rows from the workitems that POST writes, never from a constant; every such tab ships with a vitest that asserts the old fake strings are absent and runs red against the previous file.

## 2026-09-06 — Strata Leasing → Vacancies stamped every vacant unit "Posted" (hardcoded `LISTING_STATUS` default)

- **Error:** Leasing → Vacancies showed a green "Posted" listing badge (with a "Premium" slot) on every vacant unit for every account — 1,284 rows in the static fixture — with no listing data behind it. Flagged as gap (1) in the entry above; same bug class as the `MOCK_*` arrays.
- **Root cause:** `LISTING_STATUS` in `qualia-shell/src/components/StrataDashboard/modules/LeasingModule.tsx` was a one-entry map whose `default` (`{ website: true, internet: true, premium: false }`) was applied to every unit id (`LISTING_STATUS[u.id] || LISTING_STATUS.default`); the "Listing Status" column rendered that default as "Posted".
- **Fix:** the constant and the column are gone; the Vacancies card header carries `<NotYet reason="Listing status (website / internet syndication, premium placement) isn't tracked on units yet." />`, visible with or without rows. No source exists: `Unit` in `packages/types/index.ts` has no listing fields, `units.json` metadata carries only source / building / propertyId / notes / archived / soldDate, and neither `strataApi.static.ts`, `strataApi.backend.ts` nor the backend's `dwelliumRoutes.ts` serve one. The only listing strings anywhere in the backend are the AppFolio "Marketing Information" blobs in `seedBuenaVistaDetail.ts` / `importAppfolio.ts` (`postedToWebsite: 'No'`, `premiumListing: 'Off'`, or `'--'`) — per-property seed detail served by no route, and the one real value says *not* posted. Test: the "Vacancies" case in `qualia-shell/src/test/appfolioParity/leasing.module.test.tsx` (fails against 069c3e9, the previous commit; 13/13 pass now; appfolioParity dir 279/279).
- **Prevention:** a per-row badge fed by a map's `default` entry instead of a field on the row is invented data — every status column names the row field it reads or shows `<NotYet>`. The tab's vitest must mock a real vacant row when asserting the old badge text ("Posted", "Not Posted", "Listing Status") is absent, because the empty state never rendered the column and a rows-less sweep would pass against the bug.

## 2026-09-06 — Sweep of every Strata module for fabricated data; last hit: Communication → Forms "N submissions" counters

- **Sweep:** after the LeasingModule fixes (069c3e9, bd5601a) every file under `qualia-shell/src/components/StrataDashboard/` — 33 modules, the `__vendors` / `__maintenance` / `__properties` sub-panels, `StrataDashboard.tsx` and its siblings — was grepped for the three shapes this bug class takes: `MOCK|DEMO|FAKE|SAMPLE|DUMMY`-named constants; module- or component-level arrays of object rows carrying people / dollar / date / percent literals (`const X = [ … { … }`, single-line variants, `useState([{…}])` seeds); literal metric values (`value: '23'`, `'94.2%'`, `'$142,500'`, `'+1.3%'`, `'3 days ago'`, sparkline number arrays, string-typed numbers, `Record<…, number>` maps, hardcoded "N tenants/units/…" prose, `Math.random`); and per-row `.default` lookups. Every hit was read in context. The remaining constants (tab lists, stage/status enums, letter/form template names, integration cards, quick actions, report templates) are labels, not data, and stay.
- **Error:** Communication → Forms showed "12 / 8 / 34 / 19 / 6 / 3 submissions" beside the six form templates on every account, next to a "New Form" button with no handler.
- **Root cause:** `FORM_TEMPLATES` in `qualia-shell/src/components/StrataDashboard/modules/CommunicationModule.tsx` carried invented `submissions` numbers rendered unconditionally.
- **Fix:** the counts and the dead button are gone; the six template names stay (a catalog) and the card header carries `<NotYet reason="Form submissions and New Form aren't wired to a backend store yet — this list is the template catalog." />`. No forms / submissions route exists in `strataApi.static.ts`, `strataApi.backend.ts` or the backend's `dwelliumRoutes.ts` (it serves only `/communications` and `/communication-log`). Test: `qualia-shell/src/test/appfolioParity/communication.module.test.tsx` (4 tests; the Forms case fails against bd5601a, the previous commit; appfolioParity dir 283/283; `tsc --noEmit` exit 0).
- **Gaps found while sweeping (NOT changed):** the Letters tab's eight template buttons have no click handler (catalog only, nothing fake); `CommunicationModule.tsx` carries four pre-existing jsx-a11y lint errors on untouched lines (inbox row `div` with `onClick`; letter buttons with `onMouseOver`/`onMouseOut` and no focus/blur twins) — identical on the HEAD version, so not introduced here. **Fixed the same day — see the next entry.**
- **Prevention:** re-run the sweep greps above after any Strata module lands. The rule stands: a number, name or status the UI shows comes from a row the module fetched, or the surface says `Not available` / "No X yet" with `<NotYet>` — never a literal.

## 2026-09-06 — Communication inbox rows and letter buttons were mouse-only (4 jsx-a11y lint errors)

- **Error:** `npx eslint` on `qualia-shell/src/components/StrataDashboard/modules/CommunicationModule.tsx` reported 4 errors: the inbox message row was a `div` with `onClick` and no role, tab stop or key handler (`jsx-a11y/click-events-have-key-events`, `jsx-a11y/no-static-element-interactions`), and the letter template buttons highlighted on `onMouseOver` / `onMouseOut` with no `onFocus` / `onBlur` twins (`jsx-a11y/mouse-events-have-key-events` ×2). A keyboard user could not open a message and got no cue on which template they were on.
- **Root cause:** list rows built as clickable `div`s (block content inside, so not a `<button>`) without the role / tabIndex / onKeyDown trio, and hover-only styling.
- **Fix:** the row gets `role="button" tabIndex={0}` and an Enter / Space `onKeyDown` that calls the same `openMessage()` the click uses (the LeasingModule kanban-card pattern, so the two paths cannot drift); the template buttons get `onFocus` / `onBlur` that reuse the hover highlight through one `highlight()` helper. Tests: two cases in `qualia-shell/src/test/appfolioParity/communication.module.test.tsx` — keyboard Enter / Space opens a message; focus / blur toggles the border — both fail against afcf4b5, the previous commit (6/6 pass now). `npx eslint` on the file: 0 errors, 0 warnings.
- **Gaps found (NOT changed):** the same mouse-only `div` row pattern exists in sibling modules — `OwnersModule.tsx` line 125 and `VendorsModule.tsx` lines 345 and 346 at minimum; list them with `npx eslint src/components/StrataDashboard/modules/*.tsx | grep jsx-a11y`. **Swept the same day — see the next entry.**
- **Prevention:** a clickable non-button element ships with `role`, `tabIndex={0}` and an Enter / Space key handler in the same edit, and every hover style change ships with its focus / blur twin. `npx eslint <file>` shows 0 errors before a commit that touches the file.

## 2026-09-06 — Strata-wide: 78 clickable non-button elements had no keyboard path (153 jsx-a11y errors across 19 modules)

- **Error:** `npx eslint src/components/StrataDashboard` reported 153 errors from four jsx-a11y rules (`click-events-have-key-events`, `no-static-element-interactions`, `no-noninteractive-element-interactions`, `mouse-events-have-key-events`) at 78 sites in 19 module files: list rows, cards, section toggles and icon spans built as `div` / `span` with `onClick` and no role, tab stop or key handler; modal backdrops and bodies with click handlers; hover-only highlights. A keyboard user could not open an owner, vendor, work order, incident, audit entry or timeline event, expand a section, pick a calendar day, or remove a tag.
- **Root cause:** the same mouse-only pattern copied module to module (CommunicationModule was fixed first — entry above).
- **Fix (four recipes, all verified by the linter):** (1) 44 modal backdrop / body elements → `role="presentation"` — the click there is a mouse convenience and the keyboard path is the modal's own Cancel / × / submit buttons (LeasingModule precedent); the two `<form onClick={stopPropagation}>` bodies (Compliance add-item, Incident log) lost that handler and their backdrop closes only when `e.target === e.currentTarget`. (2) 24 rows / cards / toggles / icon spans with no nested controls → `role="button" tabIndex={0} onKeyDown={activateOnEnterOrSpace}` from the new `qualia-shell/src/components/common/keyboardActivate.ts` (Enter / Space on the element itself re-dispatch its click, so keyboard and mouse share one handler); toggles also carry `aria-expanded`, the tag-remove × carries `aria-label`; LegalModule's mousedown suggestion rows use an inline Enter / Space handler because a re-dispatched click cannot reach `onMouseDown`. (3) 6 cards that nest real buttons (ComplianceEngine Section 8 card, CorporateReview document card, Incident row, ProfileSpaces space tab, Properties card and workitem card) keep the container as `role="presentation"` and put the button role on the title element instead, so axe's `nested-interactive` rule stays clean — the re-dispatched click bubbles to the container's handler. (4) ReportingModule's report-template hover handlers gained `onFocus` / `onBlur` twins. Test: `qualia-shell/src/test/keyboardActivate.test.tsx` (3 cases: Enter / Space re-dispatch once and prevent default, other keys ignored, nested controls untouched, bubbling through a presentation container). Full vitest suite 2951/2951; `tsc --noEmit` exit 0; in-scope jsx-a11y errors 153 → 0 (StrataDashboard total 298 → 145).
- **Gaps found (NOT changed):** the remaining 145 errors are different patterns — `label-has-associated-control` (form labels without `htmlFor` / a nested control, 142), `no-noninteractive-tabindex` (MaintenanceModule, VendorsModule) and `no-autofocus` (ProfileSpaces). **The labels were fixed the same day — see the next entry.** TrelloCardModal's image lightbox closes on click only (no Escape / close button). LegalModule's suggestion dropdown hides on input blur after 150 ms, so Tab-then-Enter may race it. The Playwright axe gate was not run locally against these pages; CI runs it.
- **Prevention:** a clickable non-button element ships with `role`, `tabIndex={0}` and `onKeyDown={activateOnEnterOrSpace}` (or `role="presentation"` when the click is a mouse convenience and a real control inside carries the action) in the same edit; every hover style ships with its focus / blur twin. The four rules above must report 0 errors under `src/components/StrataDashboard` before a commit that touches a module.

## 2026-09-06 — Strata-wide: 139 form labels were not associated with their controls (`jsx-a11y/label-has-associated-control`)

- **Error:** `npx eslint src/components/StrataDashboard` reported 139 `label-has-associated-control` errors in 16 files: modal-form labels rendered as a sibling of their input / select / textarea with no `htmlFor` (Add Owner, Add / Edit Property, Property Inspection, Budgets, Add / Edit Tenant, Add / Edit Vendor, New Work Order, New Legal Issue, Log Incident, Add Compliance Item, Log Historical Event, New Journal Entry, Upload Document, Forecast property, Sentiment log, Design Studio), read-only captions written as `<label>`, and one checkbox label whose text sat three levels deep. Screen readers announced those inputs with no name and clicking a label focused nothing.
- **Root cause:** `<label>Text</label><input …/>` copied across every module form; captions for non-form content also written as `<label>`.
- **Fix:** a TypeScript-compiler-API pass (`ts.createSourceFile`, not regex) paired each flagged label with the first control that follows it and added `htmlFor="<module>-<label-slug>"` + the matching `id` (127 pairs; ids are module-prefixed and de-duplicated per file — a second form in the same module gets a `-2` suffix — and the pass looks for a keyed `.map` ancestor so a repeated row would have received an item-keyed id; none needed it). Nine captions with no control (Incident detail panel ×8, Design Studio "Design Type") became `<span>` with their inline styles intact (repo precedent: LeasingModule's vetting panel; none sit under the `.s-form-group label` CSS rule). LegalModule's two `TagInput` fields gained an `inputId` prop so their labels reach the query input. StrataAdminSettings' permission checkbox label got `aria-label={formatKeyName(key)}` because its text is nested past the rule's depth-2 scan. Verified: rule 139 → 0; no duplicate `id` and no dangling `htmlFor` in any file (grep-checked); full vitest suite 2951/2951; `tsc --noEmit` exit 0; jsx-a11y errors under StrataDashboard 145 → 3.
- **Gaps found (NOT changed):** the last 3 jsx-a11y errors are `no-noninteractive-tabindex` (MaintenanceModule 597, VendorsModule 729) and `no-autofocus` (ProfileSpaces 320); 3 non-a11y errors remain (`no-unused-expressions` in ResidentsModule 710 and TrelloCardModal 73, `prefer-const` in VisualizationModule 93). The Playwright axe gate was not run locally.
- **Prevention:** every new form field ships as `<label htmlFor={id}>` plus the control's `id` (module-prefixed, unique per file), or wraps its control; captions for read-only values are `<span>`, not `<label>`. `label-has-associated-control` stays at 0 under `src/components/StrataDashboard`.

## 2026-09-07 — ARA window text could not be selected (Classic, Holocron OS, Cockpit)

- **Error:** dragging the mouse over any ARA message selects nothing in all three interface layouts.
- **Root cause:** `src/styles/global.css` sets `user-select: none` on `<body>` (desktop-OS feel). `user-select` is not inherited, but the used value of `auto` follows the parent, so every descendant is unselectable unless it sets `user-select: text` explicitly. ARAConsole.css never opted in; all three layouts render the same `.ara-console` tree under that body, so the symptom was layout-independent. Proven both ways with a Playwright drag in Chromium: opt-in removed → `getSelection().toString()` = `""`; opt-in present → `"Selectable text check"`.
- **Fix:** `.ara-console { -webkit-user-select: text; user-select: text; }` (`fix/ara-text-select`); the side-panel resize handle `.ara-side-divider` stays `none` so dragging it does not start a selection. Same pattern Antigravity.css already used.
- **Checks:** `src/test/araConsoleCss.test.ts` (stylesheet assertions: body still `none`, root opts in, divider stays `none`) and `e2e/ara-text-select.spec.ts` (real mouse drag in each layout; asserts computed `user-select` on the root and the browser selection text; writes `test-results/ara-select-<layout>.png` with the highlight).
- **E2E lessons for the three layouts:** seed `dwellium-halocron-os` / `dwellium-fluid-os` `{enabled:true, open:true}` in `addInitScript`; the Holocron cinematic is gated by `sessionStorage 'halocron-os-intro-played'` (NOT `'halocron-boot-played'`, that is HalocronBoot); open ARA through the layout's own control (Cockpit `.fos-nav__row` "Open ARA Console", Holocron `main` → AGENTS → "ARA", Classic sidebar) because the OS shells sit over the sidebar and intercept its clicks; the chat area smooth-scrolls after a send, so poll the text node's rect until it is stable and `scrollIntoViewIfNeeded()` first — in Holocron the newest body sits below the fold of the short chat area and a blind drag lands on the "Conversation Actions" panel (selection = `"YOU\n"`).
- **Prevention:** any widget that shows text the user may want to copy must opt in at its root (`user-select: text`) — the body rule silently disables selection for everything else. Playwright helper `loginAs` remains the only session path (Andy's real record, no password, no dev accounts).

## 2026-09-08 — Terminal widget tabs: Paperclip toolbar overflowing the pane, CrewAI "refused to connect"

- **Error 1:** in the Cockpit (and any narrow window) the Paperclip tab's header buttons (Launch ▸ / Change / Reload / Open ↗ / Setup) render outside the panel to the right.
- **Root cause 1:** `.pc-toolbar` (and the sibling `.cr-toolbar`, `.lf-toolbar`) is a single-line flex row — `display:flex` with no `flex-wrap`; five `white-space: nowrap` buttons plus the URL need ~450 px, the Cockpit centre pane gives less, and nothing clips, so the row spills past the container.
- **Fix 1:** `flex-wrap: wrap; row-gap: 6px` on all three toolbars (`PaperclipPanel.css`, `CrewAIPanel.css`, `LangFlowPanel.css`). Proof: `e2e/terminal-panels.spec.ts` opens the Terminal at a 1100 px viewport in Classic and Cockpit, switches to Paperclip and asserts every toolbar button's box lies inside `.pc-panel` and the widget.
- **Error 2:** CrewAI tab → "Show control plane" shows the browser page "app.crewai.com refused to connect".
- **Root cause 2:** `curl -sI https://app.crewai.com` → `x-frame-options: SAMEORIGIN` and `content-security-policy: frame-ancestors https://bolt.new https://lovable.dev https://replit.com https://riff.new https://v0.app`. The host cannot be framed by Dwellium; the panel's reach check (`fetch` with `mode: 'no-cors'`) still reports "up" because it never sees those headers.
- **Fix 2:** the frame-blocked host list moved from `FluidOS.tsx` to `src/lib/frameBlocked.ts` (re-exported from FluidOS for existing imports) with `app.crewai.com` added; `CrewAIPanel` renders an "doesn't allow embedding → Open ↗ / Change" card instead of the iframe for any host on the list. Same pattern as the AppFolio preview card (2026-08-22).
- **"Why is there a thin context error on ARA" (no code change):** `hasThinContext()` in `ARAConsole.tsx` flags an assistant reply when it carries fewer than 2 context sources or fewer than 3 items in total. Sources are attached by the backend (`araChatEngine.ts`: Inbox, Trello, ruVector, Properties, Workitems, Entities, Georgia Code, Library, System Health). On the live service today Gmail is not linked, Trello has no key, the ruVector index is empty, Georgia Code is empty and the Library has never synced (Status Check, Section Check row 40), so most answers arrive with 0–1 sources and the notice is telling the truth. Replies answered by the browser LLM while the backend is unreachable carry no sources at all and are flagged too; `swarm/sections-fix` (8bc9a7a, unmerged) limits the notice to backend replies and adds "Pin context". The notice disappears on its own once the sources exist: connect Google, set the Trello key, re-index ruVector, ingest Georgia Code.
- **Prevention:** toolbars inside widget tabs wrap by default (`flex-wrap: wrap`) — the Cockpit centre pane is the narrowest container the app has; any new embed target goes through `isKnownFrameBlocked()` first, verified with `curl -sI <url> | grep -i -E "x-frame-options|frame-ancestors"`.

## 2026-09-08 — Fact Check Log said "No LLM configured and backend offline" whenever both paths failed

- **Error:** Ilya: "fact check says no llm configured" — while an LLM provider was configured in Settings → API Keys.
- **Root cause:** `FactCheckLog.submitManualCheck` tried the user's LLM (`callLlm` throws on any failure — 401 bad key, model, CORS…), swallowed the error in an empty `catch`, then fell back to a plain `fetch` of `/api/transcribe/fact-check`; if that threw (network) OR its body was not JSON (a proxy 502/504 HTML page makes `res.json()` throw), the one placeholder text blamed a missing LLM. A 4xx JSON answer produced no entry at all. The fallback also sent no session token, so once the transcribe routes are gated (backend/ship) it would silently 401.
- **Fix (`fix/fact-check-honest-errors`):** `src/lib/factCheckFailure.ts` builds the entry text from what happened — no active provider / provider call failed with its message / provider returned nothing — plus the backend outcome (unreachable with the error, HTTP status with the server's error text, non-JSON body with status, or success=false). The fallback sends `Authorization: Bearer <session>`. Tests: `factCheckFailure.test.ts` (wording), `factCheckLogHonestErrors.test.tsx` (widget: network failure, 504 HTML, 401 with token header). Note: the explanation renders only after the entry is expanded (click the row).
- **Still to learn from Ilya's machine:** which of the three it was for him. The new wording answers that on the next check; the API Keys "Active provider" flip to "— None —" seen in the Section Check (popup visit) is a separate open question about the per-user vault in popup windows.
- **Prevention:** never collapse two independent failures into one fixed sentence; keep the error text; any `fetch` to `/api/*` that the backend authenticates carries the session token.

## 2026-09-09 — "Multi-account connect needs the backend OAuth routes. Apply the backend patch" shown while the routes were live

- **Error:** Settings → Google Accounts showed the "apply the backend patch" note during the Cloud Run outage and afterwards while the limiter answered 429.
- **Root cause:** `listGoogleAccounts()` in `src/lib/googleAccounts.ts` treated every non-OK status the same as a 404. The routes from `Docs/Google_MultiAccount_Backend.md` §3–5 (`GET /api/google/accounts`, `POST /api/google/auth/start`, `PATCH`/`DELETE /accounts/:id`) exist on main and backend/ship and answer 401 without a token on the live service, so the note was pointing at a fix that was already in place.
- **Fix:** `classifyBackendFailure(status)` → `missing-route` (404) / `unauthorized` (401, 403) / `rate-limited` (429) / `unavailable` (5xx) / `network`; the card renders `failureNote(reason)` and a Retry button for everything except a missing route. Tests: `src/test/googleAccounts.test.ts` (each status → reason + wording; only the 404 note mentions the patch).
- **The part of the doc that was NOT implemented — §6, reading mail from every connected account:** implemented on backend `feat/gmail-multi-account-fetch` → `backend/ship` (28b47ed): `getGoogleAuthForAccount()` builds an OAuth2 client per linked account (same refresh/`needs_reauth` handling as the picked-account path), `fetchUnreadBatchAllAccounts()` reads every enabled account that granted Gmail and tags each message `sourceAccount` (the field InboxZero already renders), one failing mailbox is skipped with a redacted log line instead of hiding the rest, and the fetcher loop, `/api/gmail/fetch`, the automation engine and task routes all use it. With no linked account the legacy single-account path runs unchanged. Test: `tests/gmailMultiAccountFetch.test.ts`.
- **Prevention:** a UI note that names a fix must be tied to the one status that proves it (404 for a missing route); other statuses get their own wording and a retry. Grep every caller when changing a fetch path (`fetchUnreadBatch` had four).
## 2026-09-10 — Cockpit: clicking an already-open widget left its tab in the background

- **Error:** in the Cockpit, clicking a widget that is already open (sidebar row, ⌘K result) does nothing visible; the tab stays where it was.
- **Root cause:** those click paths do not open a window when one exists — `Sidebar.handleWidgetClick` and the palette call `focusWindow(existing.id)` (or `restoreWindow`), which only bumps the desktop window's z-index. The Cockpit adopted windows into tabs only when a *new* window id appeared (`seenWindowIds`), so a focus on a known window raised something hidden behind the overlay and never touched the tab. Holocron OS is unaffected: its own `openWidget` re-activates an existing tab, and the Classic desktop raises the window itself.
- **Fix:** `FluidOS.tsx` also follows focus — `topWindowId(windows)` (highest non-minimized z-index) is tracked while the cockpit is open; when it changes, `openInCockpit(component)` activates that tab (adopting it first if needed). While the cockpit is closed the reference is kept current so opening the cockpit never switches tabs by itself. Tests: `fluidOs.test.tsx` "brings an already-open tab to the front when its desktop window is focused" + `topWindowId` unit.
- **Prevention:** any surface that "opens" a widget must handle the already-open case in every layout: Classic raises the window, Holocron re-activates its tab, Cockpit follows the top window. New layouts subscribe to the same two signals (new window id, top window id).

## 2026-09-10 — FluidVoice "Not detected" in the Tools hub / Dictation card

- **Not a Dwellium bug.** The card is a live probe of FluidVoice's opt-in Local API on `http://127.0.0.1:47733` (`src/lib/fluidVoiceLocalApi.ts`). On this Mac FluidVoice.app is installed and running, but nothing listens on 47733 (`lsof -iTCP:47733 -sTCP:LISTEN` → none; `curl 127.0.0.1:47733` → connection refused), so "Not detected" is the truthful state: the Local API toggle is off inside FluidVoice (FluidVoice → Settings → Local API). Once it is on, the card flips to "Running"; the vocabulary seed and the dictation hand-off use the same port. If Chrome still reports "Not detected" with the port open, the remaining suspect is Chrome's private-network-access rule for https pages calling loopback; the desktop app has no such rule.

## 2026-09-10 — Google reconnect after the OAuth client moved to the Internal org project

- Refresh tokens are bound to the client that issued them: after switching `GOOGLE_OAUTH_CLIENT_ID`/secret to the new Internal client (revision 00058, secret version 8), the stored andy@dwellium.com token failed with `invalid_grant — Token has been expired or revoked` until the account was connected once more. After the reconnect (01:37 UTC) `POST /api/gmail/fetch` returned `{fetched: 0, processed: 0, accounts: 1}` with no skipped mailbox and Status Check reported Gmail, Calendar and Drive online. Rule: any OAuth client change = every linked Google account must be reconnected once.
- The one-time deploy that wrote a placeholder as the secret (2026-09-08) also left secret versions 6 and 7 invalid; the service is pinned to version 8 rather than `latest` so a bad newer version cannot be picked up silently. Add versions only through the deploy script's prompt line.

## 2026-09-10 — The "Search or ask… ⌘K" pill floated over the desktop and hid what was under it

- **Error:** the ⌘K trigger (`CommandPill`, plan 046 S2-8a) was `position: fixed` at the top centre of the shell (z-index 4900). It sat on top of window title bars and the cockpit header, covering whatever was there; it also had to hide itself while the cockpit was open.
- **Root cause:** the trigger was mounted in `AdminShell` as a free-floating overlay instead of inside a container that owns layout space.
- **Fix:** `SpacesSwitcher` takes a `trailing` node rendered at the right end of its SPACES label row (flex, `margin-left: auto`); `Sidebar` passes `<CommandPill compact={iconOnly} />` there, so the trigger reads "Search ⌘K" (+ the "?" shortcuts button) next to SPACES in the expanded sidebar and is an icon-only button in the icon rail. It still renders when the user has no Spaces. `CommandPill.css` lost its fixed positioning, z-index, backdrop filter and the ≤600 px hide; `AdminShell` no longer mounts it; the cockpit-hiding branch (and its test) is gone. `role="list"` moved from the switcher container to the row that actually holds the Space buttons, so the interactive trailing content is not a child of a list.
- **Checks:** `npx vitest run src/test/commandPillSidebar.test.tsx` (in the label row after the label, opens the palette, no-Spaces and rail variants, no `position: fixed`, AdminShell clean); `npx playwright test e2e/command-pill-sidebar.spec.ts --project=chromium` (inside the sidebar box, on the SPACES row to the right of the text, nothing fixed, palette opens, rail keeps an icon-only button).
- **Prevention:** persistent shell controls get a home inside the sidebar or a header that reserves space for them; a `position: fixed` element over the desktop is only acceptable for transient overlays (palette, banners, sheets) that dismiss.

## 2026-09-10 — Scribe: the Preview button floated over the editor instead of living in the toolbar

- **Error:** the live-preview toggle was a `position:absolute` button pinned to the top-right of `.scribe__editor-area` (`.scribe__preview-toggle` in `MarkdownPreview.css`), so it covered the first line of text, sat outside the document toolbar where every other view toggle (Contents, Minimap, Find, Focus) lives, and in a narrow window it disappeared under the ARA side panel / minimap column.
- **Root cause:** `previewVisible` was local `useState` inside `Scribe.tsx`, so the toggle had to be rendered where that state lived — in the editor column — rather than in `DocumentToolbar`, which reads its toggles from `scribeStore`.
- **Fix:** `previewVisible` / `setPreviewVisible` moved into `scribeStore` (next to `minimapVisible`); `DocumentToolbar` renders a `Preview` `ToolbarBtn` after Minimap with the same active styling; `Scribe.tsx` reads the flag from the store (hidden in focus mode like the other columns) and no longer renders the floating button; the three `.scribe__preview-toggle` CSS rules are gone.
- **Checks:** `npx vitest run src/test/scribePreviewToolbar.test.tsx` (toolbar order Contents → Minimap → Preview, store toggling, no floating class left) and `npx playwright test e2e/scribe-preview-toolbar.spec.ts --project=chromium` (button inside the toolbar box, above the editor, overlapping no sibling; with the old code the spec fails at "Preview not in toolbar").
- **Prevention:** a control that toggles a Scribe column belongs in `DocumentToolbar` backed by a `scribeStore` flag; never absolutely position a control over `.scribe__editor` — the editor area's stacking (ARA panel, minimap, preview splitter) hides it in narrow windows.
- **Related, not fixed here (backend):** `scribeRoutes.ts` stores files under `os.homedir()/.dwellium/scribe/<userId>`, which on Cloud Run is the container's ephemeral filesystem, so documents vanish whenever a new instance starts; reopening such a tab makes the store show its red status line ("Backend offline — opened local copy. …" when a local copy exists, otherwise the backend's message). Persisting under the mounted bucket (`/var/dwellium`) is the durable fix.

## 2026-09-10 — Scribe: "Backend offline — opened local copy. File not found" while the backend was up

- **Error:** the red status line under the Scribe toolbar (Ilya's screenshot, 2026-09-10) read "Backend offline — opened local copy. File not found" although the backend was serving; the tab kept working on the browser's cached copy and nothing was ever written back.
- **Root cause (two parts):** (1) the backend kept Scribe files under `os.homedir()/.dwellium/scribe/<userId>` — the container's ephemeral filesystem on Cloud Run — so every new instance started empty and `GET /api/scribe/files/*` answered 404 "File not found" for every previously saved document; (2) `scribeStore.openFile` treated any failure as "Backend offline" and left the local copy `dirty: false`, so the file was never re-uploaded.
- **Fix:** backend `e73a8c0` (`services/userDataDir.ts`): Scribe documents, Scribe images and File Explorer files follow `DWELLIUM_DATA_DIR` (the mounted bucket, `/var/dwellium/data`) and fall back to `~/.dwellium` only when no data dir is configured. Frontend: `openFile` now distinguishes a network failure (`TypeError` from fetch → "Backend offline — opened local copy. …") from a backend answer ("The backend has no copy of this file (File not found) — opened your local copy and re-uploading it."), opens the copy `dirty: true` and calls `saveFile`, whose `PUT` re-creates the file (the route does `mkdir -p` + `writeFile`).
- **Checks:** `npx vitest run src/test/scribeStore.test.ts` (404 with a local copy → honest message + a PUT; TypeError → "Backend offline", no PUT); backend `npx jest tests/userDataDir.test.ts`. Live: after the backend deploy, reopen a Scribe tab and save once — the red line does not come back on the next instance restart.
- **Prevention:** anything the backend writes per user goes under `DWELLIUM_DATA_DIR`, never `os.homedir()` or `process.cwd()`; a store may only say "offline" when the request never got an answer.

## 2026-09-10 — Anam Avatar Engine: "is it enabled?" — the card's Enabled checkbox did nothing

- **Question (Ilya):** make sure the Anam API is enabled and functioning.
- **Findings:** the integration is browser-direct: `avatarClient.ts` reads the user's Anam key from the vault (Settings → API Keys → Anam Avatar Engine) and calls `https://api.anam.ai/v1/auth/session-token` with `personaConfig`; the harness (`AvatarHarness.tsx`) uses Anam only for agents whose avatar profile picked "Anam (your key)" in the Avatar setup panel, an option that appears once a key is saved. `getConfigured()` checks key presence only — `AnamConfig.enabled` is documented as informational, yet the card rendered an "Enabled" checkbox bound to it, so the switch looked real and did nothing. Contract check against Anam's docs (`api-reference/sessions/create-session-token`, 2026-09-10): `personaConfig.avatarId` required for ephemeral configs, `voiceId`/`llmId`/`systemPrompt`/`name` optional, `personaId` for pre-created personas — matches `PersonaConfigInput`; the response field is `sessionToken`; no breaking changes in Anam's changelog Feb–Sep 2026; `@anam-ai/js-sdk` is pinned at 4.10.0 (latest 4.27.0, additive releases). `api.anam.ai` answers 401 "Missing or invalid API key" without a key (reachable); the Netlify CSP is report-only and lists api.anam.ai in connect-src. The backend route `POST /api/ara/avatar/session-token` (server-side `ANAM_API_KEY`) has no frontend caller and no deploy-script wiring — dead path, harmless.
- **Fix:** the card shows the real state ("Active — key saved" / "Not configured", `role="status"`) and explains that each agent opts in under Avatar setup → Provider; the inert checkbox is gone and the card no longer writes `enabled`. `src/test/anamCardStatus.test.tsx`.
- **Not verifiable from here:** whether Ilya's own vault holds a key (keys are never read by agents) and a live session mint (needs his key). Check in the app: the card reads "Active — key saved", then an agent's Avatar setup → Provider → "Anam (your key)" → the harness goes idle → connecting → live.
- **Prevention:** a control in Settings must be bound to something the runtime reads; if a flag is informational, show status text, not a toggle.

## 2026-09-10 — Cockpit terminal: a red "Failed to fetch" that never went away, "Live" after the backend dropped the session, a prompt line per layout tick

- **Error:** Ilya's Cockpit screenshot (2026-09-10): the Workspace Terminal header showed "Failed to fetch" while the shell kept answering, and the output was a column of `root@localhost:~#` prompts.
- **Root cause (three parts):** (1) `pollOutput` ran every 350 ms and any single rejected request (network blip, Cloud Run instance swap, laptop wake) went straight into `setError`, which only `createSession` ever cleared — one dropped poll became a permanent red status; polls could also overlap. (2) When the backend answered 404 (it keeps sessions in memory; a replaced instance has none) polling just stopped and the header kept saying "Live". (3) Every `ResizeObserver` tick posted `/resize`; the backend's real PTY (node-pty is built into the image) delivers SIGWINCH and bash reprints its prompt, so the Cockpit's frequent column resizes filled the output with prompts.
- **Fix:** `Terminal.tsx` — an in-flight guard; a failure counter that shows "Terminal output is not reaching the browser (…) — still retrying." only after 3 consecutive failures and clears it on the next success (input errors clear on the next successful keystroke the same way); a 404 marks the session `closedAt` (header reads "Exited") with "Terminal session ended on the backend — press Restart for a new shell."; `resizeSession` sends a resize only when cols/rows differ from the last one sent (reset per session).
- **Checks:** `npx vitest run src/test/Terminal.test.tsx` — one failed poll → no status; a streak → status while the header still says Live; success → cleared; 404 → Exited + Restart hint; two same-size resize ticks → one POST, a size change → a second. With the previous Terminal.tsx the three new tests fail.
- **Prevention:** a polling widget may only surface an error after a streak, must clear it on recovery, and must translate a "session gone" answer into visible state; never send a PTY resize for an unchanged size. Sessions on Cloud Run are per-instance and ephemeral — a "session ended" state is normal after a deploy or scale event, not a bug to hide.

## 2026-09-12 — Strata › Properties: exterior photos on cards, rows and the detail header

- **Ask (Ilya):** add property images "off of Zillow or other public information" with best-practice placement.
- **Source decision:** Zillow/MLS listing photos are copyrighted broker images and Zillow's terms forbid scraping, so they are not used. The per-address public source is the Google Street View Static API (Maps Platform, licensed for display with Google's attribution). The key lives only on the backend (`GOOGLE_MAPS_API_KEY`, Secret Manager `dwellium-google-maps-api-key` via `deploy/cloud-run.sh`); the browser sees a proxied image.
- **Backend:** `services/streetView.ts` + `GET /api/dwellium/properties/:id/street-photo` (`dwelliumRoutes.ts`). Metadata call first (free, no quota) → only `OK` fetches the image (`640x400`, `fov=80`, `source=outdoor`, `return_error_code=true`). Cached under `DWELLIUM_DATA_DIR/property-photos/<id>.jpg` + `.json` (copyright/date); `ZERO_RESULTS`/`NOT_FOUND` cached as `<id>.none.json` for 30 days; Google errors (denied/quota/network) throw → 502, never cached as "none". Route answers 503 `{ needsSetup: true }` without a key, 404 when there is no imagery. `tests/streetView.test.ts` (5 cases).
- **Frontend:** `modules/PropertyPhoto.tsx` (+ `.s-prop-photo*` in `StrataDashboard.css`), placed as the first element of each grid card (16:10, rounded, above the name), a 56×42 thumbnail at the left of each row, and a 21:9 hero above the detail header with a "Street View · date · © …" caption. An office-uploaded photo (`metadata.photos[0]`) wins over Street View. `strataGetBlob` added to `strataApi` (backend twin: authenticated binary GET keyed on status; static twin: 503). Honest states: 503 → nothing rendered and remembered for the session (no repeated asks); 404 → a quiet "No street photo" tile that keeps the grid aligned (rows render nothing); one shared request per property across card/row/hero. `src/test/propertyPhoto.test.tsx` (5 cases).
- **Owner steps to turn it on:** enable "Street View Static API" in `my-project-57391aion-ethos-api`, create an API key restricted to that API, then deploy with `GOOGLE_MAPS_API_KEY` exported (hidden prompt) so the deploy script stores it as the secret. Until then the Properties tab looks exactly as before.
- **Prevention:** never render a stock or placeholder photograph for a real property; a photo component has three honest states (image / none / not configured) and the not-configured state must be invisible, not a broken image.

## 2026-09-12 — Tools hub: "Needs setup" tiles, and how Scheduling (Cal.com) was actually set up

- **Ask (Ilya):** set up every Tools hub tile that says "Needs setup".
- **How the tile works:** `resolveToolStatus` (`src/data/toolsHub.ts`) reads `ready` only when the widget is registered AND the tool's `VITE_<TOOL>_URL` is present in the Vite env — i.e. set on Netlify at build time and inlined into the bundle. Nothing at runtime can flip it; a redeploy is required after setting the variable. The four gated tiles: E-Sign `VITE_DOCUMENSO_URL`, Scheduling `VITE_CALCOM_URL`, Broadcasts `VITE_LISTMONK_URL`, Photo Vault `VITE_IMMICH_URL`. Do not set a URL with nothing real behind it — the tile would read Ready over a dead iframe.
- **Scheduling, done:** `netlify env:set VITE_CALCOM_URL https://cal.com/andy-zohoury-pvxarr --context production`, then `netlify api createSiteBuild`; in the signed-in cal.com account the four event types the widget keys on (`calcomLinks.ts`) were created with the exact slugs `showing-30min` (30), `maintenance-window-2h` (120), `vendor-visit-1h` (60), `move-in-out-walkthrough-45min` (45). Verified: all four booking URLs answer 200; the booking page sends no `X-Frame-Options`/`frame-ancestors` so the Book tab embeds it; on production (offline-session probe) the Tools hub row reads "Scheduling … Ready · Open" and the widget's iframe src is the booking page. Still owner-side: `CALCOM_API_KEY` on the backend for the Upcoming tab (cal.com → Settings → API keys).
- **The other three need the owner first** (verified 2026-09-11: Netlify had none of the four vars; gcloud here has no active account; Tailscale is not installed on this Mac; the kits say "Ilya runs these — agents never run gcloud"): Broadcasts + E-Sign = the free e2-micro VM + DNS + Brevo per `tools/listmonk/README.md` and `tools/documenso/README.md`; Photo Vault = the office Mac with Docker Desktop + Tailscale per `tools/immich/README.md`. Once each URL exists, the same env:set + rebuild flips the tile; the backend halves (`LISTMONK_*`, `DOCUMENSO_*`, `IMMICH_*`) ride `deploy/cloud-run.sh`.
- **Prevention:** a Tools hub tile is proof of a working URL, not a checkbox — set the variable only after the service answers, and verify on production the same way (row text + iframe src), not from the Netlify UI alone.

## 2026-09-12 — Scribe › Interactive Docs: "an error whenever I click it" — a stale tab after a deploy

- **Report (Ilya, 2026-09-10):** clicking the Interactive Docs mode showed an error. Re-checked in his signed-in production session on 2026-09-12: the library renders, no failed requests, no console errors — not reproducible on a fresh tab.
- **Root cause (most likely, unconfirmed in his session):** `Scribe.tsx` loads the mode with a bare `React.lazy(() => import('./idocs/InteractiveDocs'))` (sub-component altitude, per the 2-layer rule). Nine production deploys shipped on 2026-09-10; each build renames the chunks, so a tab opened before a deploy 404s on the old `InteractiveDocs-<hash>.js` the first time the mode is clicked, and the rejection surfaced as a generic error.
- **Fix:** `IdocsLoadBoundary` (in `Scribe.tsx`) wraps the Suspense. A chunk-load failure (`isChunkLoadError`, now exported from `utils/lazyWithReload.ts`) reads "Interactive Docs didn’t load — a newer Dwellium was deployed since this tab opened." with Reload and Back to Doc; any other error shows its message with Try again. `src/test/idocsLoadBoundary.test.tsx`.
- **Prevention:** every bare `React.lazy` inside a widget needs a boundary that distinguishes "newer deploy, reload" from a real crash; a generic "Something went wrong" after a deploy is a bug report waiting to happen.

## 2026-09-17 — Universal Shell: nothing persisted, and the scratch pad was shared between accounts (plan 058)

- **Found (code-read assessment, Ilya's ask):** the shell's active container was a bare `useState` in `UniversalShell.tsx` — closing the window or reloading always landed on the first container. The scratch pad wrote ONE device-global key `dwellium-universal-scratch` (`adapters/WorkspaceHomeColumns.tsx`), so two accounts on one browser read and overwrote the same note, and it never rode One Save. Smaller: the widget tip promised a drag-from-cabinet-to-canvas that has no code behind it; the header printed the internal surface enum ("ANY"); the Astra Portfolio container was placeholder text in all four columns; the empty-column copy was a dead end; no test mentioned the shell.
- **Root cause:** the shell shipped as an F-1 scaffold before the per-user store pattern existed (its own header comment said "single useState for now") and was never brought onto it.
- **Fix:** `src/utils/universalShellStore.ts` — `gridLockStore.ts` sister shape: dynamic key `dwellium:universalShell:<uid>`, holder `universalShellUserIdHolder` registered in `perUserIdentity.ts::ALL_HOLDERS`, One Save `withSync('universalShell')`, state `{ activeContainerId, scratch }`. The shell and the scratch pad read/write through it. An empty per-user key adopts the legacy global note once; the legacy key is never written or removed. `initialContainerId` is applied once per mount by writing it into the store — deriving the active id from the prop on every render pins the switcher (caught in review before it shipped; nothing passes the prop today). Tip text rewritten, "ANY" badge dropped (readable "Strata"/"Astra" otherwise), Astra removed from `ADAPTER_REGISTRY` behind a `ponytail:` comment (adapter file kept), empty-column copy points at the switcher. `src/test/universalShellPersistence.test.tsx` (7 cases).
- **Second bug, found by the swarm reviewer and fixed at the root:** `oneSaveStore.ts::makeSynced.hydrate()` applied the remote payload unconditionally after its `await`. A lazily-loaded store registers — and hydrates — the moment its widget first opens, so typing into the just-opened scratch pad while that GET was in flight got overwritten by the older remote snapshot (every `withSync` store had the race; this is the first per-keystroke text field on one, so the first place it would bite). Fix: a `localWriteSeq` counter bumped in `set()`; `hydrate()` captures it before the await and returns without applying if it moved — the local value is newer and already queued for write-through. `src/test/oneSaveStore.test.ts` › "hydrate never overwrites a local edit made while its GET was in flight" (with a no-edit control). Also: the first two persistence tests originally passed with the `localStorage` write deleted (module cache survived the remount) — they now `universalShellStore.reset()` between unmount and remount; both fixes were mutation-checked (break the code → 4 tests fail → restore → green).
- **Prevention:** any widget state a user would expect to find again goes on a per-user `createLocalStorageStore` + `withSync` store from the first commit — never a bare `useState`, never a device-global `localStorage` key. A prop named `initial*` must seed state once, not take part in every render's derivation. A widget `tip.tryThis` must name something the widget does today. A remount test proves persistence only if the in-memory store is reset between the two mounts. Anything applied after an `await` must re-check that the world did not move during it.

## 2026-09-18 — Production: "API error 429" in Properties, Calendar dead, Inbox Zero "offline" (plan 059)

- **Report (Ilya):** after the plan-058 deploy, Properties showed API error 429, Calendar did not load, Inbox Zero read offline.
- **Root cause (verified live):** backend `globalApiRateLimiter` (`src/app.ts`) = 300 req/min keyed by `req.ip`. On Cloud Run (`trust proxy` 1) behind Netlify's `/api/*` proxy, `req.ip` is Netlify's egress address, so every browser session shared ONE bucket (same minute: direct curl `x-ratelimit-remaining: 296`, via Netlify `299` — two buckets). One page load fired ~60 `/api` calls in 8 s, mostly `PUT /api/objects/whiteboard_<uid>` (Excalidraw onChange on mount, 1.5 s debounce), and `oneSaveStore` retried each 429 three times, tripling the load → bucket empty → every `/api` call 429 for the rest of the minute (`/api/auth/me`, properties, calendar, inbox). Not related to the shell deploy; direct Cloud Run `/health` was 200 throughout. `GET /api/inbox/stream` → 404 is separate and harmless (no backend branch has the route; `readSse` fails, the 60 s poll covers it).
- **Fix:** frontend `oneSaveStore.scheduleWriteThrough` stops after ONE attempt when `syncRateLimitStore` reports a 429 (replay still parked for reconnect; banner still shown). Backend `apiRateLimitKey(req)`: signed-in traffic keyed by `sha256(bearer)[0:16]`, anonymous by IP — users no longer share Netlify's bucket. Trade-off accepted: a caller inventing tokens against the direct Cloud Run URL gets its own bucket; those requests 401 immediately and login keeps its own 20/15 min limiter.
- **Prevention:** a retry loop must treat 429 as "stop", never as "try again in 500 ms". Any limiter behind a proxy must key on something the proxy does not collapse (session, or the real client IP with the right `trust proxy` hop count). When several widgets go "offline" at once, check one `/api` call's status through the proxy before touching any widget.

## 2026-09-18 — Sync resilience + rate-limit UX (plan 060, follow-up to 059)

- **Why:** 059 stopped the retry amplification and un-shared the backend bucket; everything else that made the 429 storm possible and confusing was still there — no `Retry-After` handling, three different-looking failures for one cause, a whiteboard that PUT the whole board on mount, per-object writes on every load, one bucket for reads and writes, no burst room, and Inbox Zero calling a route that does not exist.
- **Frontend (branch `feat/060-sync-resilience`):** `syncRateLimitStore` carries `retryAt` from the backend's `Retry-After` (60 s default); `oneSaveStore` arms its one flush timer for the remaining pause so writes coalesce. `backendStatusStore` gains `'rate-limited'` + auto-clear; the global `/api/*` fetch wrapper (`installApiAuthFetch.ts`) reports every 429 there, so `BackendConnectionBanner` shows "Too many requests — retrying in N s" and `SyncStatusPill` reuses the text; Properties' raw "API error 429" now goes through `classifyBackendFailure`/`failureNote` (moved to `lib/backendFailure.ts`, re-exported from `googleAccounts.ts`); Calendar and Inbox Zero show loading, not broken, while limited. `whiteboardStore.saveSceneDebounced` skips when the scene's content key (element count : max element version : file count) equals the last persisted one — seeded from the stored board on a fresh mount so Excalidraw's init-time `onChange` never re-PUTs. All One Save write-throughs go into ONE module-level flush queue → `PUT /api/objects/batch` (per-id outcomes feed each store's existing bookkeeping; 404 = old backend → per-id `put` for the session). Inbox Zero's `readSse('/api/inbox/stream')` removed; the 60 s poll was already the real mechanism.
- **Backend (branch `feat/060-rate-limit-buckets`, `3530ec8`):** read (600/min, 80 per 10 s) and write (300/min, 60 per 10 s) limiters, both keyed by session; `Retry-After` reports the soonest reopening bucket; `PUT /api/objects/batch` (≤50, owner-checked upsert, 200/207); `deploy/cloud-run.sh` `TRUST_PROXY_HOPS=2`; test harness raises the limits so suites exercise the limiter without tripping it.
- **Gotchas met:** (1) 43 test files mock `oneSaveClient` — anything `oneSaveStore` needs from that module must live in a leaf module or be guarded (`putBatch` missing on a mock ⇒ treated as unsupported). (2) `researchLabImportGuard.test.ts` pins `oneSaveStore.ts` imports — extend the pinned list deliberately, never add an import silently. (3) The new burst bucket tripped the pagination integration tests: rate limits belong in the test harness env, not in each test. (4) A per-board skip must be seeded from persisted state, or the first mount-time save still goes out.
- **Prevention:** every 429 path reads `Retry-After`; every widget failure goes through `classifyBackendFailure`; a write coalescer has one timer and one queue; a limiter behind a proxy keys on session or real client IP; a "skip if unchanged" guard is tested from a cold start, not only from a warm one.

## 2026-09-19 — Properties: 50 street-photo requests per load against an unconfigured backend

- **Seen (production network log after plan 060 shipped):** every Properties load fired one `GET /properties/:id/street-photo` per card — ~50 requests — and every one answered `503 { needsSetup }` because `GOOGLE_MAPS_API_KEY` is not set on Cloud Run. Half of the page's API traffic, all for nothing; it was the biggest single contributor to the read-burst 429s.
- **Root cause:** `PropertyPhoto.tsx` already had a session latch ("one 503 means unconfigured until reload"), but all 50 cards mount in the same tick and issue their fetch before the first 503 has returned, so the latch never fires in time.
- **Fix:** the first request of the session is the probe; every later card awaits it and re-checks the latch before fetching. A 200 probe blocks nothing (the others fetch as before). `propertyPhoto.test.tsx`: 50 cards + 503 ⇒ exactly one request (fails without the wait); 3 cards + 200 ⇒ three requests.
- **Prevention:** a "remember the first answer" latch is useless against a fan-out that mounts in one tick — gate the fan-out on the first in-flight promise, and test with N components, not two in sequence.

## 2026-09-19 — Cloud Run: SQLite on gcsfuse corrupted itself; cutover to local disk + verified snapshots (plan 061)

- **Seen:** `OutOfOrderError` storms in the backend log (gcsfuse rejecting out-of-order journal writes); the cutover revision refused to boot because every auto-snapshot failed `quick_check`; locally `integrity_check` = `database disk image is malformed` (pages 8193+ "never used", `design_history` b-tree broken, everything else readable).
- **Root cause:** the deploy script kept `DWELLIUM_DATA_DIR` on the gcsfuse mount although the design said local disk + snapshots; gcsfuse does not support SQLite's write pattern, so lost journal writes slowly corrupted the live file, and the snapshot writer copied the damage verbatim (`db.backup()` copies pages).
- **Fix (backend `feat/061-snapshot-activation`):** `DWELLIUM_DURABLE_DIR` / `ONE_SAVE_EVENTS_DIR` keep Scribe files, LanceDB, caches and the 283 MB whiteboard event log on the mount; `SNAPSHOT_CUTOVER=1` moves the DB + One Save state to `/var/dwellium-local/data`; snapshots run `quick_check` before publishing and restore falls back newest→oldest; a 120 s watchdog on the snapshot job; `repairAndSeed.sh` (`.recover` → integrity ok → row-count diff → seed with the 74 state files). Seed uploaded, cutover deployed, verified on `00077-qlv` (restore line, 0 `OutOfOrderError`, Properties/One Save 200). `design_history` lost (accepted).
- **Prevention:** SQLite never lives on gcsfuse. Snapshots are verified before publish, so a corrupt live file can no longer poison the restore chain. Never `gcloud run services update` on top of a revision that failed to boot (it inherits the broken template — that produced an empty-DB revision for ~10 min); pin traffic, then set env explicitly. `gcloud run deploy` prints the revision holding traffic, not the one it built — check `revisions list` when traffic is pinned.

## 2026-09-19 — Saying "open research lab" to ARA did nothing (typed + Enter worked)

- **Error:** Ilya says "open research lab" with the ARA mic on; the words appear in the composer and nothing opens. Typing the same phrase and pressing Enter opens Research Lab (verified on argyleholocron.netlify.app 2026-09-19: the Research Lab tab came to the front and ARA acknowledged).
- **Root cause:** ARA had **no voice-submit path**. `toggleMic` → `startDictation(…, { onText: setInput })` only streams speech into the composer; recognition is `continuous`, and `onEnd` just refocuses the input. The `holdToTalk` pref in `araPrefsStore.ts` is wired to nothing in the UI. The parser was never the problem — `parseCommand` already resolves every phrasing ("Open research lab.", "Aura, open research lab", …) to `research-lab` because `WIDGET_ALIASES` is derived from the whole registry. A `skill-open-widget` was proposed; it would have duplicated the Conductor's Tier-3 command.
- **Fix:** `startDictation` takes an optional `onFinal(utterance, isWholeInput) => boolean`; returning true consumes the utterance (not appended). `ARAConsole` consumes only when the utterance is the WHOLE input and `parseCommand` accepts it, then calls the same `dispatchTiers` Enter runs, and stops the mic so ARA's spoken ack is not transcribed back into the box. Questions/drafts still dictate. Tests: `araDictation.test.ts` (consume / decline / whole-input), `dwelliumCommands.test.ts` (seven spoken phrasings → `research-lab` on the open-widget bus).
- **Not verified live:** the spoken path needs a real microphone + Web Speech API in a signed-in session — not drivable from an agent. Unit-verified only until Ilya says the phrase on a deploy that contains the fix.
- **Prevention:** when "ARA doesn't do X when I say it", type X + Enter first. If typing works, the bug is the voice→dispatch seam, not the parser or the widget. One tap = one command today (mic closes after a command); making it continuous needs echo handling for ARA's TTS first.

## 2026-09-22 — The local dev backend's god-user API was reachable from the network (RCE)

- **Error:** With `AUTH_ENABLED` unset (the launchd dev backend and the Electron sidecar) `authMiddleware` serves every request as the dev god user. `app.listen(port)` took no host, so it bound every interface: `http://[<Mac's public IPv6>]:3000/health` → 200, and `/api/terminal/sessions` (a real node-pty shell) → 200 with no token.
- **Root cause:** dev-mode auth bypass + wildcard bind were each fine alone; together they put a shell on the LAN. Electron's comment "sidecar binds 127.0.0.1 only" was never enforced by code.
- **Fix:** backend `src/services/bindHosts.ts` — `BIND_HOST` if set; all interfaces only when `AUTH_ENABLED=true` (Cloud Run); otherwise `127.0.0.1` **and** `::1` (macOS resolves `localhost` to `::1` first). Proven on real sockets: auth off → 200 on both loopbacks, refused on LAN v4/v6; auth on → reachable, `/api/terminal` 401. Dwellium#8.
- **Prevention:** any new listener in the backend goes through `resolveBindHosts()`. "Works on localhost" says nothing about what else can reach the port — `curl` the machine's LAN address too.

## 2026-09-22 — Port 3000 was shared: the backend on IPv6, a Docker container on IPv4

- **Error:** `http://127.0.0.1:3000/health` returned inbox-zero's HTML (404); `http://[::1]:3000` returned Dwellium. The app worked only because `localhost` → `::1` on this Mac.
- **Root cause:** `inbox-zero-services-web-1` maps `0.0.0.0:3000`. macOS lets a host-less (wildcard `::`) listener share a port that a specific-address listener already holds and routes each connection to the more specific socket — so the backend silently got IPv6 only. (Linux refuses the second bind with `EADDRINUSE`; verified in `node:22-alpine`.)
- **Fix:** same loopback bind as above — a specific `127.0.0.1:3000` bind wins that address back. `~/Library/Application Support/Dwellium/start-backend.sh` now logs a WARNING naming the foreign holder at startup.
- **Prevention:** before blaming the app, `lsof -nP -iTCP:3000 -sTCP:LISTEN` and check BOTH families answer with Dwellium's JSON.

## 2026-09-22 — `/api/terminal`, `/api/files` and 13 more routers had no role gate; `requireRole('admin')` admitted everyone

- **Error:** a `tenant` account could spawn a shell (`POST /api/terminal/sessions` → 200), download and delete any file, grant itself file permissions, read the operator's inbox through ARA, and list every invoice. Two routes used `requireRole('admin')`; `'admin'` is not in `ROLE_HIERARCHY`, unknown roles scored −1, every real role outranks −1 → the gate passed everyone.
- **Root cause:** routers mounted with `authenticate` only; `hasMinRole` failed open on an unknown *required* role; `checkFileAccess()` was dead code that returned true for every row.
- **Fix:** `hasMinRole` fails closed + a test scans `src/` for unknown role names; `requirePermission()` gates on the same per-user grants the UI uses to show widgets (throws at load on an unknown key); 13 routers gated with tenant-403 *and* granted-user-200 cases each; per-file ACL (`fileAcl.ts`) makes `restricted` real. Dwellium#7.
- **Prevention:** a denial test can pass for the wrong reason — assert the exact 403 and make sure the fixture exists (a missing route/fixture gives 404). Every gate needs a positive case or it can "pass" by breaking everyone.

## 2026-09-22 — Every file-based e-sign send was refused by Documenso, and no test could see it

- **Error:** "PDF from Dwellium files" → `POST /api/esign/send` → 502 `Documenso envelope distribute failed (400)`, and an orphaned DRAFT left in Documenso. Documenso's actual reason: `MISSING_SIGNATURE_FIELD — Signers must have at least one signature field`.
- **Root cause:** the file flow (`/envelope/create` → `/envelope/distribute`) sent recipients with no fields. Templates carry their own fields, so the template path worked and the stubbed tests — which accept any payload shape — were green throughout.
- **Fix:** one signature box per SIGNER in the create payload (percent-positioned, stacked). Re-run live: 200, envelope PENDING, email delivered with the consent disclosure and signing link, audit-log PDF downloads, signing page serves. `7c8028c`.
- **Prevention:** a stub that accepts anything verifies nothing about the upstream contract. Keep one live smoke against the real Documenso (`tools/documenso/connect-local.sh` + a send to a mailpit address) in the release checklist.

## 2026-09-22 — Backend suite failed ~1 run in 8 in a random suite (`socket hang up`, stray 404, `Parse Error`)

- **Error:** every suite passed alone; full runs failed randomly. Diagnostic caught it: the failing request's own server had received **zero** connections.
- **Root cause:** supertest starts a per-request server with `app.listen(0)` (wildcard `::`) but connects to `127.0.0.1`. A Postgres (paperclipai's embedded one) and a chromedriver on this Mac hold ephemeral-range ports on `127.0.0.1`/`[::1]`; when the kernel handed supertest one of those port numbers, the squatter got the request — non-HTTP bytes → hang up / Parse Error, chromedriver → 404.
- **Fix:** `tests/helpers/harness.ts` starts one server per file bound to `127.0.0.1` (the kernel can't assign a busy port on a specific address); `tests/setup/supertestLoopback.ts` makes direct `supertest(app)` users connect in the family they bound. 30/30 clean full runs after; 4/30 before. `247c806`.
- **Prevention:** `--runInBand` shares the process, but the *kernel* is shared with every other process on the box. When a flake is "random suite, passes alone", instrument the server side (`connection` count) before theorising about the client.

## 2026-09-22 — Local Documenso: sign-in, sign-up and forgot-password all failed silently

- **Error:** every auth action failed with a generic toast; no reset email; "unable to create your account". Pages rendered fine.
- **Root cause:** `tools/documenso/.env` had `NEXT_PUBLIC_WEBAPP_URL=http://127.0.0.1:3101` while the container is published on **3140**. Server-rendered pages worked; every client-side tRPC call went to 3101 → `ERR_CONNECTION_REFUSED`. The network tab showed it in one look; the database did not (two wrong theories first).
- **Fix:** URL corrected to 3140, container recreated; `session-json` → 200. Reset mail then landed in **mailpit** (`127.0.0.1:8025`) — never Gmail; SMTP is the local catcher.
- **Prevention:** for "the page loads but nothing works", open the network tab before the database. At production cutover `NEXT_PUBLIC_WEBAPP_URL` must equal the public URL or auth breaks the same way.

## 2026-09-22 — Entity Guardian blocks valid PDFs (open)

- **Error:** a 616-byte, spec-valid PDF upload → 403 `Upload blocked … PHONE (6), BANK_ACCOUNT (6)`.
- **Root cause:** the guardian regex-scans **raw bytes** (`buffer.toString('utf-8')`); `\b\d{8,17}\b` matches the six 10-digit byte offsets in a classic PDF xref table. Mode is STRICT with no exemption for binary formats.
- **Status:** not fixed. Worked around in the live test with an xref-stream PDF (binary offsets, no digit runs — what most modern PDFs use). Decision needed: text-extract before scanning, or exempt binary formats.
- **Prevention:** scanners that run over bytes will find "PII" in any binary container; scan extracted text.

## 2026-09-23 — One Save hydrate wrote the previous account's payload into the new account's key

- **Error:** (found by the wiki-hardening adversarial review, proven with a runtime test) switching accounts while a store's hydrate GET was in flight left account A's remote payload in account B's localStorage.
- **Root cause:** `makeSynced.hydrate()` issued the GET for the owner at call time, but after the `await` applied the payload through the dynamic-key base store, whose key now resolved to the NEW owner. Every `withSync` store had it; the new `merge` option made it worse by folding A's pages into B's local map.
- **Fix:** `hydrate()` captures `ownerAtStart` before the await and returns if `ownerId()` changed (`src/lib/oneSaveStore.ts`). The bulk-prefetch path was already safe (it looks up the NEW owner's object id). Regression: `src/test/oneSaveOwnerRace.test.ts` (mutation-checked).
- **Prevention:** anything applied after an `await` must re-check both "did local change" (`localWriteSeq`) and "is it still the same owner".

## 2026-09-23 — Wiki widget: container query never fired; staleness judged cited sources

- **Error:** at a 460px window the Wiki sidebar overflowed off the left edge; every page would have shown "Out of date".
- **Root cause:** (1) `.wiki-root` was both the `container-type` element and the target of the `@container` rule — a container query cannot restyle its own container. (2) staleness compared the page's *cited* sources (the LLM may cite a subset) with the node's files.
- **Fix:** outer `.wiki-host` is the query container; pages store `inputs` (every file compiled from) and staleness uses it; legacy pages without `inputs` are judged by modification time only.
- **Prevention:** put `container-type` on a wrapper, never on the element the query restyles. Verify responsive CSS with a real render (the standalone harness), not jsdom — all unit tests were green through this bug.

## 2026-09-24 — Inbox Zero: 8 dead tabs, dead Triage actions, a false "undo" (plan 066 phases 1–3)

- **Error:** Rules, NIF Intel, Actions, Analytics, Cold Block, Replies, Tracker and Audit Log showed empty panes forever; the search box did nothing; Snooze / Add Label silently no-oped; Undo toasted "Action undone and item recovered" while the item stayed archived; an archive that failed on the server showed nothing.
- **Root cause:** the widget was built against a backend that never existed — `/api/v1/inbox/*` (no `/api/v1` mount at all), `/api/inbox/{actions,tracker,audit/global,:id/status,:id/snooze,bulk-label,ai-classify}` (not in `inboxRoutes.ts`); `GET /api/inbox` ignored `search`; handlers never checked `res.ok`. Only `InboxZero.tsx` and `inboxLinkage.ts` had tests, so every gate stayed green. Separately, `/api/gmail/test|fetch` were mounted without `authenticate`.
- **Fix:** backend `feat/066-inbox-backend-p1` (auth on `/api/gmail`, `search` filter, settings whitelist, demo-task fallback opt-in only); frontend `feat/066-inbox-zero-p1-3` (only tabs with a backend remain; every mutation checks `res.ok` + `success` and toasts; the `'inbox-zero'` registry alias retired via `resolveWidgetId` at read time — `getWidgetMeta`, `openWindow`, session restore, named layouts, the open-widget bus — never by rewriting saved layouts).
- **Prevention:** before a UI control ships, grep the backend for its exact route (method + path) — a 404 is not a "loading" state. When a module gains an export, grep `vi.mock('…/that-module'` in `src/test/`: four hand-written registry mocks lacked `resolveWidgetId` and failed 20 tests that the agents' targeted runs never executed — run the FULL gate before trusting a green subset. Fix id aliasing in the shared function (`getWidgetMeta`, `openWindow`), not per call site: the per-site pass missed ⌘K Resume, the recent-activity recorder and ARA's resume chip.

## 2026-09-24 — Inbox Zero phase 4: durable, owner-scoped inbox (plan 066 §4)

- **Error (before):** inbox items and the Gmail dedupe set lived only in memory (lost on every restart); archive/delete used the default mailbox for mail from other linked accounts; every staff role saw every connected mailbox; the list shipped full email bodies; the poll interval needed a restart.
- **Fix:** backend `feat/066-inbox-backend-p4` (stacked on phase 1–3): `inbox_items` table with write-through upserts; owner-only visibility (G2) enforced by a required `InboxViewer` argument on every store call; per-(consumer, owner, mailbox) Gmail dedupe + history cursor; per-account Gmail actions; `rate_limited` state with backoff; body only via `GET /api/inbox/:id/body`. Frontend `feat/066-inbox-zero-p4`: the card fetches the body on expand; ⌘K scores on the snippet.
- **Traps found in review (all pinned by mutation-checked tests):** a history cursor that always advances silently loses mail (truncated bursts, transient fetch failures, and a list/getProfile race) — advance it only after a clean, complete cycle, and filter processed ids BEFORE truncating; durable dedupe turns a classification failure into permanent loss — store the mail unclassified instead; dedupe keyed by mailbox alone starves the second user who links a shared mailbox — key by owner too; one poison message can pin the cursor forever — give up after 3 tries.
- **Prevention:** any "consumed" marker that is now durable needs a story for partial failure. Make visibility a required argument so the compiler lists every caller (it caught 14 call sites; grep caught the rest: ARA context, `/api/status`, tasks/automation fetchers).

## 2026-09-24 — Backend CI: `spawn ENOMEM` in terminal/deploy-script tests (Dwellium#10)

- **Error:** "Typecheck + Jest" failed on the phase-4 backend PR: `terminalRoutes`, `terminalAccessExposure`, `deployScriptFlags` died at `child_process.spawn` with `ENOMEM` (the push run of the same SHA sometimes passed).
- **Root cause:** jest ran `--runInBand`; memory grew across the 87 suites (~1 GB of heap, plus native/wasm memory outside the heap), and Linux's heuristic overcommit refused the late suites' forks. `/api/terminal/capabilities` made it worse by spawning 12 `which` processes at once. Reproduced in `node:22` on Linux (colima; mount the copy under `$HOME` — colima does not share `/private/tmp`).
- **Fix (backend `feat/066-inbox-backend-p4`):** `npm test` = `jest --maxWorkers=1` with `workerIdleMemoryLimit: '50MB'` (worker recycled after nearly every file; 512MB still failed); CI workflows stop passing `--runInBand`; the terminal tool probe is an in-process PATH lookup (also removes a hard-coded developer home / nvm path).
- **Prevention:** never re-add `--runInBand` to the backend CI. When a Linux-only failure appears, reproduce it in Docker before pushing another guess — two CI round trips were spent on partial fixes before the local repro settled it.

## 2026-09-24 — Inbox Zero phase 5: revived undo, audit, rules, unsubscribe, snooze, AI draft (plan 066 §5)

- **Error (before):** phase 2 had removed Undo, Snooze, Unsubscribe, the Rules and Audit tabs and the AI draft because none had a backend; routing rules lived in `./data/routing-rules.json`, outside the plan-061 snapshot, so any edit would have been lost on the next deploy.
- **Fix:** backend `feat/066-inbox-backend-p5` — `PUT /api/inbox/:id/status` (re-adds the Gmail `INBOX` label FIRST; on failure 502 and the status is unchanged), `POST /:id/snooze` (resurfaced on every read path), `GET /audit/global` (owner-scoped SQL join), `GET/POST/PUT/DELETE /rules` (writes god-only; one `inbox_settings` row), `PATCH /newsletters/:sender/unsubscribe` (RFC 2369/8058), `POST /:id/draft` (never sends). Frontend `feat/066-inbox-zero-p5` — Rules + Audit tabs, an Undo bar after a successful archive/delete, a Snooze list, Draft reply on every expanded card, Unsubscribe with an honest result.
- **Traps found in review (all pinned by mutation-checked tests):** a runtime-editable regex matched against inbound mail is a whole-backend DoS — one nested quantifier took 52 s on a 35-char subject; V8 has no regex timeout, so matches run in a `vm` script with a 50 ms `timeout` and a rule that blows it is skipped until edited. The one-click unsubscribe POST goes to an attacker-chosen URL: pin DNS in the `lookup` hook against a `net.BlockList` (literal IPs skip `lookup`, so check `URL.hostname` too — with IPv6 brackets stripped); a destroyed over-cap response never emits `end`, so settle on `close` or the route hangs. `?offset=1e20` made better-sqlite3 throw (500) — clamp with `Number.isSafeInteger`. SQLite `datetime('now')` has no zone; `new Date()` reads it as local time. Editing a rule must be a partial PUT or it silently re-enables a disabled rule. A clipboard failure must not toast "copied".
- **Prevention:** any user-editable pattern that runs on external input needs a time bound, not just a compile check. Any server-side fetch of a URL taken from mail/web content goes through a DNS-pinned public-address check. Check every "success" message against the actual outcome (2xx AND `success:true`, clipboard resolved).

## 2026-09-24 — Inbox Zero phase 6: route contract, a11y to 0, Settings extraction (plan 066 §6)

- **Error (before):** nothing stopped the widget from calling routes that did not exist (the cause of the eight dead tabs); 18 jsx-a11y errors in `components/InboxZero` (cards not keyboard-reachable, the email viewer ignored Escape, unlabeled controls); an 870-line Settings block inside `InboxZero.tsx`; a viewer link to `/api/inbox/:id/attachments/:id`, a route that never existed.
- **Fix:** backend `feat/066-inbox-backend-p6` — `tests/inboxContract.test.ts`: every (method, path) the frontend calls, hit through the real app with real fixtures and all Gmail/OpenAI effects mocked; a row fails only on the app's unmatched-route 404 body. Frontend `feat/066-inbox-zero-p6` — `SettingsTab.tsx` (same text/DOM), a real expand `<button>` per card, dialogs with Escape + backdrop close, labels, tokens; dead attachments UI removed. axe (harness, dark theme): Triage 13 → 0, Settings 2 → 0 violating nodes.
- **Traps:** extracting a tab into a component that only mounts while active silently drops its unsaved edits on a tab switch — keep it mounted after the first visit and hide it (`hidden`), or lift the state. `var(--accent)` used AS text fails 4.5:1 at 10–11 px in the dark theme — use `var(--accent-text)`; status colors as text: `color-mix(in srgb, var(--danger) 50–70%, var(--text-primary))` passes in light and dark. A harness `?theme=` param is overwritten by `ThemeProvider` — seed `localStorage['dwellium-theme']` before load and assert the background is light, or a "light theme" check runs on dark. A parity check that compares two "Loading…" screens proves nothing — mock the tab's data endpoint first.
- **Prevention:** add a contract row whenever the widget calls a new endpoint. Open question for plan 064: the latte theme's `--text-secondary` (#7c6aad) is 4.31:1 on its surfaces app-wide, so every widget fails axe contrast in light mode until the token changes. `StellaAgent.tsx`'s Honcho learn daemon called `/api/v1/inbox` (404, so it never captured mail); fixed to `/api/inbox?limit=3` on Ilya's go (2026-09-24) — it now sends the caller's own three newest subjects/senders to `/api/honcho/memories/extract` while the opt-in learn daemon is on. Before any "fix the URL" on a dead call, ask what the call starts doing once it works.

## 2026-09-24 — Inbox Zero phase 7: per-user upstream stats proxy; audit_log stored camelCase secrets (plan 066 §7)

- **Finding (read-only check):** upstream Inbox Zero has no MCP server (its `api/mcp/*` is an MCP *client*); its public API is 7 endpoints (rules CRUD + `stats/by-period` + `stats/response-time`, header `API-Key`, one key per upstream mailbox), reading the mailbox upstream itself linked. The local container 500s every API route ("Invalid environment variables"), and `localhost:3000` on the Mac is the Dwellium backend, not the container.
- **Fix:** backend `feat/066-inbox-backend-p7` — `/api/inbox/upstream/{status,key,stats/*}`: each user's own key, encrypted at rest (refused with 503 when domain encryption is off), never returned/logged; base URL only from `INBOX_ZERO_API_URL`; params whitelisted; upstream 401/403 → 502 + `keyRejected` (a Dwellium 401/403 signs the user out). Frontend `feat/066-inbox-zero-p7` — `UpstreamStats.tsx` in the Stats tab.
- **Security bug found in review (pre-existing):** `auditMiddleware` stripped only exact field names (`api_key`, `token`, …) from logged bodies, so `apiKey`, `openaiApiKey`, `trelloApiKey`, `trelloToken` went into `audit_log.details` in plaintext — including every Inbox Zero Settings save that typed a key. Now: credential-named fields dropped by pattern at any depth (bodies + query), and credential-shaped values (`Bearer …`, `sk-…`, JWTs, `user:pass@` URLs) masked. **Existing audit_log rows were not touched** (a DB write needs Ilya's go); rotate any key typed into Settings before this fix.
- **Traps:** `encryptForDomain` returns the PLAINTEXT when no domain key is loaded — check `isDomainEncryptionEnabled` before storing a secret. A fetch timeout also aborts the body stream — catch around the body read or Express 4 never answers. A component whose unit tests mock an unwrapped response agrees with its own bug: UpstreamStats read `configured` off the `{ success, data }` envelope and would always have said "not connected"; the harness (real backend shapes) caught it. Fixtures must use the backend's real envelope.
- **Prevention:** redact audit bodies by pattern + value shape, never by an exact-name list. Any new stored credential: encrypted-or-refuse, never logged, never echoed, never a 401/403 back to the app.

## 2026-09-25 — Plan 067: One Save lost unsaved writes on reload; ThoughtWeaver shared/undeletable captures

- **Error (1):** a capture (or any `withSync` edit) made less than 800 ms before a reload — or after a failed flush — vanished once the backend was reachable again.
- **Root cause:** `set()` persists locally at once but the durable write-through is debounced and its retry closure lives only in memory; bootstrap's `hydrate()` then replaced local with the stale remote (no `merge`).
- **Fix:** `oneSaveStore.ts` persists `onesave:dirty:<objectId>` on write, clears it on a confirmed save; `hydrate()` keeps local and re-arms the write-through while it is set (stores with `merge` still merge). `withSyncStatic` stores share one slot across accounts, so `onesave:slot:<objectType>` records whose data is in the slot; a marker only wins while its owner still holds the slot (review found A's marker pushing B's theme into A's remote). Tests: `oneSaveDirtyMarker.test.ts` (a–f, mutation-checked).
- **Error (2):** deleted ThoughtWeaver captures came back undeletable; every user saw every other user's captures; the phone page carried the Supabase anon key in its URL.
- **Root cause:** Supabase rows were re-pulled on every load as non-local rows and deletes never reached Supabase; the backend `/api/thought-weaver` store was module-level Maps with no user scoping; phone setup was `/capture?url=&key=&user=`.
- **Fix:** Supabase rows and the new per-user phone inbox (`thought-weaver-inbox_<uid>`, written only by `POST /api/thought-weaver/inbox`) are imported ONCE by id (`planImport` + One-Save-synced `twImportedStore`, union merge). Backend `/capture` is a stateless classifier; the shared routes are gone. Phone page uses the Dwellium session. Backend commit `0dc4646` (branch `feat/067-thoughtweaver-p2`).
- **Prevention:** never let a remote snapshot replace a local write that has not been confirmed durable; any "seen ids" set must union on merge; per-user data belongs in owner-checked One Save objects, never in module-level server state; never put keys in URLs. Known follow-ups: whole-array last-writer-wins on the captures store across devices (needs per-item `updatedAt` + synced deleted ids), phone inbox only imported when the widget mounts, object event log stores full payloads (O(n²) bytes for the inbox), generic `PUT /api/objects` id squatting (separate task).

## 2026-09-25 — Plan 067 phases 3–5: per-account ThoughtWeaver, unstyled widget, substring classifier

- **Error:** ThoughtWeaver holders were set only when the widget rendered (other readers could see the previous account's thoughts); signed-out capture wrote a shared `_anonymous` bucket; any account could create another's `thought-weaver_<uid>` object; 42 rendered classes had no CSS and the root hardcoded a dark background (latte unreadable); the offline/backend classifiers matched substrings ("residue" → due, "metal" → met).
- **Fix:** holders in `perUserIdentity.ts` ALL_HOLDERS; widget requires a signed-in user; backend `objectRoutes.ts` refuses ThoughtWeaver-family ids whose `_<uid>` suffix is not the caller (case-insensitive, when AUTH_ENABLED); `morningBriefService.readUserObjects` checks ownerId; CSS rewritten on theme tokens with a container query; classifiers use Unicode word-boundary regexes compiled once.
- **Prevention:** per-user data needs its holder in ALL_HOLDERS and an owner-bound object id; verify light themes with a real render + computed-contrast check (axe skips text over gradients; Chromium serializes `color-mix()` as `color(srgb 0–1)` — scale it before computing ratios); compare classifier output old-vs-new on realistic inputs before shipping a matcher change. Local dev with AUTH_ENABLED off serves every request as the dev god user, so server-side isolation cannot be observed locally (read-only audit found 6 TW objects owned by that user under other accounts' ids — left untouched).

## 2026-09-25 — Inbox Zero: opened email was dark-blue on near-black; viewer fixed-size

- **Error (Ilya):** opening a message in Inbox Zero showed dark-blue text that was hard to read; the viewer could not be enlarged. Measured: `#1e293b` on the default theme's `#0f0f1e` pane = **1.3:1** (both the full viewer and the inline preview); the email body iframe was stuck at 200 px inside a 780 px dialog.
- **Root cause:** both `<iframe srcDoc>` stylesheets in `InboxZero.tsx` hard-coded light-mode colors (`#1e293b` body, `#0f172a` headings, `#f8fafc` table/quote backgrounds, `#2563eb` links) and used `var(--bg-surface)` / `var(--border-default)` / `var(--text-*)` — **CSS custom properties do not cross into an srcdoc iframe**, so those declarations were invalid (transparent background, no borders) and the dark theme showed through behind light-mode text. The iframe had no height, so `minHeight: 200` was all it ever got.
- **Fix (branch `fix/inbox-viewer-contrast-resize`):** new `InboxZero/emailFrame.ts` builds both documents from a literal palette — the full viewer (always-dark chrome) uses `VIEWER_PALETTE` (white `#ffffff` on `#141726`, 17.8:1); the inline preview uses `themePalette()`, which resolves the live theme tokens off `<html>` (validated so nothing can break out of the `<style>` block) → 19:1 in cosmos, 13.2:1 in latte. The viewer gained an expand toggle (`aria-pressed`) and a bottom-right drag handle (pointer capture, 2× delta because the dialog is flex-centered, clamped to 420×360 … viewport−24 px, arrow keys ±20 / Shift ±80), sizes remembered in `localStorage` (`dwellium-iz-viewer-size`, `dwellium-iz-viewer-expanded`, try/catch). The email body now flexes to fill the dialog.
- **Two traps fixed in review:** a drag released over the backdrop fires a click on it → `suppressBackdropClickRef` swallows that one click; every close path (Escape, backdrop, ✕, approve/archive/delete) unmounts the handle mid-drag with no pointerup → one effect resets the drag state when the viewer closes, otherwise the next viewer opened with `pointer-events:none` on the email body.
- **Checks:** `npx vitest run src/test/emailFrame.test.ts src/test/InboxZero.test.tsx` 41/41 (the close-mid-drag and white-text tests fail when their fix is reverted); full gate 3,278/3,278 + both builds + PII + SSR smoke; standalone harness 30/30 (before 1.3:1 → after 17.8:1, drag keeps the grip under the pointer, release over backdrop keeps the viewer open, size restored after reload).
- **Addendum — third viewer missed (fixed on `fix/inbox-audit-viewer-contrast`):** the Audit tab's "View" preview (`GlobalAuditTab.tsx`) carried its own copy of the old stylesheet (`#1e293b` on a hard-coded `#fff` slab inside the dark dialog). It now uses `buildEmailSrcDoc(…, themePalette(), 'comfortable')` → 19:1 cosmos, 13.2:1 latte. Found only by crawling production for the old CSS string after #149 shipped — the first pass grepped `InboxZero.tsx`, not every `srcDoc` in the folder.
- **Prevention:** never put `var(--…)` inside an `srcDoc` Before fixing an iframe style, `git grep -n srcDoc` the whole widget folder — every copy is a caller. — resolve tokens to literals first (`themePalette()`), and never hard-code a text color without its background; any drag that can end over a click-to-close backdrop must swallow the trailing click; state set on pointerdown must also be reset on unmount/close, not only on pointerup.

- **Prevention:** never put `var(--…)` inside an `srcDoc` — resolve tokens to literals first (`themePalette()`), and never hard-code a text color without its background; any drag that can end over a click-to-close backdrop must swallow the trailing click; state set on pointerdown must also be reset on unmount/close, not only on pointerup.

## 2026-09-25 — Plan 069: Cognitive Harness (per-render retrieval, latte log contrast, accumulating canvas grid)

- **Error:** the widget called `cmn.probe()` in its render body — a full retrieval + PageRank on every render (6 s auto-cycle included); the canvas reseeded every tab change, ignored DPR and host resizes, and kept animating while hidden in a tab group; after theming, the event log failed latte contrast (3.19–4.34:1) and the canvas grid became a heavy lattice.
- **Root cause:** (1) no memo on the engine version. (2) the canvas effect depended on `activeIndex`, sized from `offsetWidth` once, listened only to `window` resize. (3) `.ch-logs-container` used `color-mix(black 20%, transparent)` — a fixed black darkens the light surface under accent text; the coder's contrast table used the plain surface, not the composited one. (4) the grid is redrawn every frame under a 0.15-alpha trailing fade, so it accumulates to ≈ alpha / 0.15 (8 % → ~50 %).
- **Fix:** `useMemo(() => cmn.probe(), [cmn, version])`; `harnessCanvas.ts` (seed once, ResizeObserver × DPR, IntersectionObserver + visibilitychange gate, offscreen grid, CSS-token palette with a parse check); theme-relative tint `color-mix(var(--text-primary) 5%, transparent)`; grid 1.5 %. Also `hydrate()` now emits on its post-await failure path.
- **Prevention:** expensive probes belong behind the store version; tints use `--text-primary`, never literal black/white; judge contrast with axe on a real render in BOTH themes (my DOM walker read 4.61 where axe measured 4.34 on the actual pixels); for trail-fade canvases, size per-frame alphas by alpha / fadeAlpha.

## 2026-09-26 — Wiki deep-link test flaked under the full suite

- **Error:** `Wiki.test.tsx` "selects the page named by dwellium:wiki-open-page" failed ~1 in 8 under parallel load (passed alone).
- **Root cause:** not a lost event (the listener attaches on the first commit, before the awaited button appears): `window.dispatchEvent` runs outside React's event system, so the resulting state update was only scheduled, and under CPU load the flush outlasted `waitFor`'s 1 s default.
- **Fix:** dispatch inside `await act(async () => …)` and assert directly. 20/20 runs green under load; breaking the listener still fails the test.
- **Prevention:** wrap non-React `dispatchEvent` calls that should update a component in `act`, rather than widening timeouts.
