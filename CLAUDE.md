# CLAUDE.md — Repo-level Agent Notes

**Repo.** `NovaTrustSolutions/dwellium-per-spec` (private)
**Default branch.** `main`
**Subtree.** `qualia-shell/` imported 2026-04-22 from upstream `qualia-shell` via `git subtree add`.

> Per-task / per-phase detail lives in `Docs/Phase<N>_Task_<X>_Completion_Report.md` and `Docs/Phase<N>_Closure_Report.md`. Read those for narrative; this file is a pointer index.

---

## Current State (as of 2026-05-19)

- **HEAD:** `TBD-8.11` (Phase-8+ Task 8.11 closing at this HEAD pending PR #80 merge; sweep-resolution to real SHA expected at Task 8.12 OPENING per v2.74.1 branch-base discipline + established Task N TBD → squash-SHA at Task N+1 pattern). Phase-8+ Task 8.11 CLOSED — Block B 6-of-6 closer: `ssr: false → true` flip at `qualia-shell/react-router.config.ts` + NEW `Scripts/smoke_test_ssr_phase8.mjs` 5-phase smoke-test bundle (playwright-chromium probe; HARD-blocking assertions) + `@react-router/serve@7.15.1` install as production dep per Q5 LOCK + CI workflow extension at `.github/workflows/appfolio-parity-gate.yml` (NEW smoke-test step BLOCKING) + v2.73.1 in-place patch at `playwright.baseline.config.ts::webServer` (`vite preview` → `react-router-serve` per ssr:true mode). Cowork Q1 LOCK **Option D HYBRID** co-shipping: **🎯 PROVIDER-SSR-REMEDIATION 2pt → 3pt CROSS-TASK-SHAPE-ROBUSTNESS extension** (sub-shape (c) `useSyncExternalStore-migration-validation-under-true-SSR-runtime`) + **🎯 FRAMEWORK-INSTALLATION 2pt → 3pt CROSS-TASK-SHAPE-ROBUSTNESS extension** (sub-shape (c) `ssr-runtime-enablement-on-framework-mode-foundation`). **🎯 4-of-4 Phase-8+-introduced classes EXTENDED PAST FULL CALIBRATION milestone** (SCOPING-ONLY 4pt + SSR-MIGRATION-PREP 2pt + FRAMEWORK-INSTALLATION 3pt + PROVIDER-SSR-REMEDIATION 3pt; 2-of-4 at 3pt cross-task-shape-robustness; distribution-of-calibration-depths IS a substantive engineering signal). **Block B 6-of-6 COMPLETE ✓** (8.6-8.11); Block B → Block C transition gate GREEN-LIGHT; Block C 8.12-8.15 R. **🎯 Smoke-test EMPIRICAL VALIDATION:** 14 cumulative createLocalStorageStore-factory-produced stores work under true SSR runtime — pre-hydration HTML 5,949 bytes / HTTP 200 / 0 console errors / 0 warnings / 0 page errors. **🎯 Finding EE Q2 LOCK Option α EMPIRICALLY CONFIRMED** — AuthGate hydration-flash IS NOT a ssr:true regression. Cumulative state at HEAD-post-8.11: 33 findings (+1 Finding II INFORMATIONAL deferred-to-Phase-9+ → effective 34) + 16-pattern anchor-bias-mitigation cluster (NEW v2.74.1 branch-base discipline cemented at OPENING) + 12 v2.60.1 cluster altitudes + 8 v2.64.0 cross-altitude application altitudes (NEW 8th CONFIRMATION at AuthGate altitude per Q3(b) LOCK) + 19 project-wide classes + vitest 278 (+0 per Q4 LOCK; smoke-test is Node.js script not vitest) + 14 cumulative factory-produced stores + 9th cross-phase production-source-edit chunk-axis BREAK. Full Task 8.11 narrative at `Docs/Phase8_Task_8_11_Completion_Report.md`; earlier closures at `Docs/CLAUDE_history.md`.

### Phase summary

| Phase | Closed | HEAD at close | PRs | Vitest delta | Notes |
|------:|:------:|:--------------|----:|-------------:|:------|
| 0.0   | 2026-04-22 | — | — | — | Exit gate: `Docs/Baselines/phase_0_0_exit_gate_report.md` |
| 1     | 2026-04-23 | `094b91e` | — | — | `Docs/Phase1_Completion_Report.md` |
| 2     | 2026-04-25 | `1a7a39b` | 10 | +87 | Per-task reports `Docs/Phase2_Task_2_X_Completion_Report.md` |
| 3     | 2026-04-28 | `0cfb8a8` | 9  | +32 | Closure: `Docs/Phase3_Closure_Report.md` |
| 4     | 2026-04-30 | `3a41cdf` | 7  | +0  | Closure: `Docs/Phase4_Closure_Report.md` (first phase with byte-identical chunk across all tasks) |
| 5     | 2026-05-04 | `2acaa82` | 10 | +35 | Closure: `Docs/Phase5_Closure_Report.md` |
| 6     | 2026-05-11 | `b99a8ac` | 11 | +0 | Closure: `Docs/Phase6_Closure_Report.md` (11-task production-readiness arc; Phase-6 100% complete; SECOND PHASE with ZERO new tests mirroring Phase-4 +0; v1 L230 ZERO WCAG AA threshold MET on 4-page scope; 2 NEW classes COMPONENT-FIX 10th + CLOSURE-NARRATIVE-CONSOLIDATION 11th; 16-item Phase-7 carry-forward at §8; 12 consecutive cross-phase sweep-resolutions). Full per-task narrative archived to `Docs/CLAUDE_history.md`. |
| 7     | 2026-05-16 | `cfa9d0f` (closer #68; per-task SHAs at history) | 14 of 14 ✓ + closer (Phase-7 100% complete; THIRD PHASE with FULL CLOSURE +0 vitest delta mirroring Phase-4+6) | +0 | Closure: `Docs/Phase7_Closure_Report.md` (14-task arc — Block A a11y/CI 7.1-7.8 + Block B perf multi-lever 7.9-7.11 + Block C test infra 7.12-7.14 + closer). 2-finding engineering catalog: (A) lazy-load IS structurally-correct lever family at React 19 + Vite 6 + 4,500 ms-LCP baseline (cemented 6.7+7.10+7.11+7.14); (B) `vi.useFakeTimers` + React 19 scheduler primitives structurally fragile when async-cascade-to-render present (cemented 7.13 — see Conventions). 4 NEW classes (CI-CONFIG-ONLY 12th + BASELINE-ARTIFACT 13th + PERF-LEVER-LAZY-LOAD 14th + TEST-INFRA-FIX 15th). 6 GR-15 PERMANENT process change candidates v2.60.1-v2.60.6 + 3-pattern anchor-bias-mitigation finding cluster (v2.60.1+v2.60.4+v2.60.6). LCP CV 2.29% at HEAD-post-7.10 first non-degenerate noise-floor metric (~6× variance reduction). Full per-task narrative + close-cycle detail archived to `Docs/CLAUDE_history.md`. |
| 8+    | R OPEN 2026-05-19 | `5057dca` (8.1) → `b43c2bf` (8.2) → `c44198f` (8.3) → `6742484` (8.4) → `d98bd48` (8.5) → `8e04061` (8.6) → `79f0ced` (8.7) → `387abfa` (8.8) → `a0975f7` (8.9) → `784fa6d` (8.10) → `TBD` (8.11) | 11 of 15 ✓ (8.1 OPENER + 8.2-8.11; PRs #69-#76 + #78 + #79 + #80 pending) | +19 | 15-task arc; progress 10 of 15 ✓ at 2026-05-19; +19 vitest cumulative (+5 at 8.2; +0 at 8.3-8.8; +5 at 8.9; +9 at 8.10 server-snapshot tests across 9 NEW factory-produced stores); **4 NEW Phase-8+ classes introduced** (SCOPING-ONLY 16th + SSR-MIGRATION-PREP 17th + FRAMEWORK-INSTALLATION 18th + PROVIDER-SSR-REMEDIATION 19th); **🎯 4-of-4 FULLY CALIBRATED at 2pt+ milestone at 8.10 close** (PROVIDER-SSR-REMEDIATION 1pt → 2pt extension per Q1 LOCK Option A); **33 cumulative engineering findings** (A-Y + Z + AA + CC + DD + EE + FF + GG + HH); 15-pattern anchor-bias-mitigation cluster (NEW v2.72.0 leaf-component-altitude + v2.73.0 Step-7-entry-whole-file-read + v2.74.0 dynamic-key-classification) + 12 v2.60.1 cluster altitudes + 7 v2.64.0 cross-altitude application altitudes; 4 in-place v2.X.X patches across Block B opener trio (v2.66.1+v2.66.2+v2.66.3 at 8.6 + v2.68.1 at 8.8); **🎯 6-consecutive PRE0/Step-7-entry wrong-hypothesis refutations cross-altitude** (Findings L+O+W+Y+Z at PRE0 + GG at Step-7-entry); createLocalStorageStore factory extended at 8.10 with `{ key, deserializer, defaultValue }` object signature supporting dynamic-key resolver per Q2 LOCK Option β (sister-shape to existing positional signature preserved byte-for-byte); 14 cumulative factory-produced stores (5 at 8.9 + 9 at 8.10); v2.72.1 .reset() PERMANENT standing convention cemented per Q3 LOCK. Per-task closure reports: `Docs/Phase8_Task_8_1_Completion_Report.md` through `Docs/Phase8_Task_8_10_Completion_Report.md`. Block A 4-of-4 COMPLETE (8.2-8.5); Block B **5-of-6 milestone** (8.6-8.10 ✓; 8.11 R); Block C 0-of-4 (8.12-8.15 R). Earlier Task 8.1-8.9 row narrative + verbose Block B opener detail archived to `Docs/CLAUDE_history.md`. |


### Next task

**🎯 Phase-8+ Task 8.11 CLOSED — Block B 6-of-6 closer; SSR architectural migration COMPLETE.** Full narrative at `Docs/Phase8_Task_8_11_Completion_Report.md`. Cowork Q1 LOCK Option D HYBRID co-shipping: PROVIDER-SSR-REMEDIATION 2pt → 3pt CROSS-TASK-SHAPE-ROBUSTNESS extension + FRAMEWORK-INSTALLATION 2pt → 3pt CROSS-TASK-SHAPE-ROBUSTNESS extension. Smoke-test EMPIRICALLY VALIDATES 14 cumulative factory-produced stores under true SSR runtime (0 console errors / 0 warnings / 0 page errors at chromium-headless probe; pre-hydration HTML 5,949 bytes / HTTP 200). Finding EE Q2 LOCK Option α EMPIRICALLY CONFIRMED — AuthGate hydration-flash IS NOT a ssr:true regression. v2.73.1 in-place patch at `playwright.baseline.config.ts::webServer` (sister-shape to v2.66.2 server-startup altitude patch). Phase-8+ progress **11 of 15 ✓**; **🎯 Block B 6-of-6 COMPLETE** + Block B → Block C transition gate GREEN-LIGHT; 4 R remaining (Block C 8.12-8.15). **Recommended next:** Task 8.12 — LCP n=10 re-measurement (mirrors Phase-7 7.11 protocol byte-for-byte; `LH_TASK_FIELD=phase8_task_8_12 LH_ROOT_RUNS=10 node Scripts/run_lighthouse_phase8.mjs` via script-rename `git mv` per Phase-6 6.6 + 6.7 + Phase-7 7.11 MEASUREMENT-ONLY sub-shape `plus-script-rename` precedent); empirical SSR-migration delta vs Phase-7 7.11 noise-floor baseline LCP CV 2.29% / median 3,903 ms. Block C opener — Phase-8+ → Phase-9+ transition signal preview.

Earlier task-close 'Next task' recommendations: see `Docs/CLAUDE_history.md`.

### Calibration classes (in-repo, project-wide)

**19 distinct classes** at Phase-8+ Task 8.10 close (cumulative: 1 PRE-PHASE-6 + Phase-6 +2 [COMPONENT-FIX 10th + CLOSURE-NARRATIVE-CONSOLIDATION 11th] + Phase-7 +4 [CI-CONFIG-ONLY 12th + BASELINE-ARTIFACT 13th + PERF-LEVER-LAZY-LOAD 14th + TEST-INFRA-FIX 15th] + Phase-8+ +4 [SCOPING-ONLY 16th + SSR-MIGRATION-PREP 17th + FRAMEWORK-INSTALLATION 18th + PROVIDER-SSR-REMEDIATION 19th]). **🎯 4-of-4 Phase-8+ classes EXTENDED PAST FULL CALIBRATION at Task 8.11 close** (2-of-4 at 3pt CROSS-TASK-SHAPE-ROBUSTNESS; distribution-of-calibration-depths IS a substantive engineering signal): SCOPING-ONLY 4pt at 8.1+8.3+8.5+8.8; SSR-MIGRATION-PREP 2pt at 8.2+8.4; FRAMEWORK-INSTALLATION 3pt at 8.6+8.7+8.11 (sub-shapes [a] framework-mode-adoption + [b] entry-boundary-customization + [c] ssr-runtime-enablement-on-framework-mode-foundation); PROVIDER-SSR-REMEDIATION 3pt at 8.9+8.10+8.11 (sub-shapes [a] providers + [b] providers-AND-leaf-components + [c] useSyncExternalStore-migration-validation-under-true-SSR-runtime). Catalog: **34 findings** (A-Y + Z + AA + CC + DD + EE + FF + GG + HH + II INFORMATIONAL deferred-to-Phase-9+-widget-SSR-audit per Q6 LOCK). Class definitions live in the Conventions block below; per-task calibration evolution + Task 8.9 + earlier close detail archived to `Docs/CLAUDE_history.md`.

### Surviving deferred-items ledger

Cross-phase cumulative deferred-item carry-forward state at HEAD-post-8.8: Phase-6 → Phase-7 16-item carry-forward consolidated at `Docs/Phase6_Closure_Report.md §8` (3 blocks: a11y+CI / perf multi-lever / test infrastructure + 2 process improvements); all resolved within Phase-7. Phase-7 → Phase-8+ 13-item carry-forward consolidated at `Docs/Phase7_Closure_Report.md §8`; Phase-8+ Block A 4-of-4 ✓ + Block B 3-of-6 ✓ (8.6-8.8) absorbed most items. Phase-8+ Tasks 8.5-8.8 §7 add NEW deferred-items per close. Full per-phase deferred-item enumeration + Phase-6/7 carry-forward inventory archived to `Docs/CLAUDE_history.md`.

---

## CI Behavior

- `AppFolio Parity Gate` (`.github/workflows/appfolio-parity-gate.yml`) runs on push to `main` + PRs touching parity paths. Blocking gates: `tsc -b`, `vitest`, both `vite build` modes (`VITE_APPFOLIO_SEEDS={true,false}`), `verify_no_pii_leak.mjs` strict-scope.
- **Playwright baseline E2E was split at Phase-7 Task 7.3** into 2 distinct steps: (a) **Playwright axe-baseline E2E (a11y assertion-blocking)** with `continue-on-error: false` BLOCKING (gated on 7.2's hard assertion `expect(axeResults.violations.length).toBe(0)`); (b) **Playwright screenshot-baseline E2E (visual regression; sheltered pending Linux baselines)** with `continue-on-error: true` sheltered (pending Linux baseline mechanism at Tasks 7.4-7.6; Task 0.0.9 captured darwin-only). Do not flip the screenshot-baseline step back to blocking without committing the Linux snapshots first (Tasks 7.4 + 7.5 unlock that flip at Task 7.6; **Task 7.4 LAID the INFRASTRUCTURE at NEW `.github/workflows/capture-linux-baselines.yml` workflow_dispatch-only job — dispatch via `gh workflow run "Capture Linux Playwright Baselines (workflow_dispatch only)" --ref main -f reason="<reason>" -f dry_run=<true|false>`**).
- `PII Scan` (`.github/workflows/pii-scan.yml`) runs on every push and PR.
- **Paths-filter quirk.** Sweeps + tasks touching only `Scripts/**`, `Docs/**`, `Docs/Baselines/**`, `qualia-shell/e2e/**`, `qualia-shell/playwright.baseline.config.ts`, `playwright.config.ts`, `.env.example`, root `.gitignore`, or root `CLAUDE.md` fall outside parity-gate paths filter — verification needs `gh workflow run`. Auto-fire triggers via `qualia-shell/src/**`, `qualia-shell/app/**` (added v2.68.1 post-Task-8.6), or `.github/workflows/**` self-reference. **🎯 axe-baseline.spec.ts re-attribution (6.8 PRE0 finding):** `qualia-shell/e2e/axe-baseline.spec.ts` is informational-by-spec-design (no `expect()` on violation count), NOT just by workflow `continue-on-error: true`. Making axe-baseline blocking requires SPEC EDIT + workflow step-split (executed at Phase-7 Block A; see history).
- **Push-trigger flake.** Push-triggered runs have not fired reliably; prefer manual dispatch when no auto-run appears within ~90s. **Empirical observation at Task 8.10 merge cycle (Q-Δ3 LOCK):** 1st PR push from fresh feature branch may not fire auto-CI within ~90s window (sister-shape to push-trigger flake observations across Phase-6/7); force-push from rebased branch reliably triggers it (Task 8.10 PR #79 force-push from `73c79c8` fired Parity Gate run [26083267532](https://github.com/NovaTrustSolutions/dwellium-per-spec/actions/runs/26083267532) within ~30s — empirically confirms paths-filter effectiveness at the 2nd-PR-push event altitude).

---

## Deferred Items (not blocking)

1. **Linux Playwright baselines.** Capture 8 `*-chromium-linux.png` baselines (Linux dev box or CI `--update-snapshots`). See `Docs/Baselines/phase_0_0_exit_gate_report.md` "Deferred Item" section. Until done, the Playwright CI step is informational.
2. **`qualia-shell/public/assets/nebula-bg.mp4`** — 70.96 MB asset in git, over GitHub's 100 MB soft limit. Future push of this file needs Git LFS, a CDN, or a smaller replacement. **Do not** `git lfs migrate` on `main` without explicit instruction (history rewrite is out of scope).

---

## Conventions (repo-specific)

- **Phase-plan locality (PRE-FLIGHT discipline, Plan v2.29).** When scoping a task in a phase, check `Docs/Phases/Phase_<N>_Plan.md` alongside the parent plan — the phase-spec may refine per-task scope.
- **Phase-spec vs parent (GR-14, Plan v2.32 amendment).** When phase-spec *contradicts* (not refines) parent, **parent + v1-lineage wins**.
- **Source-provenance verification (Step Zero of DC-pre-flight).** Verify the kickoff-named source actually contains what the spec assumes before committing to a path. Phase-4 surfaced 4 SCOPE-COLLISION findings (4.3 / 4.5 / 4.6 / 4.7) that motivated this as a permanent step.
- **Subtree discipline.** `qualia-shell/` is a subtree; changes ideally flow back to the upstream `qualia-shell` repo via `git subtree push`. Local-only changes land directly on `main`.
- **PII.** `Scripts/verify_no_pii_leak.mjs` guards strict (`appfolioDerived/`) and legacy (`qualia-shell/public/data/`) scopes. Both strict-clean as of 2026-04-19. Do not re-introduce PII. PII guard regex `/\b(?:\d[ -]*?){13,19}\b/` at `complianceEngine.test.ts:228` will false-positive on raw UUID 16-digit hex prefixes — use slug-namespace IDs in fixtures.
- **Feature flag.** `VITE_APPFOLIO_SEEDS` gates the AppFolio-derived seed layer. Both `true` and `false` builds must succeed (CI verifies both).
- **API version header.** `strataApi.backend.ts` emits `X-Qualia-API: v2` unconditionally on `request()` and `strataUpload()`. Server-side v1/v2 routing is out-of-repo per R-4 v2.26 cross-repo amendment.
- **E2E target.** `playwright.config.ts` is dual-project: default `chromium` (static API) and `real-backend` (live backend, requires sibling repo `../ai-dashboard369-file-manager` + seeded staging DB). Select with `E2E_TARGET=real-backend npx playwright test`.
- **Cold-start sidebar.** Feature specs (`strata-nav` / `appfolio-parity-workorder` / `appfolio-parity-vendor-compliance`) require the `Property Management` widget group expanded. `helpers/auth.ts::loginAs` (L43) permanently seeds `qualia_sidebar_groups` localStorage with `["Property Management","AI Tools","Filing Cabinet"]` via `await page.addInitScript()` BEFORE `await page.goto('/')` so cold-start `Sidebar.tsx:228` `localStorage.getItem(...)` returns the seeded value when `useState` initializer fires. Canonical e2e auth helper — do not reintroduce inline-seed patterns in Playwright specs. `Scripts/run_axe_phase{N}.mjs` + `Scripts/run_lighthouse_phase{N}.mjs` retain inline-seed (non-Playwright contexts).
- **Strata window default size.** `'strata-dashboard'` opens at **1100×800** (centered, auto-region-snap-skipped) via `WindowContext.tsx::COMPONENT_DEFAULT_SIZES`. Quadrant-spawn default (~518–598 px on 1200 px desktop) collapses the 3-column grid (sub-sidebar 248 px + list-panel 320 px + detail-panel 1fr → 0). Container query at `StrataDashboard.css::.s-module` (`container-type: inline-size`) collapses `.s-split-view` to 1-col when rendered width < 700 px (defense-in-depth for manual narrowing). Other components keep quadrant-spawn behavior.
- **Tenant-row icon-button a11y aria-label.** Bulk-select icon-only `<button>` at `ResidentsModule.tsx:833` (within `filtered.map`) carries dynamic `aria-label={isBulk ? \`Deselect tenant${t.name ? \` ${t.name}\` : ''}\` : \`Select tenant${t.name ? \` ${t.name}\` : ''}\`}`. Defensive `t.name ?` fallback handles null/undefined for stub rows.
- **PRE0 mathematical-exactness signal (GR-15 v2.42).** When an a11y enumeration-rule violation count exactly equals (per-row-pattern-count × rendered-row-count) AND per-page distribution is even, single-pattern hypothesis is confirmable WITHOUT a CDP probe — single template-level edit fixes all simultaneously. Necessary-but-not-sufficient: uneven distribution requires CDP probe for sub-pattern disambiguation. Run this math at PRE0 BEFORE any CDP probe.
- **6-instance RefreshCw ghost-button aria-label.** The `<button className="s-btn s-btn-ghost"><RefreshCw size={14} /></button>` icon-only refresh pattern appears in 6 StrataDashboard module landing pages with contextual `aria-label="Refresh {entity-plural}"`: `VendorsModule.tsx:668` "Refresh vendors" / `MaintenanceModule.tsx:513` "Refresh maintenance items" / `PropertiesModule.tsx:522` "Refresh properties" / `LeasingModule.tsx:373` "Refresh leases" / `OwnersModule.tsx:107` "Refresh owners" / `AccountingModule.tsx:185` "Refresh accounting data". Future StrataDashboard modules following the s-btn-ghost RefreshCw pattern should inherit this convention (icon-only buttons fail axe-core `button-name` rule / WCAG 2.0 AA 4.1.2; `aria-label` provides discernible text without visual change).
- **Section content-div `id` attribute linking.** The `<Section>` component pattern in `VendorsModule.tsx:80-100` + `ResidentsModule.tsx:125-148` (each module has its OWN inline Section, NOT shared) renders `<button aria-controls="{module}-block-{slug}">`. The controlled content `<div>` must carry matching `id={\`vendor-block-${slug}\`}` / `id={\`tenant-block-${slug}\`}` — otherwise axe-core's `aria-valid-attr-value` rule flags `aria-controls` as referencing non-existent IDs. Future modules adding `<Section>`-style accordions follow this `aria-controls`/`id` linkage pattern.
- **2-layer altitude rule for `lazyWithReload` vs bare `React.lazy`.** Use **`lazyWithReload`** (from `qualia-shell/src/utils/lazyWithReload.ts`) for **top-level lazy candidates** at App.tsx / widgetRegistry.ts altitude — chunk-load failure has no recovery path without page reload (30+ widgetRegistry.ts data points at `src/registry/widgetRegistry.ts:53-270` + 5 App.tsx data points: AdminShell + TenantLoginScreen + TenantPortal + SecurityPortal + PopupShell + OpenJarvisWidget). Use **bare `React.lazy`** for **sub-component lazy candidates** at internal-component altitude — chunk-load failure would destroy surrounding shell state if `window.location.reload()` fired (`UniversalShell/adapters/StrataMaintenanceAdapter.tsx:27,30` + `components/InboxZero/InboxZero.tsx:18-24`). `lazyWithReload` wraps `React.lazy` with sessionStorage-gated `window.location.reload()` retry-once on `ChunkLoadError` / "Failed to fetch dynamically imported module" patterns. Named-export wrapping: `const X = lazyWithReload(() => import('./X').then((m) => ({ default: m.X })));`. Suspense fallback altitude: shared `<AppSuspenseFallback variant="viewport"|"popup" label?>` at `qualia-shell/src/components/Shell/AppSuspenseFallback.tsx` for App.tsx top-level use.
- **Paths-filter taxonomy.** Parity-gate (`.github/workflows/appfolio-parity-gate.yml`) paths-filter covers `qualia-shell/src/**` + `qualia-shell/app/**` (added at v2.68.1 post-Task 8.6 RR-v7 framework-mode adoption) + `.github/workflows/**` self-reference. Vitest-spec edits at `qualia-shell/src/test/**` AUTO-FIRE parity gate via `qualia-shell/src/**` glob. Playwright-spec edits at `qualia-shell/e2e/**` are OUTSIDE the filter — MANUAL-DISPATCH expected. Edits to `Scripts/**`, `Docs/**`, `Docs/Baselines/**`, `qualia-shell/playwright.baseline.config.ts`, root `.gitignore`, root `CLAUDE.md` also outside the filter.
- **`vi.useFakeTimers` + React 19 scheduler anti-pattern.** `vi.useFakeTimers()` mocks React 19's scheduler primitives (`MessageChannel` and microtask queue) that the React renderer uses for state-update commits in concurrent rendering mode. The `vi.useFakeTimers → vi.advanceTimersByTimeAsync → vi.useRealTimers` handoff is **empirically unsafe at jsdom + RTL + React 19 altitude** when the component-under-test has an async-cascade-to-render path: pending render commits stranded under fake primitives never fire on the real clock post-handoff. When the only Date-mocking need is `Date.now()` / `new Date()` anchoring (e.g., for "today" computation), use `vi.setSystemTime(...)` ALONE — it mocks the date without mocking scheduler primitives, leaving RTL's `waitFor()` to handle async settlement against the real clock deterministically. Reach for `vi.useFakeTimers()` ONLY when the component-under-test has explicit `setTimeout` / `setInterval` / `requestAnimationFrame` that the test needs to advance synchronously. Reference: `qualia-shell/src/test/appfolioParity/calendar.test.tsx` sister grid-regression (L212-255) + upcoming-events list (L257-294).
- **SCOPING-ONLY class definition (16th cumulative class).** Forward-scoping discipline shape — architectural inventory + framework decision tree (or per-component audit) + qualitative projections + roadmap recommendation + risks/OQs + Cowork decision gate. **No production source touched; no measurement; no closure narrative.** Structurally distinct from CLOSURE-NARRATIVE-CONSOLIDATION (retrospective), MEASUREMENT-ONLY (empirical re-measurement), DOC-CORRECTION-ONLY (single-finding investigation closure). When-to-apply: forward-scoping at any Phase-N OPENER OR mid-phase architectural-pivot task where the deliverable is a roadmap document anchoring downstream work — NOT a measurement, NOT a fix, NOT a closure.
- **Byte-count vs line-count discipline (v2.61.0).** Byte-count is the structurally-correct PRIMARY content-density measure across multiple document shapes; line-count is shape-dependent and not a primary scope-discipline measure. Matrix-heavy comparison-table documents (scoping shape) ~96 bytes/line; closure-narrative shape ~230 bytes/line — both structurally-correct for their respective shapes. When writing closure-narrative or scoping-matrix documents, target byte-count band first (60-85 KB for closure shape; size driven by empirical research density for scoping shape); line-count is informational only.
- **Library-mode RR v7 declarative routing pattern.** When adopting RR v7 ecosystem, distinguish LIBRARY MODE (`react-router` core only; `<BrowserRouter>` + `<Routes>` + `<Route>` inside App.tsx; preserves existing Vite SPA entry-point + index.html + main.tsx + vite.config.ts + build/dev scripts unchanged) from FRAMEWORK MODE (`@react-router/dev` + `@react-router/node` + `app/` directory + entry boundary files; replaces Vite SPA entry-point convention). Library-mode is appropriate for pre-framework-adoption routing migration where SSR enablement is deferred; framework-mode is the full adoption path.
- **Per-provider-SSR-safety taxonomy (3-altitude classification).** Classify `localStorage` / `window` / `document` / `navigator` access patterns for SSR-safety verdict at each React Context Provider:
  1. **Initialization-time UNSAFE** — inside `useState(() => …)` lazy initializer OR `const X = localStorage.getItem(…)` at module top-level = fires during React render() = throws `ReferenceError: localStorage is not defined` on server-side render attempt.
  2. **Effect-time SAFE** — inside `useEffect(() => …, [])` OR `useLayoutEffect(() => …, [])` = fires AFTER hydration on client (no-op on server since useEffect doesn't fire during renderToString / renderToPipeableStream).
  3. **Event-handler-time SAFE** — inside callback functions (`onClick`, `onChange`, `setItem` mutators) = fires on user interaction post-hydration.

  Init-time UNSAFE patterns require `useSyncExternalStore` migration (preferred per React 19 `getServerSnapshot` primitive) OR lazy-initializer-with-`typeof window` guards (fallback). Provider-tree SSR-safety must be evaluated at TREE altitude — a provider with no direct browser globals may still be SSR-unsafe via sibling dependency. Audit reference: `Docs/Phase8_Task_8_3_Provider_Tree_SSR_Audit.md §2-3`. **Post-Task-8.10 state:** App.tsx ThemeProvider + UserProvider (Task 8.9) + AdminShell-tree LayoutProvider + HierarchyProvider + WindowProvider + Sidebar leaf-component (Task 8.10) ALL migrated to `useSyncExternalStore` + factory pattern. ssr-flip enablement deferred to Task 8.11 per Q5 LOCK.
- **FOUC IIFE mitigation pattern (framework-agnostic theme initialization).** Pre-hydration theme attribute injection IIFE at `qualia-shell/app/root.tsx` `Layout` head (canonical Layout/Root/HydrateFallback 3-export pattern) sets `document.documentElement.className = 'theme-' + theme` BEFORE React hydrates. Reads `localStorage.getItem('dwellium-theme')` with `qualia-theme` legacy fallback + `'dark'` default; try/catch wraps `localStorage` access for sandboxed contexts. Pattern-content MUST match `className` pattern (NOT `data-theme` attribute) to match `ThemeContext.tsx:181-185` byte-for-byte. **Framework-mode constraint (Finding V):** at RR v7 framework-mode SPA Mode altitude, FOUC IIFE shipped via `index.html` alone is RENDERED INERT — build-time `renderToPipeableStream` ships RR v7's default HydrateFallback placeholder shell, not index.html-shipped content. FOUC IIFE MUST be HTML-shipped via Layout (wraps HydrateFallback at build time → FOUC IIFE auto-ships to HTML by construction). Empirical signature post-fix: `grep -c "dwellium-theme" build/client/index.html` = 1.
- **Chunk-axis-preservation taxonomy.** When tracking chunk-axis preservation across edits:
  - **Entry-graph-altitude edits** at `qualia-shell/src/**` BREAK `dist/assets/**` (or `build/client/assets/**`) chunk graph by construction.
  - **Parent-altitude edits** at `qualia-shell/index.html` PRESERVE chunk axes (Vite content-hashed graph; bundle resolution independent of HTML shell) AND CHANGE `dist/index.html` (or `build/client/index.html`) byte-count by construction (the source IS the build output for the HTML shell).
  - **Framework-mode build-output-directory:** at HEAD-post-8.6 with `@react-router/dev/vite::reactRouter()` plugin wired, build emits to `build/client/` (NOT `dist/`); SPA Mode (`ssr: false`) emits `build/client/index.html` + `build/client/assets/*.{js,css}`; `build/server/` is post-emission removed. Track chunk-axis preservation at `build/client/assets/**` glob.

  Always compare like-vs-like build modes — parity-gate canonical (bare `npx react-router build` / `VITE_APPFOLIO_SEEDS={true,false} npx react-router build`) vs static-API alt-build (`VITE_USE_STATIC_API=true`) produce structurally distinct chunks at the same source HEAD.
- **FRAMEWORK-INSTALLATION class (18th; 3pt FULL CALIBRATION + ROBUSTNESS at 8.6+8.7+8.11).** Production-source-altitude framework primitive set wiring: (i) packages (Vite plugin + server runtime + serve CLI + peer deps); (ii) framework config (`react-router.config.ts`); (iii) `app/` directory; (iv) root layout / entry boundaries; (v) declarative route config; (vi) `package.json` scripts cutover (`vite build` → `react-router build`); (vii) structural prod-source refactor (named-export promotion). Sub-shapes: (a) framework-mode adoption [8.6] + (b) entry-boundary customization [8.7] + (c) ssr-runtime enablement on framework-mode foundation [8.11]. Per-package install discipline: framework server runtime peers (`@react-router/node`, `@react-router/serve`, auto-installed `isbot`) MUST live in production `dependencies` (Finding S). Empirical failure signature when misplaced: *"Could not determine server runtime..."* at `react-router build` time. Reference: `react-router.config.ts` + `app/` + Completion Reports 8.6/8.7/8.11.
- **PROVIDER-SSR-REMEDIATION class (19th; 3pt FULL CALIBRATION + ROBUSTNESS at 8.9+8.10+8.11).** Production-source migration of `useState(() => browser-global)` lazy initializers → `useSyncExternalStore` + `createLocalStorageStore` factory pattern. Sub-shapes: (a) providers [8.9] + (b) providers-AND-leaf-components [8.10 Q1 LOCK Option A scope-expansion] + (c) useSyncExternalStore-migration-validation-under-true-SSR-runtime [8.11 Q1 LOCK Option D HYBRID co-shipping; smoke-test EMPIRICALLY VALIDATES all 14 factory-produced stores]. 14 cumulative stores at HEAD-post-8.11 (5 at 8.9 + 9 at 8.10). When-to-apply: any production-source touch surfacing init-time UNSAFE `useState(() => localStorage.getItem(...))` at Provider OR leaf-component OR widget altitude.
- **`createLocalStorageStore` factory dual-signature + `.reset()` standing convention.** Factory at `qualia-shell/src/utils/createLocalStorageStore.ts` accepts (i) **positional** `(readFromStorage, serverDefault)` for static-key stores OR (ii) **object** `{ key: string | (() => string), deserializer, defaultValue }` for dynamic-key stores requiring per-render-resolvable keys (8.10 Q2 LOCK Option β). Dynamic-key cache invalidates automatically when resolver returns different key vs cached; requires module-level holder pattern in consuming Provider (e.g., `savedLayoutsUserIdHolder.current` at `WindowContext.tsx`) updated DURING render BEFORE `useSyncExternalStore` invocation. v2.72.1 PERMANENT standing convention (Task 8.10 Q3 LOCK): every factory-produced store MUST export `.reset()` escape-hatch; test files MUST call `.reset()` in `beforeEach` (prevents cross-test module-cache pollution).
- **RR v7 framework-mode primitives empirical record (Findings U-REVISED + W + Y).** (1) `route('*', 'file')` splat does NOT match root `/` (RR v6 → v7 API divergence); canonical: `index('file')` matches root, `route('path', 'file')` matches specific, `route('*', 'file')` matches non-root catch-all. Default route IDs derive from file paths — duplicates require explicit `id` disambiguation. (2) `entry.server.tsx` IS structurally invoked at build time even at `ssr: false` (SPA Mode `routerContext.isSpaMode` branch → `onAllReady` callback generates static `build/client/index.html` shell). (3) Per-route SSR opt-out is structurally infeasible at RR v7.15.1 — `ReactRouterConfig::ssr` is GLOBAL boolean only. Reference: `app/routes.ts` + Conventions block + Completion Reports 8.6/8.7/8.8.
- **`npx vite build` SILENT NO-OP with `@react-router/dev/vite::reactRouter()` plugin wired.** Direct vite-CLI invocation silently no-ops the build pipeline (exit 0 + zero artifacts). Build orchestration MUST use `npx react-router build` (framework-mode CLI). CI workflow build steps + Playwright webServer command + smoke-test Phase A bootstrap all use `npx react-router build` post-Task-8.6 v2.66.1 in-place patch.
- **In-place v2.X.X patch cluster (Phase-8+ Block B).** 7 cumulative patches across Block B (v2.66.1+v2.66.2+v2.66.3 at 8.6 build-command/server-startup/routing-config + v2.68.1 at 8.8 paths-filter + v2.72.1 at 8.10 .reset() + v2.73.1 at 8.11 webServer `vite preview` → `react-router-serve`). Pattern: in-place v2.X.X amendments within a single task close cycle to remediate empirical CI compat surfacing at implementation are structurally acceptable (extends Phase-7 7.3/7.4/7.9 precedents). Reference: per-task Completion Reports.
- **Per-provider-SSR-safety taxonomy (3-altitude classification).** Browser-global access (`localStorage`/`window`/`document`/`navigator`) classifies as: (1) **init-time UNSAFE** — `useState(() => ...)` lazy init OR module top-level (throws `ReferenceError` on server-render) → requires `useSyncExternalStore` + factory migration; (2) **effect-time SAFE** — `useEffect(() => ...)` / `useLayoutEffect`; (3) **event-handler-time SAFE** — callbacks. Provider-tree SSR-safety evaluated at TREE altitude (sibling-provider dependencies count). **Post-Task-8.11 state:** All Phase-8+-audited sites remediated (Theme + User + Layout + Hierarchy + Window Providers + Sidebar leaf-component). Smoke-test EMPIRICALLY VALIDATES at chromium-headless probe (zero `ReferenceError` / zero hydration mismatch). Finding II widget-altitude (TranscriptionHub.tsx:376 `useState(() => window.SpeechRecognition)`) cemented as INFORMATIONAL deferred-to-Phase-9+ widget-SSR-audit per Task 8.11 Q6 LOCK (operationally unreachable at initial server-render due to AuthGate Branch 1 gating).
- **AuthGate hydration-flash empirical resolution (Task 8.11 Q2 LOCK Option α cemented; Finding EE PERMANENT baseline).** Flash exists at BOTH `ssr:false` AND `ssr:true` (NOT a `ssr:true` regression); `ssr:true` empirically REDUCES transitions (1 spinner → final-view vs 2 HydrateFallback → spinner → final-view at `ssr:false`). Smoke-test HARD-blocks if hydration mismatch warnings surface (HALT-IF #3 escalation → Option β Suspense OR Option γ pre-hydration cookie). Options β + γ DEFERRED to Phase-9+ polish.
- **SSR smoke-test bundle discipline (`Scripts/smoke_test_ssr_phase8.mjs`; Task 8.11 Q4 LOCK 5-phase design).** Production-build SSR runtime verification via chromium-headless probe. Phases A→E: Bootstrap (`react-router build`; `SMOKE_TEST_SKIP_BUILD=true` for local) + Server start (`npx react-router-serve build/server/index.js`; env-overridable port) + Probe (playwright-chromium `page.goto({ waitUntil: 'domcontentloaded' })` + 1.5s hydration grace; NOT `networkidle` — app has persistent async fetches) + Assertions HARD-blocking (zero `ReferenceError` + zero hydration mismatch warnings + FOUC IIFE/AuthGate sanity) + Cleanup (SIGTERM + force-kill 2s + browser close). CI integration: BLOCKING step at `.github/workflows/appfolio-parity-gate.yml` after `react-router build (seeds=true; pre-Playwright preview)`. Vitest delta +0 (Node.js script). Sister-shape to Phase-7 `Scripts/run_lighthouse_phase7.mjs`.
- **Anchor-bias-mitigation 16-pattern cluster (Phase-8+ standing PRE-FLIGHT discipline cluster).** v2.60.1 (falsified-hypothesis empirical-verification at 12 altitudes) + v2.60.4 + v2.60.6 + v2.62.1 + v2.64.0 (audit-content empirical-vs-hypothetical at 8 altitudes — 6 REFUTATION + 2 CONFIRMATION oscillation pattern cemented as engineering-record discipline regardless of outcome direction) + v2.65.0 + v2.66.0 + v2.67.0 + v2.68.0 + v2.69.0 + v2.70.0 + v2.71.0 (audit-scope-completeness) + v2.72.0 (leaf-component-altitude SSR-remediation) + v2.73.0 (Step-7-entry whole-file-read; READ ENTIRE FILE for every audit-scoped source file BEFORE first Edit/Write; sister-shape to v2.71.0) + v2.74.0 (dynamic-key classification: STATIC vs DYNAMIC key resolver) + v2.74.1 (branch-base discipline: feature branches MUST be created from `origin/main` AFTER prior Task squash-merge propagated; `git rebase --onto origin/main <prior-task-commit-SHA>` resolution if blocked). At every task PRE0 + Step-7-entry, treat audit citations as STARTING-POINT-HYPOTHESES requiring empirical verification.
- **6-consecutive PRE0/Step-7-entry wrong-hypothesis-refutation cross-altitude pattern (Task 8.10 Q5 LOCK).** Findings L+O+W+Y+Z at PRE0 altitude (Tasks 8.5-8.9) + Finding GG at Step-7-entry altitude (Task 8.10) — recursive-validation discipline IS the project's most substantive engineering record pattern.

---

## Useful commands

- **Strict gate (mirrors CI; Task 8.11 ssr:true update):**
  `cd qualia-shell && npx tsc -b && npx vitest run && npx react-router build && VITE_APPFOLIO_SEEDS=false npx react-router build && cd .. && node Scripts/verify_no_pii_leak.mjs && SMOKE_TEST_SKIP_BUILD=true node Scripts/smoke_test_ssr_phase8.mjs`
- **Dispatch parity gate:**
  `gh workflow run "AppFolio Parity Gate" -R NovaTrustSolutions/dwellium-per-spec --ref main`
- **Watch latest run:**
  `gh run watch $(gh run list -R NovaTrustSolutions/dwellium-per-spec --workflow "AppFolio Parity Gate" --limit 1 --json databaseId -q '.[0].databaseId') -R NovaTrustSolutions/dwellium-per-spec --exit-status`
