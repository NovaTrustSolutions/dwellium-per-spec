# Implementation Plans

Generated by the `improve` skill on 2026-06-20, audited against commit **`a619279`**
(`standard` depth). Each plan is self-contained for an executor with zero prior context.

**Before executing any plan, read it fully and honor its STOP conditions.** All
verification (vitest, builds, `Scripts/gate.sh`) runs on the **Mac** — the Linux sandbox
cannot run Vite/vitest (missing native binaries; `tsc` works there). Build with
`npm run build` / `npx react-router build` — **`npx vite build` is a silent no-op** in this
framework-mode app. Per repo policy (`CLAUDE.md`): the gate must be green before commit,
and **do not push or open a PR without Ilya's explicit go.**

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix README quick-start (build/Node/login) | P1 | S | — | DONE — shipped 19d0f3b (pushed) |
| 002 | Unify Node version on 22 | P1 | S | (pairs w/001) | DONE — shipped 19d0f3b (pushed) |
| 005 | Declare `@huggingface/transformers` dependency | P1 | S | — | DONE — shipped 19d0f3b (lockfile synced, pushed) |
| 010 | Remove `onclick` from markdown sanitizer (XSS) | P1 | S | — | DONE — shipped 19d0f3b (pushed) |
| 012 | Mask Sentry Session Replay + drop email | P1 | S | — | DONE — shipped 19d0f3b (pushed) |
| 006 | Defer the 71 MB login video until interaction | P1 | S | — | DONE — shipped 19d0f3b (pushed) |
| 007 | Stop eager-loading intro videos; delete dead v1 | P2 | S | — | DONE — shipped 19d0f3b (preload=none + dead halocron-intro.mp4 removed) |
| 016 | Stop OpenJarvis mic on unmount (privacy) | P1 | S | — | DONE — shipped 19d0f3b (pushed) |
| 011 | Add CSP (report-only) + security headers | P1 | S–M | (complements 010) | DONE — shipped 19d0f3b (pushed) |
| 015 | One Save: retry dropped writes + isolate bootstrap | P1 | M | — | DONE — fea6104 + retry-guard fix 991452e (gate GREEN, pushed) |
| 008 | Lazy-load Halocron OS + its heavy screens | P1 | M | — | DONE — shipped fea6104 (gate GREEN at 991452e, pushed) |
| 009 | Lazy-load moonshine (2.37 MB off main chunk) | P1 | M | 005 | DONE — shipped fea6104 (gate GREEN at 991452e, pushed) |
| 003 | Add ESLint (non-blocking gate) | P1 | M | — | DONE — fea6104 + typescript-eslint fix 991452e (gate GREEN, pushed) |
| 004 | Archive stale root docs + launch scripts | P2 | S | — | PARTIAL — shipped 19d0f3b (13 docs archived); 9 launch scripts kept at root (referenced by AutonomousRuns.tsx) — follow-up: relocate + repoint |
| 013 | Strengthen at-rest secret encryption (opt. passphrase) | P2 | S/M | — | TODO |
| 014 | Remove committed account creds + PII | P2 | M | — | TODO |
| 021 | Gmail/Calendar OAuth (original) | P2 | M | — | SUPERSEDED by 027 (OAuth largely wired 2026-07-02) |

### Round 2 — audited 2026-07-02 against `730c82a` (frontend) / `6695bd4` (backend), `standard` depth

Covers the backend repo (`../dwellium-backend/ai-dashboard369-file-manager`) for the first time, plus a frontend correctness/debt pass. Selected by Ilya: all four fix-it bundles + all three direction spikes.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 023 | Backend security quick-wins (seed god password, error leak, IP trust) | P1 | S | — | DONE + MERGED to main (e418f1c) + DEPLOYED to Cloud Run (rev dwellium-backend-00023-bq4, healthy). ⚠️ Ilya must still rotate any already-seeded andy@dwellium.com password (removing the literal only affects fresh seeds). Backend main NOT yet pushed to GitHub. |
| 024 | Backend test + CI baseline (auth/objects/audit) | P1 | L | — | DONE (branch advisor/024-backend-test-baseline @ b08f6f1; APPROVED by review — 39 tests GREEN [21 integration + 18 pre-existing], tsc 0, scope clean [11 files]). Solved a jest/jsdom ESM blocker via a stub. ⭐ The suite CAUGHT + FIXED a real production bug: failed logins returned HTTP 500 (audit FK crash) instead of 401 — now fixed in auditLogStore.ts. NOT merged/pushed — awaiting Ilya. Contains a LIVE-bug fix worth deploying. |
| 025 | Decouple the shared per-user-id holder (kill #185 loop class) | P2 | M | — | DONE (branch advisor/025-decouple-holder; APPROVED by review — full gate GREEN 1634 vitest, GOAL grep clean, 12-holder single-writer, storage keys preserved; reviewer cleaned commit hygiene; NOT merged/pushed — awaiting Ilya) |
| 026 | Paginate + de-block the One Save object store | P2 | M | 024 | DONE + MERGED to backend main (fa9e529) + PUSHED. APPROVED — 11 tests green, ownership preserved under pagination, response shape unchanged, scope clean (3 files). NOT yet deployed (rides next backend deploy). Executor deferred full-async writes (documented follow-up). |
| 027 | Complete live Gmail/Calendar OAuth (per-user token storage) | P2 | M | 024 | DONE (code) — APPROVED; branch `advisor/027-gmail-oauth-close` @ `4fc95f7`. Spike CONFIRMED tokens were PLAINTEXT → now AES-256-GCM at rest (env `TOKEN_ENCRYPTION_KEY`, fail-soft, migration-safe read); `invalid_grant` → persisted `needs_reauth` status via /oauth/status; 20/20 google tests + full suite green. DECISION recorded: Testing mode stays (memo). Operator follow-ups: (1) set `TOKEN_ENCRYPTION_KEY` on Cloud Run (`openssl rand -base64 32`); (2) real interactive consent round-trip after deploy; (3) frontend Reconnect button consuming `needs_reauth` (small follow-up plan if wanted). NOT merged — awaiting Ilya |
| 028 | SPIKE: ThoughtWeaver ↔ Supabase cross-device sync design | P3 | M | — | TODO |
| 029 | SPIKE: consolidate moonshine vs @huggingface/transformers | P3 | S | — | TODO |

### Round 6 — LIVE-site audit 2026-08-16 against https://argyleholocron.netlify.app/ (deploy etag `e06d26d…`, local HEAD `3530a73`)

| Plan | Title | Priority | Effort | Repo | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 043 | Live-site improvement plan — 10 items: creds in bundle (=014), LCP image, 17-family font link, immutable asset cache, 74 MB video, missing favicon/OG/robots, mobile login clip, CSP enforce | P0–P2 | S each | frontend | 014 (item 1) | DONE (items 2–8, 10) + PARTIAL (9: Permissions-Policy + CSP font origins; enforce blocked by RR inline scripts) + SKIPPED (1, per Ilya) — branch `advisor/043-live-site-fixes`, tsc/vitest 1775/build GREEN, browser-verified 375 w + 1280×720; NOT pushed/deployed — awaiting Ilya |

### Round 3 — audited 2026-07-03 against `df3bf79` (frontend) / `fa9e529` (backend), delta + reconcile depth

Delta audit only (Round 2 was one day prior; its findings and rejections stand). Sources: the
two commits since `730c82a` (both vetted clean) + the 2026-07-03 live auth incident (Netlify +
Cloud Run env wipe, OAuth origin_mismatch, consent-screen 403). Selected by Ilya: all five.

**Designed for parallel execution by 3–5 agents — no two Round-3 plans share a file.**

| Plan | Title | Priority | Effort | Repo | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 030 | Record failed logins in the audit log (sentinel-user FK gap) | P1 | S | backend | — | DONE (round 2 after plan amendment: schema is `dwelliumSchema.ts`, sentinel role='tenant' per CHECK enum) — APPROVED; branch `advisor/030-failed-login-audit` @ `b8a8636`, worktree `~/dwellium-backend/worktrees/advisor-030`; 30/30 integration tests; NOT merged/pushed — awaiting Ilya |
| 031 | Deploy-env baseline manifest + read-only drift verifier | P1 | S–M | frontend (Scripts/Docs) | — | DONE — APPROVED; branch `advisor/031-deploy-env-baseline` @ `68c0b3c`, worktree `.advisor-worktrees/031`; verifier re-run by reviewer against live site: 3 PASS / 1 SKIPPED, exit 0; NOT merged/pushed — awaiting Ilya |
| 032 | OAuth Testing-mode constraint + publish decision memo | P2 | S | frontend (docs) | — (informs 027) | DONE (round 2 re-scoped: memo only; reviewer applied the plan-027 append directly — `plans/` is untracked so it was absent from the worktree) — APPROVED; branch `advisor/032-oauth-testing-decision` @ `782cf16`; 7-day Testing-mode token expiry CONFIRMED against Google docs; memo ends with `DECISION (Ilya): ____` — needs Ilya's call |
| 033 | Audit server: IP geo enrichment + `AUDIT_LOG_VIEWER_EMAILS` allowlist | P1 | M | backend | — | DONE — APPROVED; branch `advisor/033-audit-geo-allowlist` @ `da358fe` (2 commits), worktree `~/dwellium-backend/worktrees/advisor-033`; 50/50 integration tests; reviewer re-verified tsc + scope + diff; NOT merged/pushed — awaiting Ilya |
| 035 | `deploy/cloud-run.sh`: preserve-unknowns env merge + DEPLOY_DRY_RUN (kills the wipe class) | P1 | S–M | backend | — | DONE — APPROVED; branch `advisor/035-env-preserve` @ `e2d353e`; dry-run verified against live service (6 vars preserved, secrets skipped, revision untouched); NOT merged — awaiting Ilya |
| 036 | Checked Netlify deploy wrapper + scheduled env-drift workflow | P1 | S | frontend | 031 (DONE) | DONE — APPROVED; branch `advisor/036-deploy-wrapper` @ `5fac7c8` (2 commits); dry-run gates PASS, YAML valid; operator follow-up: dispatch "Env Drift Check" once after merge; NOT merged — awaiting Ilya |
| 034 | Audit widget: render locations + capability-based access | P2 | M | frontend | soft: 033 (degrades gracefully without it) | DONE — APPROVED; branch `advisor/034-audit-widget-access` @ `fb33437` (2 commits), worktree `.advisor-worktrees/034`; full vitest 1635 green + build green; lint gate not met at REPO level (741 pre-existing errors, ZERO in touched files — documented deviation, accepted); NOT merged/pushed — awaiting Ilya |

**Parallel dispatch guide (one wave, 3–5 agents):**

- **Agent A** → 030 (backend: `authRoutes.ts` call sites untouched; edits `database.ts`, `auditLogStore.ts` comment, new test)
- **Agent B** → 033 (backend: `auditRoutes.ts`, new `ipGeoService.ts`, new test) — same repo as A, **disjoint files**; use separate branches/worktrees
- **Agent C** → 034 (frontend: `AuditLog/` + `widgetRegistry.ts` line 68 + its test)
- **Agent D** → 031 (frontend: new `Scripts/verify_deploy_env.mjs` + `Docs/ops/DEPLOY_ENV_BASELINE.md`)
- **Agent E** → 032 (frontend: new `Docs/ops/GOOGLE_OAUTH_PUBLISHING.md` + append-only edit to plan 027) — may be folded into D for a 4-agent run

Merge order after all approve: backend 030 → 033 (rebase trivial — no shared files), frontend any order.
Post-merge operator steps (Ilya): deploy backend (033 needs `AUDIT_LOG_VIEWER_EMAILS` set on Cloud Run), Netlify redeploy for 034, then add the new env var to 031's baseline doc.

**Round 3 execution notes (2026-07-04):**

- All five plans executed in ONE parallel wave by 5 executor subagents in isolated worktrees; 3 approved first-pass, 2 stopped correctly on plan errors, were amended, and approved on round 2.
- **Merge order for Ilya**: backend `advisor/030-…` then `advisor/033-…` (disjoint files, trivial rebase); frontend `advisor/031-…`, `advisor/032-…`, `advisor/034-…` in any order. After merging: deploy backend with NEW env `AUDIT_LOG_VIEWER_EMAILS` set on Cloud Run; Netlify redeploy picks up 034; add `AUDIT_LOG_VIEWER_EMAILS` to `Docs/ops/DEPLOY_ENV_BASELINE.md` (031's doc) at that time.
- **Repo-hygiene finding (from 032 round 1): `plans/` has NEVER been committed to git** — it exists only as untracked working-tree files. One `git add plans/ && git commit` would version this whole planning history. Ilya's call.
- Cleanup after merging: `git worktree remove` the four `.advisor-worktrees/*` + `~/dwellium-backend/worktrees/*` entries and delete merged branches.
- Standing backend follow-up confirmed again: jest open-handle leak (a bare `npx jest <file>` without `--forceExit` hangs after green tests) — the `--detectOpenHandles` cleanup noted at Round 2 remains worth doing.

**Round 3 merge/deploy log (2026-07-04, Ilya's go):**

- Backend main: 030 + 033 MERGED (`cd2e14b`), tsc clean, **175/175 tests green** post-merge. Deploy pending `gcloud auth login` (expired). ⚠️ Root cause of the recurring env wipes CONFIRMED: `deploy/cloud-run.sh` deploys with `--env-vars-file`, which REPLACES the full env set — `GOOGLE_CLIENT_ID`, `GOOGLE_AUTH_ALLOWED_EMAILS`, `AUDIT_LOG_VIEWER_EMAILS` must be re-applied via `gcloud run services update --update-env-vars` after EVERY script deploy until the script is fixed (candidate plan 035).
- Frontend main: 031 + 032 + 034 MERGED (`8892c68`), **full gate GREEN** (`Scripts/gate.sh` exit 0 incl. SSR smoke), **PUSHED to origin/main**. Production-mode build artifact ready at `qualia-shell/build/client` (verified: `_redirects` proxy lines, client-ID baked, new AuditLogWidget chunk, `VITE_API_URL` empty-override). **DEPLOY BLOCKED: Netlify account credit usage exceeded — API refuses all new deploys until credits are added.** Once resolved: `cd qualia-shell && npx netlify-cli deploy --prod --dir build/client`.

**Round 3 SHIPPED (2026-07-04):** Frontend deployed to Netlify (deploy `6a48affd…`; first attempt 404'd — `netlify deploy` re-ran the build WITHOUT the NETLIFY env → ssr:true → no index.html; rolled back in ~3 min via restoreSiteDeploy, redeployed with `--no-build`; **rule: always `netlify deploy --prod --no-build --dir build/client` after a NETLIFY=true local build**). Backend deployed to Cloud Run (revision `dwellium-backend-00029-vp5`; first build failed on a PRE-EXISTING package-lock desync — synced at `db40f4c`, pushed). Post-deploy env re-applied: `GOOGLE_CLIENT_ID` + `GOOGLE_AUTH_ALLOWED_EMAILS` + `AUDIT_LOG_VIEWER_EMAILS` = andy@dwellium.com (verified on revision). End-to-end verifier: ALL PASS. Both repos pushed to GitHub (frontend `8892c68`, backend `db40f4c`). Remaining human items: `DECISION (Ilya)` line in `Docs/ops/GOOGLE_OAUTH_PUBLISHING.md`; plan 035 candidate (fix `deploy/cloud-run.sh` env wipe); worktree cleanup.

**🚀 ROUND 5 SHIPPED 2026-07-04 (Ilya's go):** 037 + 038 + 039 + 040/041/042 all MERGED to main (`5e6545b` incl. OAuth-DECISION docs commit; zero merge conflicts; both new perUserIdentity holders coexist = 14 total), full gate GREEN on merged main, PUSHED to origin, deployed via `Scripts/deploy_netlify.sh` (deploy `6a4a00e1…`; wrapper's post-deploy verifier ALL PASS). Live freshness PROVEN by content-hash chunk probes: `FluidOS-BjxwEfm1.js` / `AvatarHarness-B_GpDqpC.js` / `AuditLogWidget-kdg-xM2M.js` all 200 on production + FaceLandmarker present in the live eye-contact page. NOTE: the 2KB route manifest does NOT enumerate widget-level lazy chunks — freshness checks must probe exact hashed filenames from the local gate-green build (learned this round). Worktree cleanup pending (9 advisor worktrees). Backend avatar branch remains DO-NOT-MERGE.

**Plan 042 (keyless local avatar provider) — DONE, APPROVED 2026-07-04.** Same FE branch, now @ `2f0e05d`. Avatars need NO key at all by default: `LocalPhotoAvatarAdapter` (photo → MediaPipe FaceLandmarker once → canvas talking head: blinks 2-6s, ±0.6° sway, gaze micro-shifts, viseme-envelope mouth warp from speechSynthesis boundary events w/ 10.5Hz syllable fallback; browser TTS voice picker; optional keyless SpeechRecognition mic; brain = existing per-user llmClient keys w/ spoken fallback), provider defaults 'local', Anam optional ONLY when its key exists (zero Anam mention keyless — test-pinned), empty-vault-reaches-live test-pinned, full teardown spied. 1663/1663 vitest, gate GREEN. Reviewer re-verified: lineage 2f0e05d→fed620a→5efd17c, 19/19 focused. Executor finding (recorded): the live eye-contact MediaPipe prototype is served by a dev-only vite plugin from a host scratch folder — plan 037's committed page supersedes it at merge. Owner live pass judges warp quality.

**Plan 041 (backendless avatar rework) — DONE, APPROVED 2026-07-04, SUPERSEDES 040's backend.** Same FE branch `advisor/040-avatar-harness-fe`, now @ `fed620a` on top of `5efd17c`. Fully backendless per Ilya's directive: browser-direct api.anam.ai (CORS `*` verified empirically), key = new "Anam Avatar Engine" card in the per-user encrypted vault (mirrors RecallCard; crypto extension mechanical), profiles = new per-user `avatarProfilesStore` (One Save `avatar-profiles`, 13th holder in perUserIdentity), zero `/api/avatar` references remain (reviewer-verified grep=0), no key logging (verified), consent + teardown preserved. Gate GREEN, 1646 vitest. **Plan-040 BACKEND branch `advisor/040-avatar-harness-be` = OPTIONAL/DO-NOT-MERGE** (server-mediated mode if ever wanted). No Cloud Run env needed — the user pastes their Anam key in Settings → API Keys. CSP-enforcement note: add `https://api.anam.ai` + WebRTC origins (enumerate from live network tab) to connect-src when flipping CSP.

**Plan 040 (Anam avatar harness) — DONE, APPROVED 2026-07-04 [backend half superseded by 041 — see above].** Cross-repo: FE branch `advisor/040-avatar-harness-fe` @ `5efd17c` (worktree `.advisor-worktrees/040-fe`; 10 files: AvatarHarness + AnamAdapter + AvatarSetupPanel w/ REQUIRED likeness-consent gate + avatarClient + tests; ARAConsole swapped onto the harness keeping its talk-through-avatar behavior; Stella gains an avatar toggle) + BE branch `advisor/040-avatar-harness-be` @ `4c12d0a` (avatarRoutes: session-token/create-from-image/options/per-user profiles; avatar_profiles table; araRoutes back-compat shim; API key never echoed — reviewer-verified). Pinned: Anam v1 session-token/avatars/voices APIs + js-sdk@4.10.0 (streamToVideoElement/talk/interruptPersona). FE gate GREEN (1640 vitest), BE 198 tests green. Any future agent = `<AvatarHarness agentId="x" />`. Operator steps at merge: set `ANAM_API_KEY` on Cloud Run (survives deploys per 035) + add to DEPLOY_ENV_BASELINE.md. NOT merged — awaiting Ilya.

**Plan 039 (Fluid OS third layout) — DONE, APPROVED 2026-07-04.** Branch `advisor/039-fluid-os` @ `cf2c1e2`, worktree `.advisor-worktrees/039`. Third interchangeable interface layout (EverSwap/Lusion motion language, original implementation, no new deps): rAF spring physics (stiffness 210/damping 26), canvas metaball background (pauses when closed; teardown verified — cancelAnimationFrame + listener removal), full registry parity incl. `restrictedToEmails` gating (parity test pins audit-log visibility rules), keyboard nav + `/` search + Escape, `prefers-reduced-motion` at JS+CSS layers, FluidLauncher droplet, 3-way layout toggle w/ mutual exclusivity in BOTH toggle surfaces (HalocronOS settings + ControlPanel Appearance). Gate GREEN ×2, 13 new tests. Live visual pass = Ilya (headless QA blocked by auth flow — environmental). NOT merged — awaiting Ilya.

**Plan 038 (universal app history + expandable audit) — DONE, APPROVED 2026-07-04.** Branch `advisor/038-app-history` @ `0a3ddf2` (2 commits), worktree `.advisor-worktrees/038`. Every app now logs open/close automatically at the WindowContext choke point; `logActivity()` API + 3 exemplar widgets (Terminal/Stella/ThoughtWeaver, 140-char previews, no secrets); per-login ring-buffer store (2000) synced via One Save (`activity-log`), holder in perUserIdentity (F-015 discipline); Audit Log: expandable rows on ALL tabs (full details JSON/IP/location/ISO) + "My Activity" tab for ANY signed-in user (local, no fetch, works under 403). Gate GREEN, 1650 vitest. NOT merged — awaiting Ilya. Follow-ups deferred: backend mirror, retention UI, remaining widgets' domain events.

**Plan 037 (eye-contact real correction) — DONE (code) + reviewer-verified visually, 2026-07-04.** Branch `advisor/037-eye-contact` @ `7bc61de`, worktree `.advisor-worktrees/037`. MediaPipe FaceLandmarker (pinned tasks-vision@0.10.35) iris-warp with all naturalness clamps (≤30% displacement, dead zone, blink/pose guards, 150ms envelope, feathered clip — warps the user's own pixels, never overlays). Full vitest green (1632). Reviewer ran the photo-demo in a real browser: correction engages ("correcting" meter), gaze visibly re-centered, NO seams / eyelid distortion / changes outside eyes. **Awaiting Ilya's live-camera aesthetic pass (the acceptance bar) before merge.** Note: enable toggle defaults OFF — flip "Enable eye-contact correction" + set strength. Follow-up candidate: pipe corrected canvas into AraMeeting's actual call stream (captureStream).

**Round 4 SHIPPED (2026-07-04, Ilya's go):** 027 + 035 merged to backend main (`8651525`, 185/185 tests, pushed) · 036 merged to frontend main (`5fac7c8`, gate GREEN, pushed) · backend deployed via the NEW preserve-pass script — **"Preserved 6 existing env var(s)" in the real deploy log = plan 035 validated in production** · revision `dwellium-backend-00032-gfw` live with `TOKEN_ENCRYPTION_KEY` set (generated `openssl rand -base64 32`, value never displayed/stored outside Cloud Run) · verifier ALL PASS · `Env Drift Check` workflow dispatched (first run: actions/runs/28709921405). **Remaining operator items:** (1) Ilya performs the interactive Gmail Connect in the app to verify the real consent round-trip (tokens will now persist encrypted; weekly expiry surfaces as `needs_reauth`); (2) optional: frontend Reconnect button consuming `needs_reauth` (small follow-up plan); (3) worktree cleanup (8 advisor worktrees across both repos); (4) decide whether to `git add plans/` (still untracked).

**Round 3 status reconciliation (index corrections):**

- Plan **025** → DONE + **MERGED to frontend main** at `df3bf79` (2026-07-02) — previous row said "awaiting Ilya".
- Plan **024** → DONE + **MERGED to backend main** at `b08f6f1` — previous row said branch-only. Backend main is still **NOT pushed to GitHub**.
- **2026-07-03 incident record**: Netlify `VITE_GOOGLE_CLIENT_ID` restored + rebuilt (deploy `6a47ca26…`); Cloud Run `GOOGLE_CLIENT_ID` + `GOOGLE_AUTH_ALLOWED_EMAILS` restored (new revision); OAuth client gained JS origin `https://argyleholocron.netlify.app`; consent test users now iklipinitser@gmail.com + andy@dwellium.com. Root-causing the wipe class is plan 031.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale) | SUPERSEDED.

Recommended first five (cheapest, highest-leverage, clean verification): **001, 010, 012, 006, 015.**

**Tier 1 batch — ✅ SHIPPED `19d0f3b` → main (gate-green, pushed 2026-06-20).** See [`000-tier1-implementation-plan.md`](000-tier1-implementation-plan.md) for the umbrella plan (scope, who-it's-for / who-it's-not-for, sequencing, risk) covering plans **001, 002, 004, 005, 006, 007, 010, 011, 012, 016**. (004 launch-script relocation is the one remaining follow-up.)

## Dependency notes

- **009 requires 005**: lazy-loading/possibly removing moonshine relies on `@huggingface/transformers` being a *declared* direct dependency (today it's only transitive via moonshine).
- **011 complements 010**: CSP is the defense-in-depth layer under the XSS fix; land 010 first, but 011 can proceed independently (report-only, breaks nothing).
- **002 pairs with 001**: both must state Node **22**; land them together or 001 first.
- 013 Phase 2 and 014 Step 2 each contain an **operator decision gate** (passphrase opt-in; login bootstrap) — resolve with Ilya before the gated step.
- **Round 2 ordering: 024 first** (verification baseline) — it unblocks safe verification of **023** and is a hard dependency of **026** and **027** (their ownership/token tests need the harness). Recommended order: **024 → 023 → 026 → 027 → 025**, with spikes **028 / 029** any time (independent).
- **025 and 027 depend on nothing structurally but 027's tests need 024's harness.** 025 (holder decouple) is self-contained frontend and can run in parallel.
- **Cross-repo note:** plans 023, 024, 026, 027 touch the **backend** repo (`/Users/ilyaklipinitser/dwellium-backend`, app in `ai-dashboard369-file-manager/`), stamped at backend commit `6695bd4`. Plan 025 touches the **frontend** repo at `730c82a`. Each plan's drift check targets the right repo.
- **Operational (not a plan, but flagged 2026-07-02):** Netlify CI builds have been publishing **stale output** (49s "builds" reusing old chunks); recent prod deploys were done via `netlify deploy --prod` CLI from a local `react-router build` instead. Root-cause the CI build cache before relying on push-to-deploy. The backend repo also has local commits (`7e87ec8`, `6695bd4`, deploy-script fix) **not yet pushed** to `NovaTrustSolutions/Dwellium`.

## Audited but NOT selected this round (tracked, not planned)

These vetted findings were out of the four selected bundles. Listed so they aren't re-audited:

- **CORRECTNESS** — TranscriptionHub unmount leaks the chunk-recorder loop/timers (`TranscriptionHub.tsx:2203-2223`); no `AbortController` on widget fetches (StellaAgent/ARAConsole/Desktop); inconsistent email-iframe `srcDoc` sanitization (`InboxWidget.tsx:377`). (OpenJarvis mic-on-unmount is now planned as **016**, part of Tier 1.)
- **SECURITY** — NL agent skills run arbitrary JS via `new Function` + pipe untrusted text between steps (`skills.ts:131`, `conductorChain.ts:175`) — prompt-injection→action surface (MED; needs trust-boundary work); verbose upstream errors echoed to UI (`UserContext.tsx:316`, `skills.ts:311`); `protobufjs` high advisory reachable only via the transcription model (monitor — upstream fix).
- **TECH DEBT / TESTS / PERF** — god-file seam extraction (TranscriptionHub/ARAConsole/Stella); thin characterization tests on highest-churn files (ARAConsole, Desktop, oneSaveStore) — **prerequisite before any god-file refactor**; Strata list re-derivation without memoization (`PropertiesModule.tsx:548`); 3 state patterns (zustand 2 stores / react-query 7 / factory 61); `oneSaveClient`/`pdfRaster` static+dynamic conflict; missing root `.env.example`; auth-UI test thinness.

## Direction (options for the maintainer — design/spike plans if pursued)

Not problems; grounded next-steps. Each would become a *design/spike* plan, not a build-everything plan.

- **ThoughtWeaver phone↔desktop sync via Supabase** — fully specced in `BACKLOG.md`; capture store is local-only `localStorage`; per-user Supabase already wired. Trade-off: preserve the "never-deleted / verbatim" guarantee across sync. Files: `ThoughtWeaver/thoughtWeaverStore.ts`, `localCategorizer.ts`, integrations Supabase config.
- **Live Gmail/Calendar OAuth** — the multi-account Inbox UI exists but Google OAuth is "scaffolded to the credential blocker" (UI-without-a-connection asymmetry). Trade-off: needs a backend session + the Google client JSON (a standing blocker per BACKLOG).
- **Consolidate the two in-browser ML/STT stacks** (moonshine vs `@huggingface/transformers`, both ONNX-on-WASM) — spike whether one covers live transcription, dropping a dependency + ~21 MB of duplicated WASM. Pairs with plan 009.

## Findings considered and rejected (so nobody re-audits them)

- Duplicated `clamp()` — only 2 same-shape copies (below the 3+ bar); the others are different functions. Not worth a shared util on its own.
- `JSON.parse(JSON.stringify())` deepClone — only 2 trivial production sites; `structuredClone` swap is a nice-to-have, fold into a future utils pass.
- 1.2 MB `autobuild-*.log` — already matched by `.gitignore` `*.log` and **not tracked**; nothing to do but optionally delete the local file.
- RefreshCw `aria-label` / `<Section>` `aria-controls` repeats — documented intentional conventions in `CLAUDE.md`, not duplication.
- SSR-off-on-Netlify and `nebula-bg.mp4` *git size* — documented decisions (the *autoplay* cost is addressed in plan 006, a different problem).
- React 19 / React Router 7 / Vite 6 versions — all current; no upgrade needed. `d3-*` / `react-is` transitive duplicates — benign.

### Round 2 — NEW finding surfaced during execution (2026-07-02)

- **Failed-login events are never recorded in the audit log (FK gap).** Fixing the 500→401
  crash (plan 024) means `logAuditEvent()` now swallows the FK error — so a `LOGIN_FAILED`
  for the sentinel `'unknown'` user still writes NO audit row (its `user_id` has no `users`
  match). Result: brute-force / failed-login attempts leave no audit trail. Fix options: make
  `audit_log.user_id` nullable + log anonymous events with `null` user_id, or relax the FK for
  sentinel events. Candidate for a small follow-up plan (030). MED severity (security
  monitoring gap), S effort.
- **Jest open-handle leak** (024 NOTES): a "worker failed to exit gracefully" warning — a DB
  handle/timer not torn down. Non-blocking (39 tests pass, exit 0); worth a `--detectOpenHandles`
  follow-up for clean CI teardown.

### Round 2 rejections (2026-07-02)

- **Committed `.env` secrets** (flagged HIGH by 3 subagents) — REJECTED: `.env` is gitignored AND untracked in **both** repos (`git ls-files | grep .env` empty; `.gitignore` covers it). Live keys sit in local dev `.env` only; prod uses Cloud Run Secret Manager. Not in git = not exposed. (The *seed* passwords in `authService.ts` source ARE real and are plan 023.)
- **IDOR on `/api/objects`** — REJECTED: ownership is enforced server-side (`obj.ownerId === req.user.id` on GET/PUT/DELETE); user B cannot reach user A's objects. Verified by reading the routes (and pinned by plan 024's tests).
- **SQL injection in better-sqlite3** — REJECTED: all queries use prepared statements with `?` placeholders; no string concatenation. Confirmed across auth/object/audit stores.
- **Audit-route RBAC bypass** — REJECTED: non-privileged users read only their own trail; cross-user/search/global require `corporate`+. Hardened this session (`auditRoutes.ts`).
- **CORS `credentials` gap** — REJECTED as a current vuln: the app uses Bearer tokens, not cookies, so `credentials` is correctly absent. Revisit only if session storage moves to cookies.
- **scrypt default work factor (N=16384)** — noted LOW/forward-looking, not planned: acceptable today; a lazy re-hash-on-login upgrade is a nice-to-have, folded into a future auth pass.
- **LIKE audit search "injection"** — DOWNGRADED: prepared statements prevent injection; the residual is a data-minimization/recon concern gated to `corporate`+ — not worth a standalone plan now.
