# INGEST_PROGRESS.md — Scribe Ingestion + Honcho + Statute arc

Branch: `feat/scribe-ingestion-honcho` (off `feat/pm-exec-dashboard` @ `69bc37d`).
Vitest baseline at branch base: **459 passed / 56 files**. Higher = added tests (note delta).
Gate (source cycles): `tsc -b` + `vitest run` + 2× `react-router build` + PII verify + SSR smoke (`SMOKE_TEST_PORT=3458`). 6/6 = commit.

---

## Cycle log

### Cycle 1 — AUDIT + PLAN (docs-only) ✅ DONE
- Read & verified ground truth: htmlToMarkdown, dropHandler, createLocalStorageStore (dynamic-key + `.reset()`), fileExplorerStore (canonical per-user mirror), honchoDreamStore, HonchoHermesPanel (563L, NOT registered), Stella honcho/hermes tabs (INLINE — does NOT import the panel), TranscriptionHub statute path (`scanSegmentsViaLlm` + `legalShieldClient.ts` 99L), widgetRegistry/hierarchy format, `dwellium:open-widget` bus, `Docs/backend-file-explorer-routes.ts` contract shape, ThoughtWeaver dir.
- Wrote `INGEST_PLAN.md` (data flow, share-not-move Honcho approach, statute list, SSR notes, 18-cycle sequence, risks) + `INGEST_DECISIONS.md` (D-1..D-4).
- Key finding: Stella has its OWN inline honcho/hermes code → Honcho standalone is **zero-Stella-touch** (register widget that lazy-loads HonchoHermesPanel). Lowest risk.
- Docs-only cycle → no gate; `git status` only.

### Cycle 2 — Ingestion storage layer (no UI) ✅ DONE — `b5b21b2`
- NEW `src/components/Scribe/ingestion/ingestionStore.ts` — per-user `createLocalStorageStore` (dynamic-key `scribe-ingestion:<userId>`, sister to fileExplorerStore). State: `sourceFolderName` + `backupFolderName` + `lastSyncAt` + `converted[]` (ConvertedFileEntry: sourceName/destName/status/bytes/convertedAt/note). Helpers: `saveIngestion` / `setConvertedIndex(entries, syncedAt)` / `recordConverted` / `clearConvertedIndex` / `clearIngestion`. Metadata-only persistence per **D-2** — live `FileSystemDirectoryHandle` held in module-memory `ingestionHandles{source,backup}` (NOT serialized; re-pick on reload). Caller passes the clock (`syncedAt`) → deterministic tests, no fake timers.
- NEW `src/components/Scribe/ingestion/ingestionApi.ts` — typed `/api/ingest` client (registerWatch/fetchIngestStatus/convertOnBackend/fetchBackendConverted) mirroring fileExplorerApi (`API_BASE` + `getAuthHeaders` + `{success,data}` envelope, single `call<T>`). Covers ONLY what the browser can't (always-on watcher + pdf/docx server conversion); the Cycle-5 client-side path doesn't use it.
- NEW `src/test/ingestionStore.test.ts` — 15 tests: SSR-safety (getServerSnapshot), per-user isolation (Andy≠Lisa), normalize/coerce malformed data, index mutation helpers, handle-ref reset, mocked-fetch api client (incl. "Backend route not implemented" 404 → typed throw). `.reset()` in beforeEach per v2.72.1.
- SSR-safe: zero module-eval localStorage/window/fetch. Smoke-test PASS confirms.
- **GATE 6/6 GREEN:** tsc ✓ | vitest **474/57** (baseline 459/56 → +15 tests / +1 file) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (0 console errors / 0 warnings / 0 page errors).
- **Next: Cycle 3 — Backend ingest route CONTRACT (`Docs/backend-ingest-routes.ts`, docs-only).**

### Cycle 3 — Backend ingest route CONTRACT (docs-only) ✅ DONE
- NEW `Docs/backend-ingest-routes.ts` — Express route contract mirroring `backend-file-explorer-routes.ts` shape (`Router` + `authenticate` + `{ success, data }` envelope). 4 endpoints matching `ingestionApi.ts` byte-for-byte on shape:
  - `POST /api/ingest/watch` `{ sourcePath, destPath, label? }` → `{ success, data: WatchedFolder }` — register a source→dest pair with the always-on daemon (chokidar reference); idempotent on (sourcePath, destPath); persists to per-user `~/.dwellium/ingest/<userId>/watches.json` ledger.
  - `GET /api/ingest/status` → `{ success, data: IngestStatus }` — `{ watching[], lastRunAt, queueDepth }`.
  - `POST /api/ingest/convert` `{ sourcePath, destPath }` → `{ success, data: BackendConvertedFile }` — server-side pdf/docx/xlsx/pptx/rtf/odt → md. **REUSE directive:** delegate binary→html to the soffice path in `backend-docs-convert-routes.ts`, then run htmlToMarkdown (server port) — do NOT re-implement binary conversion. Currently returns `501` with a "not implemented — see backend-docs-convert-routes.ts" message (the shape the FE expects once it lands is in a comment).
  - `GET /api/ingest/converted` → `{ success, data: BackendConvertedFile[] }`.
- Scope boundary documented at the top: browser handles html/txt/md in-app (Cycles 4-5, no contract needed); this contract covers ONLY the two things a browser structurally cannot do — always-on watching + binary-format conversion — and is OUT OF SCOPE for this branch (sibling backend / Electron implements it). FE client already shipped against this exact shape (Cycle 2), degrades gracefully (typed throw) until routes exist.
- Docs-only cycle → `Docs/**` outside parity-gate paths filter; no source gate. `git status` confirms only `Docs/backend-ingest-routes.ts` new.
- **Next: Cycle 4 — Folder-picker UI in Scribe (File System Access API, event-handler-gated, SSR-safe; FULL gate).**

### Cycle 4 — Folder-picker UI in Scribe ✅ DONE — `894a532`
- NEW `src/components/Scribe/ingestion/fsAccess.ts` — minimal, SSR-safe File System Access API surface. The TS DOM lib here ships `FileSystemDirectoryHandle` WITHOUT async-iteration (`values()`) and does NOT declare `window.showDirectoryPicker`, so this module declares exactly the slice we use (`FsDirectoryHandle`/`FsFileHandle`/`FsWritableStream` incl. `createWritable` for Cycle-5 writes). `isFsAccessSupported()` (SSR-safe `typeof window` guard) + `pickDirectory(mode)` (AbortError=user-cancel→`null`, else throw). Zero module-eval `window` access.
- NEW `src/components/Scribe/ingestion/useIngestion.ts` — hook mirroring `useFileExplorer`: reads `UserContext` directly (not `useUser()`), updates `ingestionUserIdHolder.current` DURING render before `useSyncExternalStore`, exposes `supported`/`hasSource`/`hasBackup` + event-handler-gated `pickSource`/`pickBackup` (set the in-memory handle + persist folder NAME via `saveIngestion` → re-render).
- NEW `src/components/Scribe/ingestion/IngestionPanel.tsx` — source + backup picker buttons, picked-name display (with "(re-pick after reload)" when only the persisted name survives — D-2 metadata-only), Convert-now button SHELL (disabled until `hasSource && hasBackup && onConvert`; Cycle 5 passes the real `onConvert`), unsupported-browser fallback banner, `role="status"` error + status regions. Mounted in Scribe `EmptyState` (always-reachable landing surface). +CSS block (acid-lime/#333 design-system match).
- NEW `src/test/ingestionPanel.test.tsx` — 7 tests, MOCKED `window.showDirectoryPicker`, real clock (no fake timers): unsupported banner + no pickers; source pick → name + store + handle; backup pick → name + handle; AbortError cancel → silent no-op; Convert gating (disabled w/o onConvert, enabled + fires when wired + both folders); "being wired up" caption. `ingestionStore.reset()` + handle reset in beforeEach.
- SSR-safe: smoke-test PASS (0 console errors / 0 warnings / 0 page errors); all `window`/FS-API access is onClick-gated.
- **GATE 6/6 GREEN:** tsc ✓ | vitest **481/58** (Cycle-2 474/57 → +7 tests / +1 file) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (port 3458).
- **Next: Cycle 5 — Client-side convert + backup write** (enumerate source via `fsAccess` values(), html/txt→md via htmlToMarkdown, .md passthrough, write into backup handle via createWritable, update converted index; queue pdf/docx as `queued-backend`; wire the real `onConvert` into Scribe→IngestionPanel; FULL gate).

### Cycle 5 — Client-side convert + backup write ✅ DONE — `b3cc796`
- NEW `src/components/Scribe/ingestion/ingestionConvert.ts` — `convertFolder({source,backup,now,htmlToMarkdown?})` pure engine. Enumerates source handle top-level via `values()` (skips sub-dirs — flat pass; deep-walk is the backend-watcher's job per Cycle-3 contract). Per-file routing by ext: `.html/.htm` → `htmlToMarkdown` (REUSED, injectable) → `<base>.md` status `converted`; `.md/.markdown` → verbatim passthrough, original name kept; `.txt/.text/.log` → body verbatim → `<base>.md` status `converted`; everything else (pdf/docx/xlsx/…) → `queued-backend` (NOT read/written, destName null, note → `/api/ingest/convert`). Writes via `getFileHandle(name,{create:true})` + `createWritable()`. Per-file try/catch → status `error` w/ message. Caller passes the clock → deterministic, no `new Date()`/fake-timers in the engine; no window/localStorage/module-eval → SSR-safe by construction.
- `useIngestion` gains `converting` + `convertError` (local `useState`) + `convert()`: narrows `ingestionHandles.source/backup` to `FsDirectoryHandle`, runs `convertFolder` with `now: () => new Date().toISOString()` (event-handler context — safe), then `setConvertedIndex(entries, syncedAt)`. Guards on missing handles.
- `Scribe.tsx` EmptyState now calls `useIngestion()` and passes the real `onConvert={convert}` + `converting` + `convertError` into `<IngestionPanel>`. IngestionPanel itself UNCHANGED (still prop-driven) → Cycle-4's 7 tests stay green untouched. Two hook instances share the store + module handles, so hasSource/hasBackup stay consistent.
- NEW `src/test/ingestionConvert.test.ts` — 8 tests vs FAKE FS handles (in-memory source files + write-capturing backup): html→md via injected converter + filename swap; .md/.markdown passthrough (name kept); .txt→.md body; pdf/docx→queued-backend (unread/unwritten, note set); sub-dir skipped; read-failure→error; write-failure→error; empty folder→empty index. Real clock (injected `now`).
- **GATE 6/6 GREEN:** tsc ✓ | vitest **489/59** (Cycle-4 481/58 → +8 tests / +1 file) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (port 3458, 0 console errors / 0 warnings / 0 page errors).
- **Next: Cycle 6 — Honcho standalone widget** (register `honcho` in widgetRegistry.ts lazy-loading HonchoHermesPanel + pinned `dock-honcho` in hierarchy.ts group AI Tools; Stella's inline honcho tab UNTOUCHED per Cycle-1 zero-Stella-touch finding; add a widget test; FULL gate).

### Cycle 6 — Honcho standalone widget (extraction) ✅ DONE — `b892c00`
- Promoted Honcho out of being only a Stella tab into its OWN registered widget. **Zero-Stella-touch** (per Cycle-1 finding: Stella has its own INLINE honcho/hermes code and does NOT import HonchoHermesPanel — so registering a sibling widget that renders the panel leaves Stella entirely untouched; both surfaces coexist).
- `widgetRegistry.ts` — NEW `'honcho'` entry (category `ai`, icon `brain-circuit`, minWidth 600 / minHeight 400) lazy-loading the SHARED `HonchoHermesPanel` via `lazyWithReload`. Auto-flows into `WINDOW_COMPONENTS` (derived map) → Desktop/Sidebar/CommandPalette auto-discover it. Inserted between `stella-agent` and `hydra-ai` in the AI block.
- `hierarchy.ts` — NEW pinned `dock-honcho` entry in the **AI Tools** group (`{ id:'dock-honcho', label:'Honcho', icon:'brain-circuit', component:'honcho', pinned:true, group:'AI Tools' }`), placed right after `dock-stella`. (Cycle 8 will layer the "opens by default" behaviour on top of this pinned base.)
- NEW `src/test/honchoWidget.test.ts` — 6 registration-integrity tests (linkage-test style, NO panel render → no UserProvider/backend needed, since `HonchoHermesPanel` uses `useUser()` which throws without a provider): registry metadata, lazy-component object + identity with `WINDOW_COMPONENTS.honcho`, AI-category listing, Stella non-collision (distinct component, stella-agent still registered), dock metadata, dock→registry resolution. Caught + scoped back an over-broad "all docks resolve" assertion (pre-existing: `hierarchy-browser` etc. render outside the registry) to honcho-only.
- **GATE 6/6 GREEN:** tsc ✓ | vitest **495/60** (Cycle-5 489/59 → +6 tests / +1 file) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (port 3458, 0 console errors / 0 warnings / 0 page errors).
- **Next: Cycle 7 — Honcho markdown arrange/filter view** (in the Honcho widget, list converted .md files from the ingestion index / file-explorer tree with sort+filter by size/date-created-modified/name A→Z + text filter; drill-down opens a file in Scribe via `dwellium:open-widget`; add tests; FULL gate).

### Cycle 7 — Honcho markdown arrange/filter view ✅ DONE — `57a4919`
- Added a **Files tab** (`📄 Files`) to the standalone Honcho widget (`HonchoHermesPanel.tsx`) that lists the converted `.md` files recorded in the Scribe ingestion index (`ingestionStore.converted`, read via `useIngestion()` — which reads UserContext directly, so it degrades to `_anonymous` and never throws). Sort dropdown (Date / Name / Size) + a direction toggle button (↑ Asc / ↓ Desc) + a text filter input. Per-file drill-down button reuses `dispatchOpenWidget('scribe','Scribe','📝')` (from `workspaceScribe.ts` — NO duplicate bus logic) to surface Scribe via `dwellium:open-widget`.
- NEW `src/components/HonchoHermesPanel/markdownArrange.ts` — **pure** arrange/filter engine over `ConvertedFileEntry[]`: `isMarkdownFile`/`markdownFiles` (selects `converted`+`passthrough` with a destName; EXCLUDES `queued-backend`/`error` — those aren't .md artifacts), `arrangeMarkdownFiles({sortKey,sortDir,filterText})` (sort by name/size/date asc/desc via `.slice().sort()` — never mutates input; case-insensitive substring filter across source+dest names), `displayName`, `formatBytes` (B/KB/MB, guards 0/neg), `DEFAULT_ARRANGE` (date/desc/''). No window/localStorage/module-eval → SSR-safe by construction; unit-testable without rendering the panel (which needs a UserProvider).
- CSS: NEW `hhp__btn` (was referenced, undefined) + `hhp__file-list`/`hhp__file-row`/`hhp__file-open`/`hhp__file-name`/`hhp__file-meta`/`hhp__file-badge` (acid-lime hover, design-system match). a11y: `aria-label` on filter input, sort select, dir toggle, and each open button (`Open <name> in Scribe`); `aria-label` on the `<ul>`.
- Empty states: distinguishes "no converted files yet" (points to Scribe → Convert now) vs "no files match the filter".
- Zero-Stella-touch preserved: Stella renders its own inline honcho code, not HonchoHermesPanel, so this change is invisible to Stella.
- NEW `src/test/markdownArrange.test.ts` — 13 tests: md-file selection (accept/reject/exclude), sort by name asc+desc, size asc, date-desc default, no-mutate guard, filter (case-insensitive cross-name / empty / no-match), formatBytes band.
- **GATE 6/6 GREEN:** tsc ✓ | vitest **507/61** (Cycle-6 495/60 → +12 tests / +1 file) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (port 3458, 0 console errors / 0 warnings / 0 page errors; first run hit a transient port race, clean PASS on immediate re-run).
- **Next: Cycle 8 — Honcho "always-on by default" + abilities surface** (make Honcho pinned/open by default per the hierarchy `pinned:true` + default-open mechanism; surface dream/memory abilities — appendDream/deleteDream/clearDreams — in the standalone widget; document the multi-physical-screen ask as REQUIRING Electron/OS-level access (NOT possible in web) as a deferred item in INGEST_DECISIONS.md; FULL gate).

### Cycle 8 — Honcho "always-on by default" + abilities surface ✅ DONE — `09558b8`
- **Always-on by default** (D-5): Honcho was already pinned (Cycle 6). Added "opens by default" as a ONE-TIME auto-open. NEW pure helper `src/components/Shell/honchoAutoOpen.ts` (`HONCHO_AUTO_OPEN_KEY='honcho:auto-open:v1'` + `shouldAutoOpenHoncho(flag)` predicate + component/done constants; zero module-eval browser-global access → SSR-safe by construction). NEW effect in `Desktop.tsx` (inside `useEffect`, client-only): on first ready Desktop, if the flag isn't `done`, set it + `openWindow('honcho','Honcho','brain-circuit')`. Once the user closes it, it never auto-reopens (most reversible reading of "opens by default"; flip `shouldAutoOpenHoncho`→`true` for every-session reopen, documented inline). try/catch wraps localStorage for sandboxed contexts.
- **Abilities surface** (dream/memory): NEW `🌙 Dreams` tab in the standalone Honcho widget (`HonchoHermesPanel.tsx`) surfacing the per-user `honchoDreamStore` abilities — list dreams (title/text/sources/time), `+ Add Dream` form (title+text → `appendDream`), per-dream delete (`deleteDream`), `🗑️ Clear all` (`clearDreams`). Wired via `useSyncExternalStore(dreamStore.*)` + `dreamUserIdHolder.current = user?.id` updated DURING render (mirrors useIngestion / savedLayouts dynamic-key pattern). Reused `hhp__` memory-card / add-form / toolbar classes; NEW `hhp__hint` CSS. a11y labels on every new control. Zero-Stella-touch preserved (Stella renders its own inline honcho tab, not this panel).
- **Multi-screen ask DEFERRED** (D-6): "see multiple physical desktop screens" documented as REQUIRING Electron/OS-level access — browsers can't enumerate/capture other monitors or OS desktops (Window Management API exposes only screen geometry for app-window placement). Logged in INGEST_DECISIONS.md; no web-app attempt.
- NEW tests: `honchoAutoOpen.test.ts` (4 — predicate null/done/garbage + constants) + `honchoDreamStore.test.ts` (7 — empty/server-snapshot/append-prepend/newest-first/delete/clear/per-user-isolation). No fake timers (real clock per Phase-7 Finding (B)).
- **GATE 6/6 GREEN:** tsc ✓ | vitest **518/63** (Cycle-7 507/61 → +11 tests / +2 files) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (transient port race on first try → clean PASS on re-run, port 3459; 0 console errors / 0 warnings / 0 page errors).
- **Next: Cycle 9 — TranscriptionHub statute-matching improvements** (better statute extraction + dedup of matchedStatutes, surface similarity/excerpt in the UI, graceful no-LLM + failed-fetch states, clearer matched-statutes display; add/extend tests; FULL gate).

### Cycle 9 — TranscriptionHub statute-matching improvements ✅ DONE — `0166c73`
- Improved the Legal Shield statute path. Previously the LLM-adapt block produced exactly ONE matched statute per segment (`similarity` hard-coded to 1, `excerpt` = summary) even when the LLM named several O.C.G.A. sections — and `matchedStatutes` (similarity/excerpt) was never surfaced in the UI at all (only the single `statute` string showed, in a tooltip).
- NEW `src/components/TranscriptionHub/statuteMatch.ts` — **pure**, SSR-safe (no window/localStorage/module-eval/Date.now) engine, unit-testable without rendering the Hub (which needs UserProvider): `normalizeStatute` (canonical `O.C.G.A. § <section>`; named-act passthrough), `extractStatuteRefs` (ALL coded sections from free text, normalized + deduped + order-preserved; handles `44-7-30(a)` sub-section parens), `buildMatchedStatutes` (primary from `code_ref` @ sim 1, secondary summary-only @ 0.6, non-coded-authority fallback kept verbatim so citations are never dropped), `dedupMatchedStatutes` (by normalized volumeId, keep max sim, sort desc, excerpt backfill — robust against backend dupes), `formatSimilarity` (clamped whole-%), `primaryStatuteLabel` (top + "+N").
- `TranscriptionHub.tsx`: LLM-adapt path → `matchedStatutes: buildMatchedStatutes(r)`; backend path → `dedupMatchedStatutes(r.matchedStatutes)`; NEW matched-statute detail list (id + similarity% badge + excerpt, a11y `aria-label`) under the inline legal badge; NEW no-LLM hint beside the Legal Shield toggle gated on `hasActiveLlm(integrations.llm)` ("Add an LLM key in Settings → API Keys to enable statute matching"). Graceful no-LLM + failed-fetch states already handled in the scan effect (Path A LLM → Path B backend → silent).
- `TranscriptionHub.css`: `th-legal-hint` + `th-segment__legal-matches`/`-match`/`-match-id`/`-match-sim`/`-match-excerpt` (acid-lime/#d6fe51 design-system match, tabular-nums similarity badge).
- NEW `src/test/statuteMatch.test.ts` — 22 tests (normalize 5 / extract 4 / build 6 / dedup 3 / format 1 / label 3); real clock (no fake timers per Phase-7 Finding (B)). Caught a regex bug at gate (trailing `\b` after the optional `(a)` group dropped the paren) — fixed by removing the trailing word boundary.
- **GATE 6/6 GREEN:** tsc ✓ | vitest **540/64** (Cycle-8 518/63 → +22 tests / +1 file) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (transient port-3458 playwright nav flake on first try → clean PASS on re-run port 3461; 0 console errors / 0 warnings / 0 page errors).
- **Next: Cycle 10 — a11y + polish + interactivity** (consistent loading/empty/error UI across the new ingestion + Honcho surfaces, WCAG AA labels on the new controls — pickers, sort/filter, convert button — keyboard nav; FULL gate).

### Cycle 10 — a11y + polish + interactivity ✅ DONE — `9ac84d7`
- WCAG AA accessible-name + live-region polish over the surfaces this arc introduced. Audited first: IngestionPanel (Cycle 4) + the standalone Honcho widget's Dreams/Files tabs (Cycles 7-8) were ALREADY well-labelled (every picker/sort/filter/convert control had discernible text or aria-label). The remaining real gaps were in the now-standalone Honcho widget's loading region + its (pre-existing-but-now-user-facing) memory/delegate controls, plus the IngestionPanel status/error semantics.
- **IngestionPanel.tsx:** sync-status `<p>` (`ingest-status`) → polite `role="status"` live region so "Last sync: … / No conversions yet." updates are announced. Picker + conversion error `<p>` promoted from `role="status"` → assertive `role="alert"` (errors should interrupt). Doc-comment updated (this IS the Cycle-10 pass the Cycle-4 comment promised). No behavior change.
- **HonchoHermesPanel.tsx (standalone widget — zero-Stella-touch preserved; Stella renders its own inline honcho code, NOT this panel):** loading `<div>` → `role="status" aria-live="polite"`; added aria-labels to memory search input ("Search memories"), memory type-filter select ("Filter memories by type"), add-memory textarea ("Memory content") + type select ("Memory type"), memory delete icon-button ("Delete memory" — icon-only 🗑️ now has a real accessible name, not just `title`), and the Hermes delegate input ("Hermes task prompt"). All additive — no behavior/style change.
- Keyboard nav already native: every new control is a `<button>`/`<input>`/`<select>` — no custom-div widgets introduced this arc, so tab/enter/space work by default. Files-tab open buttons + sort toggle are native buttons (confirmed).
- TranscriptionHub statute surface left as-is (just polished in Cycle 9: matched-statute list already aria-labelled, no-LLM hint already added).
- NEW test: +1 assertion in `ingestionPanel.test.tsx` — sync-status carries `role="status"` + a non-abort picker failure surfaces in a `role="alert"` region (verified `pickDirectory` swallows AbortError but rethrows other errors → alert path is real).
- **GATE 6/6 GREEN:** tsc ✓ | vitest **541/64** (Cycle-9 540/64 → +1 test) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (port 3458, 0 console errors / 0 warnings / 0 page errors).
- **Next: Cycle 11 — BLOCK A sub-closure (docs-only).** Write `Scripts/autorun/INGEST_CLOSURE.md` (Block A commit SHAs cycles 1-10, what ships client-side vs deferred to `/api/ingest/*`, Honcho-standalone summary, statute-improvement summary, deferred items incl. multi-screen + non-html conversion). Do NOT touch ALL_DONE — Block B (cycles 12-18) continues. Commit. End turn.

### Cycle 11 — BLOCK A sub-closure (docs-only) ✅ DONE
- Wrote `Scripts/autorun/INGEST_CLOSURE.md` — Block A ledger: Cycle 1–10 commit SHAs (feature + log), vitest trajectory 459/56 → 541/64 (+82/+8), what ships client-side (folder picker + Convert now + Honcho Markdown arrange + honest-unavailable banner) vs deferred to `/api/ingest/*` (always-on watcher + non-html conversion), Honcho-standalone summary (D-1 SHARE, zero-Stella-touch, pinned + auto-open D-5, Files + Dreams tabs), statute-improvement summary (Cycle 9 `statuteMatch.ts` multi-statute extract/dedup/UI), 6-item deferred-carry-forward table (incl. multi-screen D-6 + non-html conversion + handle persistence D-2 + FS-API fallback D-3), conventions honored, and an explicit "Block B continues — ALL_DONE NOT touched" note.
- Docs-only cycle: only `Scripts/autorun/INGEST_CLOSURE.md` + this progress entry changed; no `qualia-shell/src/**` touched → no full gate required (per loop contract). `git status` confirms docs-only.
- **Block A (Cycles 1–11) COMPLETE.** ALL_DONE deliberately NOT touched — Block B (Cycles 12–18) is next on the same branch.
- **Next: Cycle 12 — TW reports + insights store (local).** Create `src/components/ThoughtWeaver/reportStore.ts` (per-user createLocalStorageStore: daily reports + weekly summaries + insight entries, key `thought-weaver:reports:<userId>`) + `insights.ts` helper (injectable per-user LLM dep: categorize captures / draft daily report / daily+weekly to-do gen / non-obvious cross-capture insights). Pure functions + mocked-LLM unit tests. KEEP ALL DATA LOCAL. FULL gate.

### Cycle 12 — TW reports + insights store (local) ✅ DONE
- **BLOCK B opens.** Two new local-first ThoughtWeaver modules; KEEP-ALL-DATA-LOCAL honored (per-user `createLocalStorageStore`, no backend dependency).
- NEW `src/components/ThoughtWeaver/reportStore.ts` — per-user store, single key `thought-weaver:reports:<userId>` holding one `ReportData` container `{ dailyReports[], weeklySummaries[], insights[], lastDailyReportDate, lastWeeklyReportWeek }`. Mutators: `addDailyReport` (dedup-by-date, supersede; tracks max date), `addWeeklySummary` (dedup-by-weekStart Mon-anchor), `setInsights` (wholesale replace — insights are a fresh analysis each run, not an append log), `clearReports`. `lastDailyReportDate`/`lastWeeklyReportWeek` exist to drive Cycle-13 on-open catch-up. SSR-safe (typeof-window guards mirror thoughtWeaverStore/todoStore); `.reset()` via factory; getServerSnapshot = EMPTY default.
- NEW `src/components/ThoughtWeaver/insights.ts` — **pure**, LLM-injectable (`LlmFn = (req)=>Promise<LlmResponse|null>`), zero window/localStorage/new Date inside (date context passed in). Helpers: date utils (`dayKey`/`weekStartOf` Mon-anchor UTC math/`capturesForDay`/`capturesForWeek`); `categorizeCapture` (LLM-first, loose-JSON parse w/ prose-fence extraction, clamps confidence, bad-bucket→needs_review, null on no-LLM/fail); `draftDailyReport`/`draftWeeklySummary` (LLM-first, **graceful** `heuristicDailySummary` counts/buckets fallback — never null); `generateTodoSeeds` (LLM-first imperative-task extraction validating sourceId against capture ids; `heuristicTodoSeeds` admin+action-verb fallback → `TodoSeed` for `syncTodosFromCaptures`); `surfaceInsights` (NON-OBVIOUS cross-capture patterns — genuinely LLM-dependent, returns [] without LLM / <3 captures, no fake heuristic). Reuses the exact categorize prompt shape from ThoughtWeaver.tsx:283 (Cycle 13 will adopt the shared helper).
- NEW tests: `reportStore.test.ts` (11 — empty/server-snapshot/add-dedup-supersede/max-date/weekly-dedup/setInsights-replace/per-user-persist+isolation/clear) + `insights.test.ts` (19 — date helpers / categorize happy+prose+clamp+null / daily+weekly report LLM+heuristic+empty / todo seeds heuristic+LLM-parse+fallback+empty / surfaceInsights no-LLM+<3+parse+junk). Stub `LlmFn` (canned text / null / throw); real clock (timestamps injected).
- **GATE 6/6 GREEN:** tsc ✓ | vitest **571/66** (Cycle-11 541/64 → +30 tests / +2 files) ✓ | react-router build ✓ | seeds=false build ✓ | PII clean (51 files) ✓ | SSR smoke PASS (port 3458; assertions PASS, 0 console errors / 0 hydration warnings).
- **Next: Cycle 13 — TW reports/insights UI + on-open catch-up + "generate now".** Wire reportStore+insights into ThoughtWeaver.tsx (Reports/Insights view, "Generate now" button, on-open catch-up computing "due" from lastDailyReportDate/lastWeeklyReportWeek). Graceful no-LLM/empty states. Real-clock tests (NOT fake timers). FULL gate.
