# Plan 053 — Every plan-047 tool to 100 % (Ilya, 2026-08-23: "get everything to 100 percent")

## Definition of 100 % (applies to all ten; the builder of each tool owns closing every ❌/🟡 in its gap report)

1. **Every headline upstream feature is reachable from inside Dwellium.** In-window when the vendor allows framing (or when we self-host behind our own proxy and can set `frame-ancestors`); otherwise one click via **Open ↗** with a *deep link into the exact screen* (never a bare homepage). The in-Dwellium widget must cover the daily workflow natively (create / list / act / status) against the vendor's REAL API — fetched spec or docs, snapshotted as a test fixture with a drift-guard test (see `~/dwellium-backend/.../tests/esignRoutes.test.ts` "Documenso v2 OpenAPI drift guard"). No endpoint may be written from memory or description.
2. **Customized around Andy and his work.** Andy runs Georgia multifamily properties (AstraStrata; see `qualia-shell/src/components/StrataDashboard/modules/LeasingModule.tsx` guest-card fixtures — Woodland Parc Townhomes, Riverwood Club Apartments; `StrataDashboard/fixtures/appfolioDerived/*`; `qualia-shell/src/data/araStarterPrompts.ts`; `plans/047-oss-tools-integration-and-onboarding.md` per-tool sections). Each tool ships Andy-specific defaults as code: templates, seeds, naming, bridges from Strata data (properties / units / residents / vendors / leases / work orders) into the tool, and from the tool back into Strata/Task Board/Inbox where the plan names it. Generic wrappers are not 100 %.
3. **Honest states, never a blank or a crash.** Unconfigured → the exact account/env step; unreachable → retry; configured → the real thing. `resolveToolStatus` must flip to Ready automatically once env exists.
4. **Verified, not described.** Backend: jest green + drift fixture. Frontend: vitest for every state + `tsc -b` + eslint + `NETLIFY=1 npx react-router build`. Final report lists, per upstream feature, where it is now reachable (in-widget / Open ↗ deep link / native client) — the builder's own gap table updated to 100 %.
5. **The only things left to a human are account creations and pasting env values** — the report names them exactly (service, plan, what to click, which env var), nothing else.

## Ownership and conflict rules
- Each builder edits ONLY its own component dir, its own `tools/<tool>/` dir, its own tests (`src/test/<tool>*.test.tsx`) and — if unavoidable — a minimal additive change to `src/registry/widgetRegistry.ts`, `src/data/toolsHub.ts`, `src/lib/hierarchy.ts`, `src/data/iconMap.ts` (report every shared-file line you touched). Do not touch Auth/, UserContext, login, passwords, or any other tool's files.
- Backend: builders cannot run git in `~/dwellium-backend`. Copy the app dir to scratch (`rsync -a --exclude node_modules --exclude .git --exclude data --exclude dist`), symlink node_modules, `git init` + baseline commit, build, test, then `git diff --cached --binary > plans/053-<tool>-backend.patch` (paths `a/src/...`). The coordinator applies with `cd ~/dwellium-backend && git apply --directory=ai-dashboard369-file-manager/ <patch>`. Keep backend patches additive (new route files; one `app.use` line in `app.ts`; one line per env in `deploy/cloud-run.sh` if a secret is needed).
- No new runtime dependencies without a one-line justification; AGPL tools stay unmodified (customisation lives in Dwellium); zero new paid services — if a feature needs a paid tier, say so in the report instead of silently skipping.
- Commit on your worktree branch in small commits; never push; final message = the report.

## Vendor facts already verified (2026-08-23) — do not re-derive
- Documenso v2 OpenAPI: `https://app.documenso.com/api/v2/openapi.json` (89 paths; `Authorization: <api key>`; `POST /template/use`, `POST /envelope/create` multipart, `POST /envelope/distribute`, `GET /envelope/{id}`, `GET /document/{id}/download?version=signed`). Free plan: 5 docs/month; API access may start at Individual $25/mo.
- listmonk: image `listmonk/listmonk`, API docs https://listmonk.app/docs/apis/apis/ (`Authorization: token user:token`). Dub: `https://api.dub.co` Bearer; pricing page shows no free plan. cal.com booking pages embed (no X-Frame-Options); API keys may be paid tier. Penpot cloud sends `X-Frame-Options: SAMEORIGIN` (launcher only unless self-hosted). FluidVoice: `brew install --cask fluidvoice` (v1.6.9), local API `127.0.0.1:47733`. Immich: `ghcr.io/immich-app/immich-server:release`. RustDesk: `rustdesk/rustdesk-server` (hbbs/hbbr), clients v1.4.9.
