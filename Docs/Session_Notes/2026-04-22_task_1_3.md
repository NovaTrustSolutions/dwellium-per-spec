# Session Notes — 2026-04-22 — Task 1.3 (Properties Schema)

**Branch:** `phase-1/task-1-3-properties-schema`
**Head at close:** `df05237`
**Prior session:** the previous Claude Code attempt pushed an empty branch (zero commits ahead of main). This session is a full Task 1.3 redo from scratch.

## Summary

- Adds 4 interfaces to `packages/types/index.ts`: `PurchaseHistory`, `LateFeePolicy`, `MaintenanceConfig`, `FixedAsset`. All referenced from `Property` as optional fields (additive, back-compat preserved).
- Adds a 5th optional field to `Property` — `parcelNumber?: string` — per Ilya's Option A ack after the plan's "5 optional fields" wording was flagged ambiguous.
- Re-exports the 4 interfaces from `strataTypes.ts`.
- Extends `PropertiesModule.tsx` with 4 collapsibles in the plan-mandated order (Purchase History → Late Fee Policy → Maintenance Config → Fixed Assets). Each wrapped in the shared `<ErrorBoundary>` with a section-specific fallback; each emits a Sentry breadcrumb on expand/collapse (GR-13) via the existing `Sentry` import (no-op when DSN unset — matches the Task 1.1 pattern in `ResidentsModule`).
- New `qualia-shell/src/components/StrataDashboard/modules/__properties/FixedAssetsTable.tsx` — typed tabular render of `FixedAsset[]` extracted from the inline JSX in `PropertiesModule.tsx`.
- Augments `qualia-shell/public/data/properties.json` entry at UUID `e4b440e9-5062-4da1-ae25-818dffab8b3b` (128 Buena Vista) with purchase history (`amount: 2270000`), late fee policy, maintenance config, 4 fixed assets, and the parcel number. No other seed entry modified (asserted in the contract test).
- Contract test: `qualia-shell/src/test/appfolioParity/properties.test.ts` — 4 `it` blocks asserting the shape at the API seam + the seed-on-disk + parcel + "no drift on other entries" guard.

## Files changed

```
 packages/types/index.ts                            |  52 +++++
 qualia-shell/public/data/properties.json           |  32 +++
 .../StrataDashboard/modules/PropertiesModule.tsx   | 215 ++++++++++++++-------
 .../modules/__properties/FixedAssetsTable.tsx      |  56 ++++++
 .../src/components/StrataDashboard/strataTypes.ts  |   4 +
 .../src/test/appfolioParity/properties.test.ts     |  92 ++++++++-
 6 files changed, 371 insertions(+), 80 deletions(-)
```

## Local gate results

- `tsc -b`: **clean** (exit 0, no output)
- `vitest run --reporter=dot`: **96/96** (26 files)
- `vite build` (default): green (built in 5.16s)
- `vite build` (VITE_APPFOLIO_SEEDS=true): green (built in 5.13s)
- `vite build` (VITE_APPFOLIO_SEEDS=false): green (built in 5.12s)
- PII strict scope: **0 leaks across 44 files**

## Id note

The plan's Task 1.3 contract-test description uses the placeholder id `appfolio-18`. No property with that id exists in `properties.json`; the real 128 Buena Vista entry was already present at UUID `e4b440e9-5062-4da1-ae25-818dffab8b3b` from the appfolio_csv import. Task 1.3 extends that entry in place rather than introducing a duplicate. The contract test asserts against the real UUID.

## Deviations

1. **5th `Property` field — `parcelNumber?: string`** (Option A, ack'd). The plan said "5 optional fields" but named only 4 interfaces. Ilya picked Option A: `parcelNumber` (primitive, no PII, natural property-admin field). Seeded with `"22/28/15/23310/011/0160"` — the DoR's "Pinellas County Parcel" prose prefix was dropped per Ilya's instruction; only the parcel id string stored.
2. **PII redaction on seller.** The DoR's free-text description names prior owners "Dolan & Brenda Ricketts" at a real street address. Seeded `seller: "Prior owner (redacted per GR-6)"`; address omitted entirely; "Wolinka & Wolinka Title Ins Agency" (business entity, not PII) kept.
3. **Legacy metadata.fixedAssets / metadata.lateFeePolicy coexistence.** Those pre-existing untyped fields remain on the entry — not removed — so older consumers keep working. The module prefers the new typed `selected.*` fields when present and transparently falls back to `pm.*` otherwise.
4. **No new UI for `parcelNumber`.** Scope was "add 4 collapsibles"; `parcelNumber` is a primitive, not a section. It's available on the typed `Property` and covered by the contract test. A future PR can surface it in the existing Property Information panel if/when someone asks.
5. **No new commit for the placeholder test rename.** Unlike Task 1.2 (which needed a `.ts → .tsx` rename), the properties test stays `.ts` — it doesn't render any JSX, it's a pure data-assertion test.

## Guardrails touched

- GR-1 additive-only ✅ (all new Property fields `?:`)
- GR-3 seed parity ✅ (only `e4b440e9…` touched; contract test guards against drift on the other 35 entries)
- GR-6 PII clean ✅ (strict-scope 0 leaks; redaction applied at the seller + prior-owner-address seam)
- GR-8 ErrorBoundary on all 4 new sections ✅
- GR-13 Sentry breadcrumb on expand/collapse ✅ (safe no-op when DSN unset)
- GR-14 dual-mode vite green ✅ (both seeds=true and seeds=false builds succeeded)

## Rollback

`git revert <merge-sha>` — safe. All type additions optional; UI changes are additive (new Purchase History section + wrap-only modifications on existing sections); seed adds JSON keys legacy consumers ignore.

## Handoff

PR open against `main`. CI dispatched via `gh workflow run` (parity gate). Ilya merges manually after review — this session does NOT merge.
