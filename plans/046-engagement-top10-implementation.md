# Plan 046: Make Dwellium friendlier — and something users itch to open (top-10 engagement items)

> **Executor instructions**: six clusters, each on its own branch `ux/046-<slug>`, drafted by six agents against `main` @ `5e78ca8` (2026-08-19) and cross-checked by a critic pass. Every step names the file:line to touch and a `Verify` that must be run and pasted before marking done. Frontend repo `~/Downloads/Dwellium -Per Spec`, app in `qualia-shell/`; backend repo `~/dwellium-backend/ai-dashboard369-file-manager` (Cluster B + A4 only). Build with `npx react-router build` (never `vite build`); gate per branch = `cd qualia-shell && npx tsc -b && npx vitest run && NETLIFY=1 npx react-router build` (+ `npx eslint <files>`); backend gate = `OPENAI_API_KEY=sk-dummy npx jest --forceExit`. **No push/merge without Ilya's go. Never DELETE/TRUNCATE; One Save is upsert-only.**
>
> **Drift check (run first, per cluster)**: `git log --oneline -3 -- <files listed in your cluster>` and re-read the anchors; re-anchor by identifier/grep, never by line number. **Plan 045 is already merged** (`0a80102` ⊂ `5e78ca8`) — every "if 045-X is mid-flight" sentence inside a cluster section is dead; follow the ownership table in *Critic notes* instead.

## Status

- **Source review**: the top-10 list delivered 2026-08-19 (this plan), built on [`044-ui-review-2026-08-16.md`](044-ui-review-2026-08-16.md) + [`045-ui-improvements-implementation.md`](045-ui-improvements-implementation.md) (merged).
- **Priority / effort**: S2, S1 are the quick wins (~1 day each); D, F, B are M; A is L (streaming + proactive ARA). Suggested order for most value per hour: **S2 → S1 → D → F → B → A** (also the merge order — see Critic notes).
- **Top-10 → cluster map**: 1 first-run → **F1–F2**; 2 ARA speaks first → **A1–A2**; 3 streaming → **A3–A4**; 4 daily loop (server brief + notifications + badge) → **B**; 5 one-click add-a-key → **S1a–S1c**; 6 widget descriptions (no renames) → **S2**; 7 demo mode + Labs → **D**; 8 ⌘K pill + `?` sheet → **S2**; 9 saved/synced pill → **S1d–S1e**; 10 one-screen front door → **F3**.
- **Decision gates for Ilya** (collected at the end of this file): 13 — defaults are stated for each; executors proceed with the default unless Ilya answers.

---

## Cluster S2 — Discoverability: widget descriptions + ⌘K/shortcuts  (branch `ux/046-s2-discover`; files: `registry/widgetRegistry.ts`, `CommandPalette/CommandPalette.tsx`, `Sidebar/widgetSearch.ts`, `Window/Window.tsx`, `Shell/AdminShell.tsx`, NEW `Shell/CommandPill.tsx(.css)`, NEW `Shell/ShortcutSheet.tsx(.css)`, `StrataDashboard/GlobalSearch.tsx`, `lib/dwelliumCommands.ts`, minimal `Sidebar/Sidebar.tsx`; tests: `test/registryWalker.test.tsx`, `test/GlobalSearch.test.tsx`, `test/dwelliumCommands.test.ts`, NEW `test/discoverability.046.test.tsx`)

> Drift check first: `git log --oneline -3 -- qualia-shell/src/registry/widgetRegistry.ts qualia-shell/src/components/CommandPalette/CommandPalette.tsx qualia-shell/src/components/Sidebar/Sidebar.tsx qualia-shell/src/components/Shell/AdminShell.tsx` — anchors below are `main` @ `5e78ca8` (2026-08-19). Note: plan-045 Cluster A already landed on main (`a72a120`), so Sidebar.tsx line numbers here are post-045-A. NO widget renames anywhere in this cluster (locked).

### S2-6. One-line description per widget, surfaced in ⌘K / sidebar hover / window title — M

- `src/registry/widgetRegistry.ts:23-46` `WidgetRegistration`: add `/** One plain-English line: what it does for a property manager. Shown in ⌘K rows, sidebar hover, window-title hover. */ description?: string;` after `label`. Then add `description:` to **all 55** entries (`grep -c "^    '[a-z0-9-]*': {" = 55`, matches `registryWalker.test.tsx:43` guard). Exact strings (≤ 95 chars; written from each component's header comment — cite in commit if you change one):

| id | description |
|---|---|
| audit-log | Who signed in, when, from where, and what each account worked on (Andy only). |
| strata-dashboard | Day-to-day property operations: residents, work orders, money, compliance — your main desk. |
| astra-dashboard | Executive view: portfolio heatmap, watchdog list, financial snapshots, compliance calendar. |
| wiki | AI-compiled reference pages for each domain, project and thread, with source citations. |
| synthesis | Ask a question across your documents, keep the answer, and build on it with a second pass. |
| foundry | Intake for new documents: capture, AI triage, review, then admit to your library. |
| knowledge-graph | Interactive map of how your files, projects and agents connect. |
| memory-graph-rag | Long-term memory for AI: stores facts from your documents and answers with sources. |
| builder-agents | Three AI helpers that draft data schemas, requirement docs and gap analyses. |
| hive | Control room for every AI agent: status, last action, manual trigger, cost by provider. |
| system-health | Which AI features are connected, which are limited, and a button to fix each one. |
| content-search | Full-text search across notes, syntheses, wiki pages, intake items and file names. |
| autonomous-runs | Catalog of long-running automated jobs with the exact command to launch each. |
| universal-shell | Four-column workbench: filing cabinet, scratch pad, canvas and orchestrator side by side. |
| inbox | Email triage: sort signal from noise, approve, and route messages to the right place. |
| api-keys | Your own AI provider keys and which model is active (stored privately, write-only). |
| inbox-zero | Older Inbox Zero entry kept for saved layouts — use Inbox Zero instead. |
| tasks | Your to-do list with urgency, status, AI ranking and a Kanban view. |
| trello-board | Your Trello boards and cards, live inside Dwellium. |
| task-board | Local Kanban: drag cards between columns, bulk-move, full undo history. |
| tag-file | Everything you've tagged anywhere in the app, grouped by tag. |
| home-upkeep-ai | Tracks building systems against their lifespans and flags inspections and wear early. |
| automation-hub | Recurring workflow automations with schedules, launch buttons and an audit log. |
| tenant-portal-mgmt | What residents see — directory, maintenance, payments, messages, lease alerts — and its admin. |
| georgia-code | Search the Official Code of Georgia (OCGA) by meaning, not just keywords. |
| agent-lab | Give a goal to a team of AI specialists; they split the work, verify it and merge a result. |
| ara-console | Your main AI assistant: chat, voice, files and actions across Dwellium. |
| meeting | ARA joins or listens to a meeting, transcribes live and coaches you during it. |
| stella-agent | Personal AI assistant with its own tools, memory and task follow-through. |
| honcho | The AI's always-on memory: what it remembers about you, searchable and editable. |
| hermes | The AI agent roster: a persona card for each specialist and what it has learned. |
| hydra-ai | Ask one question, see every configured AI model answer side by side. |
| thought-weaver | Drop raw thoughts — meetings, ideas, tasks — and AI sorts them into action items. |
| notebooklm-context | Link your Google NotebookLM notebooks as AI context (manual — Google has no API). |
| two-brains | Shared board for two people: notes, reactions, screen share and an audit log. |
| transcription | Record or upload audio, transcribe it, identify speakers and search what was said. |
| fact-check-log | Log of claims checked by AI, with verdicts and evidence. |
| cognitive-harness | Animated map of the AI stack's parts (retrieval, memory, tools, routing) with live status. |
| file-manager | Same as File Explorer — kept so older layouts still open. |
| doc-viewer | Read and mark up uploaded or generated documents. |
| pdf-gear | PDF toolkit in the browser: merge, split, compress, convert, fill and sign. |
| notepad | Quick Markdown notes; type @ to link a task or project. |
| mission-control | Mid-term goals with AI-drafted plans: the agent's actions, your actions, open questions. |
| artifact-gallery | Everything your AI agents produced — replies, drafts, images — browsable and pinnable. |
| ai-spend | Today's AI calls and cost, a 14-day trend, by provider, plus plan advice. |
| connections | Status of every integration (AI keys, Google, database, backend) and every memory store. |
| template-generator | Fill DOCX/HTML templates with variables and export PDFs — leases, notices, letters. |
| scribe | Markdown writing desk: tabs, AI redlines, comments, versions and interactive docs. |
| file-explorer | Browse, rename, create and drag files in your Dwellium file tree. |
| workspace | Browse by Domain → Project → Thread, with status and stage per thread. |
| terminal | Command line and CLI tools (hidden by default; re-add from the widget gallery). |
| cloud-browser | A web browser rendered on the server, for sites that refuse to load in an iframe. |
| control-panel | Appearance, layout and system settings for the shell. |
| time-travel | Version history for any saved object: diff versions and restore without losing anything. |
| holocron-library | Animated gallery of the eight holocrons — click one to read its lore. |

- **⌘K rows** — `CommandPalette.tsx:797` `subtitle: match.item.group || 'Widget'` → `subtitle: getWidgetMeta(match.item.component)?.description ?? match.item.group ?? 'Widget'` (import `getWidgetMeta` from `../../registry/widgetRegistry` — exists at `widgetRegistry.ts:614`). The subtitle span already renders at `CommandPalette.tsx:1074`; no CSS change.
- **⌘K search by description** — `Sidebar/widgetSearch.ts:236` `summaryTokens = tokenize(profile?.summary || '')` → `tokenize([profile?.summary, getWidgetMeta(item.component)?.description].filter(Boolean).join(' '))` so "what merges pdfs" finds PDF Gear. One line; the 22 hand-written `summary` profiles stay.
- **Sidebar hover (minimal, 045-A file)** — `Sidebar.tsx:648` `title={item.label}` → `title={withDesc(item)}`; `Sidebar.tsx:849` `title={\`${item.label} (Middle-click to close)\`}` → `title={\`${withDesc(item)} (Middle-click to close)\`}`; `Sidebar.tsx:1151` gallery card `title={it.label}` → `title={withDesc(it)}`. Add one local helper at top of Sidebar.tsx: `const withDesc = (i: { label: string; component: string }) => { const d = getWidgetMeta(i.component)?.description; return d ? \`${i.label} — ${d}\` : i.label; };`. Three `title=` edits + one helper; nothing else in Sidebar.tsx.
- **Window chrome** — `Window/Window.tsx:337` `<span className="window__title">{state.title}</span>` → add `title={getWidgetMeta(state.component)?.description}` (native tooltip; `WindowState.component` exists, `data/types.ts:26`). Skip the WidgetLoader empty-state line (`Desktop.tsx:41-55` is 045-B; tooltip on the titlebar covers "what is this window" for free).
- **Why this shape**: one optional field on the existing single source of truth (`widgetRegistry.ts:1-14` says Desktop/Sidebar/CommandPalette auto-discover from here); every surface reads it via the existing `getWidgetMeta`; no new store, no renames.
- **Test**: extend `src/test/registryWalker.test.tsx` `describe('WIDGET_REGISTRY integrity')` with `it('every widget has a one-line description', () => { for (const r of Object.values(WIDGET_REGISTRY)) { expect(r.description, r.id).toBeTruthy(); expect(r.description!.length, r.id).toBeLessThanOrEqual(100); expect(r.description!, r.id).not.toMatch(/\n/); } })`. New `src/test/discoverability.046.test.tsx`: render `CommandPalette` with the `useWindows` mock pattern from `sidebar.045.test.tsx:15-30`, open it (dispatch `keydown` metaKey+k on window), assert a widget row's subtitle equals its registry description.
- **Verify**: `cd qualia-shell && npx vitest run src/test/registryWalker.test.tsx src/test/discoverability.046.test.tsx`; browser: ⌘K → rows show the one-liner under the name; hover "Foundry" in the sidebar → tooltip "Foundry — Intake for new documents…"; hover a window title → same line.
- **Overlaps with 045**: A (`Sidebar.tsx` — already merged as `a72a120`; three `title=` lines + one helper, no structural change). B (`Desktop.tsx`) — deliberately **not touched**.

### S2-8. ⌘K and shortcuts are discoverable: pill, "?" sheet, de-dup ⌘K, footer hint — M

- **(a) Persistent pill** — NEW `src/components/Shell/CommandPill.tsx` (+ `CommandPill.css`): `<button type="button" className="cmd-pill" aria-label="Search or ask anything (⌘K)" onClick={() => window.dispatchEvent(new CustomEvent('dwellium:open-palette'))}><Search size={13}/> Search or ask… <kbd>⌘K</kbd></button>`. CSS: `position: fixed; top: 8px; left: 50%; transform: translateX(-50%); z-index: 9980` (same tier as the ThreadSwitcher pill, `Workspace/ThreadSwitcher.css:4-9`; below SystemHealth/MorningBrief banners at 98000 and the palette at 5000 — check: palette `CommandPalette.css:4` is 5000, so set the pill to **4900** so the palette scrim covers it); pill + kbd styling copied from `Shell/Desktop.css:409-432` (`.desktop__empty-kbd`) minus the fade-in animation; `@media (max-width: 600px) { .cmd-pill { display: none } }` inside CommandPill.css (not `responsive.css` — 045-E). Mount in `Shell/AdminShell.tsx:159` right after `<CommandPalette />`: `<CommandPill />`. There is no top bar in the shell (`AdminShell.tsx:156-164` is Sidebar/Desktop/palette/FAB/banners); top-center fixed is the existing banner convention and only overlaps the empty drag area of a maximized window's titlebar (title is left-aligned, `Window.tsx:335-337`).
- Palette opener event — `CommandPalette.tsx:630-648` keydown effect: add `const onOpenEvt = () => openPalette(); window.addEventListener('dwellium:open-palette', onOpenEvt);` + matching remove. Name mirrors `dwellium:open-tag` (`Shell/TagHotkey.tsx:38`) and `dwellium:open-widget` (`CommandPalette.tsx:897`).
- Leave `Desktop.tsx:1547` ("Press ⌘K to open anything") as-is — 045-B file; the pill supersedes it (note for 045-B executor: safe to delete).
- **(b) "?" ShortcutSheet** — NEW `src/components/Shell/ShortcutSheet.tsx` (+ `.css`, scrim/card classes copied from `Shell/TagHotkey.css:1-11` `.tagdlg__scrim`, z-index 200000). Rows (only shortcuts that are live in the admin shell — verified handlers): `⌘K` Search or open anything (`CommandPalette.tsx:632`) · `?` This sheet · `⌘W` Close the front window (`AdminShell.tsx:137`) · `Esc` Close the front window when not typing (`Desktop.tsx:925-943`) · `⌘T` Tag the selected text (`TagHotkey.tsx:24`) · `⌘⇧2` Open Two Brains (`AdminShell.tsx:146`) · `⌘⇧F` Search Strata — when Strata is open (after (c)) · `⌘Tab` / `⌘⇧Tab` Cycle Holocron OS tabs — when Holocron is open (`HalocronOS.tsx:325`) · `↑↓ Enter Esc` inside ⌘K. Do **not** list ⌘J/⌘G: `OpenJarvis` (⌘J, `OpenJarvis.tsx:653`) is mounted only for `role === 'tenant'` (`App.tsx:103-106`), and `Antigravity.tsx:190` ⌘G is unmounted. Keyboard: two `window` listeners — bubble-phase `keydown`: `if (e.key === '?' && !e.metaKey && !e.ctrlKey && !e.altKey && !e.defaultPrevented && !isField(e.target)) toggle()` (`isField` = same 1-liner as `IDocEditor.tsx:84`; `defaultPrevented` defers to IDocEditor's capture-phase `?` at `IDocEditor.tsx:554` so the editor keeps its own sheet); capture-phase `keydown` when open: `Escape → close(); e.stopPropagation()` so `Desktop.tsx:928` does not also close the front window (capture-on-window precedent: `TagHotkey.tsx:38`). Also open on `dwellium:open-shortcuts` CustomEvent. Mount in `AdminShell.tsx:159` next to the pill. Add a "?" ghost button at the right end of the pill (`aria-label="Keyboard shortcuts (?)"`) so mouse users find it.
- ⌘K lists the sheet — `lib/dwelliumCommands.ts` `parseSingle` (~`:245`, before the theme rules): `if (/^(keyboard )?shortcuts?$|^hotkeys?$|^show shortcuts$/.test(l)) return { label: 'Keyboard shortcuts', run: () => window.dispatchEvent(new CustomEvent('dwellium:open-shortcuts')) };` — surfaces as the existing score-1000 command row (`CommandPalette.tsx:813-815`), zero palette changes.
- **(c) De-duplicate ⌘K** — `StrataDashboard/GlobalSearch.tsx:112` `if ((e.metaKey || e.ctrlKey) && e.key === 'k')` → `if ((e.metaKey || e.ctrlKey) && e.shiftKey && e.key.toLowerCase() === 'f')`; `:188` placeholder `"Search… ⌘K"` → `"Search Strata… ⌘⇧F"`; header comment `:13` updated. **Decision: ⌘⇧F, not focus-scoping** — both handlers are `window` listeners (`CommandPalette.tsx:646`, `GlobalSearch.tsx:114`) and `stopPropagation` does not stop sibling listeners on the same node, so today ⌘K with Strata open opens the palette *and* focuses Strata's input underneath; scoping to "Strata focused" would still double-fire. ⌘⇧F is free app-wide (only `IDocEditor.tsx:552` binds mod+F, and only inside its own root) and matches editors' "find in project". Update `src/test/GlobalSearch.test.tsx:54,68,88,101,122` placeholder strings to `'Search Strata… ⌘⇧F'` and rename the `:52` test to `'renders the search input with ⌘⇧F placeholder'`.
- **(d) Palette footer** — `CommandPalette.tsx:1096-1098`: prepend `<span><kbd>⌘K</kbd> Toggle</span>` and append `<span><kbd>?</kbd> Shortcuts</span>` (existing `kbd` style `CommandPalette.css:375`). Keep ⌘ glyph hardcoded — app convention (`Desktop.tsx:1547`, `OpenJarvis.tsx:1105`); all handlers already accept `ctrlKey`. Skipped: an `isMac()` helper — none exists (`grep navigator.platform` = 0 hits); add when a Windows user asks.
- **Why this shape**: two tiny new components mounted from `AdminShell` (not a 045 file) + one CustomEvent each; palette, Strata, Desktop, Sidebar untouched except the 2-line opener and the 2-line ⌘⇧F rebind.
- **Test**: `src/test/discoverability.046.test.tsx` (same file as S2-6): (1) render `CommandPill` → click → `dwellium:open-palette` dispatched (spy on `window.dispatchEvent`); (2) render `ShortcutSheet` → `fireEvent.keyDown(window, { key: '?' })` → `getByRole('dialog', { name: /keyboard shortcuts/i })` visible and lists `⌘K`, `⌘W`, `⌘T`, `⌘⇧2`, `Esc`; `keyDown` Escape → closed; typing `?` while an `<input>` is focused → stays closed. Extend `src/test/dwelliumCommands.test.ts` with `it('parses "shortcuts" → Keyboard shortcuts', …)`. `GlobalSearch.test.tsx` as above; add `fireEvent.keyDown(window, { key: 'f', metaKey: true, shiftKey: true })` → input has focus, and `{ key: 'k', metaKey: true }` → does **not** focus.
- **Verify**: `cd qualia-shell && npx tsc -b && npx vitest run src/test/discoverability.046.test.tsx src/test/GlobalSearch.test.tsx src/test/dwelliumCommands.test.ts src/test/registryWalker.test.tsx && npx eslint src/components/Shell/CommandPill.tsx src/components/Shell/ShortcutSheet.tsx src/components/CommandPalette/CommandPalette.tsx src/components/StrataDashboard/GlobalSearch.tsx src/lib/dwelliumCommands.ts src/registry/widgetRegistry.ts && NETLIFY=1 npx react-router build`. Browser: fresh login → pill visible top-center; click → palette; `?` on the desktop → sheet; `?` inside ARA's composer → nothing; Esc closes the sheet and the front window is still open; open Strata → ⌘K opens only the palette, ⌘⇧F focuses Strata search; palette footer shows ⌘K and ?.
- **Overlaps with 045**: none for new files / AdminShell / GlobalSearch / dwelliumCommands / Window.tsx. B (`Desktop.tsx:1547`) — read-only; E (`responsive.css`) — avoided by keeping the pill's media query in `CommandPill.css`.

### STOP conditions
- If `registryWalker.test.tsx:43` count ≠ 55 on your branch, a widget was added/removed since `5e78ca8` — add/remove its description row and stop to sync this table before merging.
- If `Sidebar.tsx:648/849/1151` have moved (045-A follow-ups), re-anchor by grepping `title={item.label}` / `Middle-click to close` / `widget-gallery__label`; do not restructure anything else in Sidebar.tsx.
- Do not touch `Desktop.tsx`, `LayoutContext.tsx`, `styles/global.css`, `styles/responsive.css` in this branch (045-B/C/E ownership).
- No push/merge without Ilya's go; never `vite build`.

### Ilya decision gates
- none — proceed. (⌘⇧F for Strata search and the 55 one-liners are proposed as final; if any wording should change, it is a string edit in `widgetRegistry.ts`, not a gate.)

---

## Cluster S1 — AI readiness + saved/synced feedback  (branch `ux/046-s1-ai-ready`; files: src/hooks/useAIAvailability.ts, src/components/Shell/AIDegradedState.tsx, HydraAI/HydraSplit.tsx, AstraDashboard/AstraDashboard.tsx, BuilderAgents/BuilderAgents.tsx, Synthesis/Synthesis.tsx, HonchoHermesPanel/HonchoHermesPanel.tsx, Wiki/Wiki.tsx, TranscriptionHub/TranscriptionHub.tsx, ARAConsole/ARAConsole.tsx [045-D, 3 lines], src/lib/oneSaveStore.ts, NEW src/components/Shell/SyncStatusPill.tsx + .css, src/components/Shell/AdminShell.tsx, tests)

Facts verified on main @ 5e78ca8: `AIDegradedState` lives at **src/components/Shell/AIDegradedState.tsx** (not `common/`); its only importer is src/test/aiAvailability.test.tsx:14. `useAIAvailability.configure()` emits `openWidgetBus.emit({ widgetId: 'control-panel' })` (useAIAvailability.ts:46-48) — that opens the whole 721-line Control Panel, not the keys. A dedicated `api-keys` widget already exists (registry/widgetRegistry.ts:223-231 → ApiKeysWidget.tsx mounting `<ApiKeysPanel/>`), and WindowContext.tsx:535-542 opens any `dwellium:open-widget` payload via `openWindow(widgetId, label, icon)` with no permission gate. `callLlm()` (lib/llmClient.ts:54-80, dispatchLlm 85-109) has **no backend fallback** — it returns null without a personal key — so the hook's `'backend-only'` verdict (canCall:true, CTA hidden at AIDegradedState.tsx:46) is wrong for every widget below that calls `callLlm` directly. No UI anywhere reads sync state: oneSaveStore.ts:94-130 debounces 800 ms, retries 3×, then only `backendStatusStore.markOffline('One Save write failed')` (L128) and the write is dropped (migrate() L152-156 won't re-push an existing object).

### S1a. `configure()` opens API Keys directly; `AIDegradedState` learns "needs a personal key" — S
- useAIAvailability.ts:46-48 → `openWidgetBus.emit({ widgetId: 'api-keys', label: 'API Keys' })` (OpenWidgetPayload has `widgetId`+`label?` — busChannels.ts:23-26). Update the doc comment at L18/L42 to say "Control Panel → API Keys window".
- AIDegradedState.tsx:18-24 add two optional props: `needsKey?: boolean` (this surface calls `callLlm` directly, so `'backend-only'` must be treated as degraded) and `reason?: string` (override text). L34: `if (availability.status === 'ready' || (availability.status === 'backend-only' && !needsKey)) return null;`. L41: `{reason ?? (needsKey && availability.status === 'backend-only' ? 'Needs your own AI key — add one to run this.' : availability.reason)}`. L46: show the CTA when `availability.status !== 'backend-only' || needsKey`. L48 default label `'Open API Keys'`.
- Why this shape: one prop flips the existing component to the truthful verdict for direct-`callLlm` widgets; no new component, no new status in the hook (keeps aiAvailability.test.tsx:69-75 green).
- Test: extend src/test/aiAvailability.test.tsx — (1) `<AIDegradedState availability={a} needsKey />` in the default backend-only probe renders a button "Open API Keys"; clicking it fires `dwellium:open-widget` with `detail.widgetId === 'api-keys'` (pattern: avatarHarness.test.tsx:315-329); (2) existing "no CTA without needsKey" test unchanged.
- Verify: `cd qualia-shell && npx tsc -b && npx vitest run src/test/aiAvailability.test.tsx`.
- Overlaps with 045: none.

### S1b. Wire the banner into the 7 direct-`callLlm` widgets (minimal diff each) — M
Each: `import { useAIAvailability } from '../../hooks/useAIAvailability'; import AIDegradedState from '../Shell/AIDegradedState';` + `const ai = useAIAvailability();` next to the existing `useIntegrations()` call, then:
- HydraSplit.tsx:83-87 — replace the `.hyd__empty` text block with `<AIDegradedState availability={ai} needsKey ctaLabel="Add a key" reason="No LLM providers configured — each enabled key becomes a Hydra head." />` (hook call beside L38).
- AstraDashboard.tsx:864-869 — keep the `.a-research-nokey` wrapper + icon; replace the two `<p>` with `<AIDegradedState availability={ai} needsKey ctaLabel="Add a key" reason="Connect your own AI provider to run market & regulatory research." />` (hook beside L823).
- BuilderAgents.tsx:105 — insert `<AIDegradedState availability={ai} needsKey ctaLabel="Add a key" />` immediately above the `{err && …}` line (hook beside L20). Keep the L36 guard; shorten its string to `'No LLM configured — add a key above.'`.
- Synthesis.tsx:118 — same insertion above `{err && …}` (hook beside L24); L40 string → `'No LLM configured — add a key above.'`.
- Wiki.tsx:175-179 — replace the inline div with `<AIDegradedState availability={ai} needsKey ctaLabel="Add a key" reason="No LLM configured — “Compile” builds a structure-only page. Add a key for full synthesis." />` (hook beside L46).
- TranscriptionHub.tsx:2348-2352 — replace the `.th-legal-hint` span with `{legalShieldEnabled && <AIDegradedState availability={ai} needsKey ctaLabel="Add a key" reason="Add an LLM key to enable statute matching." />}` (hook beside L347).
- HonchoHermesPanel.tsx:686 — after the `</p>` add `{hermesVia === 'offline' && <AIDegradedState availability={ai} needsKey ctaLabel="Add a key" reason="No backend Ollama and no API key — add a key to bring Hermes up." />}` (hook beside L114; `hermesVia` union at L157).
- HomeUpkeepAI.tsx:237 — **skip**: `/api/maintenance/analyze-photo` uses the server's key (backend src/routes/maintenanceAgentRoutes.ts:119-130); a personal key would not help. Only fix the lie: catch text → `'Photo analysis unavailable — the server’s vision model didn’t respond.'`.
- Why this shape: the component already handles `'ready'` → null, so every site is a drop-in replacement with no conditional logic added to the widget.
- Test: extend src/test/aiAvailability.test.tsx with one render of `<HydraSplit/>` (already SSR/anon-safe; useIntegrations degrades to `_anonymous`) asserting `getByRole('button', { name: 'Add a key' })`; other widgets are covered by the S1a component tests (same props).
- Verify: `npx eslint <the 8 files>`; browser: fresh profile (no keys) → open Hydra, Synthesis, Builder Agents, Wiki, Astra, Honcho, Transcription Hub (Legal Shield on) → each shows the banner; click "Add a key" → the **API Keys** window opens; save an Anthropic key → every banner disappears without reload.
- Overlaps with 045: none (none of these files are in 045's list).

### S1c. ARAConsole: missing-key banner above the composer — S
- ARAConsole.tsx: import as above; `const ai = useAIAvailability();` directly after `const { integrations } = useIntegrations();` (L421); immediately above `<div className="ara-input-bar">` (L2536) add `{ai.status !== 'backend-only' && <AIDegradedState availability={ai} />}`. ARA really does try the backend first (L1188-1194), so `'backend-only'` is legitimately silent; `'unavailable'`/`'erroring'`/`'rate-limited'` show the reason + "Open API Keys" instead of the user discovering it via "No output — the model returned nothing." (L1327/L1410).
- Why this shape: three lines inside a 045-D-owned file; conditional keeps the default first window quiet when the backend path works.
- Test: none new (covered by S1a component tests; ARAConsole has no render test harness — STOP rule below).
- Verify: stop the backend (or set `VITE_API_BASE` to a dead port) + no key → ARA shows "No AI key configured and the backend is unreachable. [Open API Keys]".
- Overlaps with 045: **D — ARAConsole.tsx**: import + 2 lines; land after 045-D or rebase (no shared hunks with 045-D1b voice-gender move at L2666).

### S1d. `syncStatusStore` inside oneSaveStore + reconnect replay — S
- src/lib/oneSaveStore.ts (module scope, above `makeSynced` L83): `const pending = new Set<string>(); const failed = new Map<string, () => void>(); let lastSavedAt: number | null = null; const listeners = new Set<() => void>();` + `emit()`; export `syncStatusStore = { subscribe, getSnapshot: () => snap, getServerSnapshot: () => SERVER, reset }` with `interface SyncStatusSnapshot { pending: number; lastSavedAt: number | null }` (cache `snap` object; replace on change — same shape as aiHealthStore.ts:39-55). SSR-safe: no browser globals.
- In `scheduleWriteThrough` (L94): after `clearTimeout` add `pending.add(scheduledObjectId); emit();`. On the owner-switch drops (L102, L111): `pending.delete(scheduledObjectId); emit(); return;`. On success (L120): `pending.delete(…); failed.delete(…); lastSavedAt = Date.now(); emit(); return;`. After the loop (L128): `pending.delete(…); failed.set(scheduledObjectId, () => scheduleWriteThrough(value)); emit();` then the existing `markOffline`.
- Replay (makes "will retry" true): module-level `backendStatusStore.subscribe(() => { if (backendStatusStore.getSnapshot().state !== 'online' || failed.size === 0) return; const fns = [...failed.values()]; failed.clear(); fns.forEach((f) => f()); });` — guarded by `if (ONE_SAVE_ENABLED)`. Replay re-runs the owner check, so a switched account never gets the old user's write (same invariant as L101-111). `// ponytail: last-value replay only; full outbox if multi-tab ordering ever matters`.
- Why this shape: the counter lives where the timer lives — no widget changes, no new store file; replay closes the silent drop at L128 (data-loss path ⇒ not lazily skipped).
- Test: extend src/test/oneSaveStore.test.ts (mocked `oneSaveClient.put`, fake timers already in place L8-15, L31-35): `set()` → `syncStatusStore.getSnapshot().pending === 1`; advance 800 ms + resolve put → `pending === 0`, `lastSavedAt` is a number; put rejects/returns null ×3 → `backendStatusStore.getSnapshot().state === 'offline'`, `pending === 0`; then `backendStatusStore.markOnline()` → `put` called once more with the same payload.
- Verify: `npx vitest run src/test/oneSaveStore.test.ts src/test/backendStatusStore.test.ts`.
- Overlaps with 045: none.

### S1e. `SyncStatusPill` mounted in AdminShell — S
- NEW src/components/Shell/SyncStatusPill.tsx: reads `syncStatusStore` + `backendStatusStore` via `useSyncExternalStore`. Render rules: `!ONE_SAVE_ENABLED` → null; backend `state === 'offline'` → `Offline — will retry` (class `--offline`); `pending > 0` → `Saving…`; `lastSavedAt` set → `Saved ✓` with `title={new Date(lastSavedAt).toLocaleTimeString()}`; never-saved-this-session → null. `role="status" aria-live="polite"`. `// ponytail: no ticking "just now" clock — tooltip carries the time; add a 60 s tick if Ilya wants relative text`.
- NEW SyncStatusPill.css: `.sync-pill { position: fixed; bottom: 10px; left: 50%; transform: translateX(-50%); z-index: 97000; font-size: 11px; padding: 3px 10px; border-radius: 999px; background: var(--surface-2, rgba(255,255,255,.05)); border: 1px solid var(--border-subtle, rgba(255,255,255,.08)); color: var(--text-tertiary); pointer-events: none; }` + `.sync-pill--offline { border-color: var(--danger-border, rgba(244,113,116,.4)); color: var(--text-secondary); }`. Bottom-centre is free: SystemHealthBanner is fixed top-centre (SystemHealth.css:73-78, z 98000), MorningBriefBanner top 56px (MorningBriefBanner.css:4-6), BackendConnectionBanner top full-width, `.qualia-toast` + Assistant FAB bottom-right (Desktop.css:660-663, AssistantLauncher.css:4-6); the mobile rail stays a 56px left column (responsive.css:9-14) so nothing collides.
- AdminShell.tsx:155-165: add `import SyncStatusPill from './SyncStatusPill';` and `<SyncStatusPill />` after `<TagHotkey />`.
- Why this shape: passive, pointer-events:none, different corner from the "N connections need setup" toast (SystemHealthBanner.tsx:34-46) — they never fight; no Sidebar.tsx edit (045-A).
- Test: NEW src/test/syncStatusPill.test.tsx — `vi.mock('../lib/oneSaveClient', () => ({ ONE_SAVE_ENABLED: true, oneSaveClient: {…} }))`; drive `syncStatusStore` via a wrapped `withSync` store + fake timers, assert texts "Saving…" → "Saved ✓"; `backendStatusStore.markOffline()` → "Offline — will retry"; `backendStatusStore.reset()` in `beforeEach`. Add `SyncStatusPill` to the getServerSnapshot sweep in src/test/appfolioParity/providerSSRSafety.test.tsx if it enumerates Shell stores (check L100+).
- Verify: `cd qualia-shell && npx tsc -b && npx vitest run && NETLIFY=1 npx react-router build` (+ `npx eslint src/components/Shell/SyncStatusPill.tsx src/lib/oneSaveStore.ts src/components/Shell/AdminShell.tsx`); browser: type in Wiki → pill flips "Saving…" → "Saved ✓" within ~1 s; stop backend, type → "Offline — will retry" alongside the red top banner; restart backend → banner clears, pill returns to "Saved ✓" and the network tab shows the replayed `PUT /api/objects`.
- Overlaps with 045: none (AdminShell.tsx is not owned by any 045 cluster).

STOP conditions
- `callLlm` grows a backend fallback before this lands → drop `needsKey` from S1b call sites (the plain component is then truthful). Re-read llmClient.ts:54-109 first.
- 045-D merges first and `ara-input-bar` moved → re-anchor S1c by grepping `className="ara-input-bar"`; do not touch anything else in ARAConsole.tsx.
- `VITE_ONE_SAVE` is not `'true'` in the deployed env (.env:5 / .env.example:46 say true) → the pill intentionally renders nothing; do not fake a "Saved" state from localStorage.
- Any S1d test needs `oneSaveClient.put` to throw rather than return null → the loop at L113-125 treats only `null` as failure; keep that contract, don't widen it.
- Never push/merge without Ilya's go.

Ilya decision gates
- none — proceed.

---

## Cluster D — Honest demo mode + Labs group  (branch `ux/046-d-demo-labs`; files: NEW `lib/demoWorkspaceStore.ts`, NEW `components/common/DemoBanner.tsx`, NEW `components/common/NotYet.tsx`, `StrataDashboard/strataApi.ts`, `StrataDashboard/StrataDashboard.tsx`, `AstraDashboard/AstraDashboard.tsx`, `ControlPanel/ControlPanel.tsx`, 11 Strata-module/TemplateGenerator stub sites, `FileExplorer/FileExplorer.tsx` (comment only), `data/hierarchy.ts` + `Sidebar/Sidebar.tsx` + `Sidebar/iconMap.ts` (Labs, 6 lines total))

> **Two corrections to the brief (verified while anchoring)**: (1) `VITE_APPFOLIO_SEEDS` does NOT gate a usable seed layer — `fixtures/appfolioDerived/*.ts` (1,451 lines) carry **real-person AppFolio data** ("so no real-person data leaks into public builds", `tenants.ts:7-8`; `netlify.toml:21` sets the flag `false`) and have **zero importers** outside the barrel (grep `APPFOLIO_` in `src/` → only `StrataDashboard.tsx:54,602` comments). The flag's only runtime effect is `SHOW_SAMPLE_KPIS` (`StrataDashboard.tsx:59`). Do NOT wire those fixtures into demo mode. The real, shippable demo dataset is `public/data/*.json` (7.3 MB, 40 tables, already served at `/data/*` in the prod client build — `build/client/data/` exists) read by `strataApi.static.ts:19-24` (`loadTable` → `fetch('/data/<table>.json')`), with writes persisted **only** to `localStorage['dwellium-changes-<table>']` (`strataApi.static.ts:15-16, 1449-1473`; no other reader of that key in `src/`). (2) File Explorer is **not** a stub: header `FileExplorer.tsx:4-6` is stale — it calls `fetchTree()` (`FileExplorer.tsx:93` → `fileExplorerApi.ts:27` → backend `src/routes/fileExplorerRoutes.ts:113`). Fix the comment; it stays in Filing Cabinet.

### D1. Demo workspace = per-user runtime flag that routes Strata+Astra through the static data layer — M

- NEW `src/lib/demoWorkspaceStore.ts` (copy the shape of `lib/hiddenWidgetsStore.ts:13-36`): `KEY='dwellium-demo-workspace'`; `export const demoWorkspaceStore = withSyncStatic(createLocalStorageStore<boolean>(() => localStorage.getItem(KEY) === 'true', false), { objectType: 'demo-workspace', storageKey: KEY })` (`lib/oneSaveStore.ts:184`, `utils/createLocalStorageStore.ts`); `export const isDemoWorkspace = () => demoWorkspaceStore.getSnapshot()`; `setDemoWorkspace(on: boolean)` (persist + `localStorage.setItem`); `useDemoWorkspace()` via `useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot)`.
- `src/components/StrataDashboard/strataApi.ts:26` `const impl = USE_STATIC ? staticImpl : backendImpl;` → `const pick = () => (USE_STATIC || isDemoWorkspace()) ? staticImpl : backendImpl;` and `impl.` → `pick().` in `strataGet/strataPost/strataUpload/strataPut/strataDelete/strataGetPaginated` (L48-87). **Leave `isStaticMode` (L36) build-time** — in demo mode the 4 write-guards (D3 table rows 12-15) stay off, so demo writes land in the localStorage sandbox and the demo feels alive. Astra needs no edit: `AstraDashboard/dashboardData.ts:27,36` already reads via `strataGet`.
- Toggle lives in Settings: `ControlPanel.tsx` — new `<section className="cp-section">` directly above the Activation Center section (L602-604), `<h3 className="cp-section__title">Data</h3>`, `cp-label` *"Demo workspace"*, On/Off markup copied from the Desktop Regions toggle (L444-449). `onClick`: `setDemoWorkspace(on); window.location.reload();` — reload is the cache-invalidation (react-query caches in `useStrataQueries.ts:13`, module state). Helper copy under it: *"On: Strata and Astra show a sample portfolio you can click around and edit (edits stay on this device). Off: your live data."*
- **Write-path guard (stated)**: with the flag ON, every Strata/Astra read and write goes through `strataApi.static.ts`, whose only network call is `GET /data/<table>.json` (L22); POST/PUT/DELETE resolve `matchWriteRoute` locally (L1449-1473) into `localStorage['dwellium-changes-*']`. No code reads those keys into backend mode. One bypass to close: `AstraDashboard.tsx:934` POSTs `${API_BASE}/api/dwellium/workitems/${id}/promote` directly — add `if (isDemoWorkspace()) { toast? no — just return; }` at the top of that handler. Widgets that don't go through `strataApi` (Task Menu `/api/tasks` `TaskMenu.tsx:106`, Inbox Zero `INBOX_API`, File Manager) keep showing **real** data and get **no** banner — there is no `tasks.json`/inbox table in `public/data/` to seed them (ls verified). State this in the toggle helper copy: *"Tasks, Inbox and Files always show your real data."*
- **Default**: OFF. The store is new, so "no stored value" is true for every *existing* account on every device; default-ON would swap a real portfolio for seeds until the user finds the toggle. Safe auto-ON needs a portfolio-empty probe (`backendImpl.strataGet('/stats').totalProperties === 0` before first render, skip when backend offline) — ~15 lines, **Ilya gate** below. Instead, the real empty state offers it in one click (D2).
- **Why this shape**: the static layer already exists, is CI-verified (`playwright.config.ts:36` runs e2e against it), and its writes cannot reach the backend; one runtime predicate in the router beats a second data path.
- **Test**: extend `src/test/strataApi.test.ts` — `it('routes to the static layer when demo workspace is on')`: `setDemoWorkspace(true)`; `await strataGet('/stats')`; assert the mocked `fetch` URL starts with `/data/` (not `API_BASE`); `setDemoWorkspace(false)` → URL starts with `API_BASE`. NEW `src/test/demoWorkspaceStore.test.ts` (clone `hiddenWidgetsStore.test.ts:9-26`): default `false`; `setDemoWorkspace(true)` persists `'true'` under `dwellium-demo-workspace`; `reset()` restores.
- **Verify**: `cd qualia-shell && npx tsc -b && npx vitest run src/test/strataApi.test.ts src/test/demoWorkspaceStore.test.ts src/test/appfolioParity && NETLIFY=1 npx react-router build && VITE_APPFOLIO_SEEDS=true npx react-router build && VITE_APPFOLIO_SEEDS=false npx react-router build && npx eslint src/lib/demoWorkspaceStore.ts src/components/StrataDashboard/strataApi.ts src/components/ControlPanel/ControlPanel.tsx`. Browser: Settings → Data → Demo workspace On → reload → Strata Properties lists `public/data/properties.json` rows; Network tab shows `/data/*.json` and **zero** `/api/dwellium/*` calls from Strata/Astra; add a property → appears; toggle Off → gone, live data back.
- **Overlaps with 045**: none (ControlPanel.tsx, strataApi.ts, AstraDashboard.tsx are in no 045 cluster).

### D2. Dismiss-proof "Demo data" banner + honest empty state when OFF — S

- NEW `src/components/common/DemoBanner.tsx`: `useDemoWorkspace()`; returns `null` when off. When on: `<div role="status" className="demo-banner">` (inline styles; amber `#f59e0b` border like the pill at `StrataDashboard.tsx:609-612`) with text **"Demo data — this isn't your portfolio."** and two buttons: **"Replace with your data →"** → `openWidget('control-panel')` (`lib/dwelliumCommands.ts:133`; lands on Settings → Activation Center → *Live AppFolio sync* card `ActivationCenter.tsx:39-58`) and **"Add a property"** → `openStrataModule('properties')` (`StrataDashboard/strataDeepLink.ts:53`; module's "Add Property" button `PropertiesModule.tsx:543`). No close/dismiss control, no localStorage "seen" flag.
- Mount once per surface: `StrataDashboard.tsx:1872` first child inside `<div className="strata-dashboard strata-shell">`; `AstraDashboard.tsx:1356` first child inside `<div className="astra-dashboard">`.
- Delete the invented-money block: `StrataDashboard.tsx:51-59` (`SHOW_SAMPLE_KPIS`) and L592-638 (six hardcoded KPI cards + "Sample data" pill). Prod already hides them (`netlify.toml:21`); in demo mode the real KPI band (L560-580) computes from seeds via `strataApi.static.ts` `/stats` (L57-75). After this, `VITE_APPFOLIO_SEEDS` gates only the unimported fixture files — both builds still compile.
- Empty state when OFF: `StrataDashboard.tsx:571-577` (`noPortfolio` KPI subtitle *"Import properties to begin"*) — below the KPI grid, when `noPortfolio && !demo`, render `<EmptyState icon={Building2} message="No properties yet" sub="Add one, connect AppFolio, or look around with demo data." action={<>…three buttons…</>} />` (`StrataDashboard/StateView.tsx:24-35` `EmptyState` already takes `action`). Third button **"Try with demo data"** → `setDemoWorkspace(true); window.location.reload()`.
- Astra "Sample" badge: `AstraDashboard.tsx:88-91` `MockBadge` on `QuickArbitrage` (L1036, data `ARBITRAGE_OPPORTUNITIES = []` L1027) and `DomainViews` (L1081, `DOMAIN_VIEWS = []` L1076) render empty lists under a "Sample" label — replace both panel bodies with `<NotYet reason="…" />` (D3 rows 16-17); keep `MockBadge` only on HR Snapshot (L770) until its source is live.
- **Why this shape**: one component, two mounts, no dismiss state to sync; the CTAs are the two real entry points that exist today.
- **Test**: NEW `src/test/demoBanner.test.tsx` — off → `container.firstChild === null`; on → `getByRole('status')` has text `/Demo data/`, two buttons, `queryByLabelText(/close|dismiss/i) === null`; click "Replace with your data" → `dwellium:open-widget` event with `widgetId: 'control-panel'` (spy on `window.dispatchEvent`).
- **Verify**: `npx vitest run src/test/demoBanner.test.tsx`; browser: demo On → banner on Strata and Astra, no × anywhere; demo Off on an empty backend → EmptyState with the 3 buttons.
- **Overlaps with 045**: C owns `StrataDashboard.tsx` **chart colours** only — D2 touches L51-59, L571-577, L592-638, L1872 (disjoint lines; rebase trivially).

### D3. `<NotYet reason>` — one inline component for every stub — S

- NEW `src/components/common/NotYet.tsx`: `export function NotYet({ reason, testId }: { reason: string; testId?: string })` → `<div data-testid={testId} className="not-yet" role="note" style={{ fontSize: 11, color: 'var(--text-tertiary)', fontStyle: 'italic', display: 'flex', gap: 6, alignItems: 'center' }}><span style={{ border: '1px solid currentColor', borderRadius: 3, padding: '0 5px', fontSize: 9, fontWeight: 700, letterSpacing: .6, textTransform: 'uppercase', fontStyle: 'normal' }}>Coming soon</span>{reason}</div>`. Also `export const notYetMessage = (action: string) => \`${action} is not available in this read-only preview.\`` for the 4 string-state guards (contains "not " so `SentimentModule.tsx:219`'s red/green regex still classifies it as an error).
- Sweep (replace the ad-hoc string; keep `data-testid`s; keep the phrases existing tests grep for):

| # | Site | Replace with |
|---|------|--------------|
| 1 | `MaintenanceModule.tsx:219` BlockViewAsTech | `<NotYet testId="wo-block-view-as-tech" reason="Tech-portal view lands with role-based access." />` |
| 2 | `MaintenanceModule.tsx:243` BlockInvoices | `<NotYet testId="wo-block-invoices" reason="Invoice ledger per work order." />` |
| 3 | `MaintenanceModule.tsx:252` BlockTexts | `<NotYet testId="wo-block-texts" reason="SMS thread per work order." />` |
| 4 | `MaintenanceModule.tsx:261` BlockEmails | `<NotYet testId="wo-block-emails" reason="Email thread per work order." />` |
| 5 | `ResidentsModule.tsx:158` BlockFolioGuardUpsell | `reason="FolioGuard Smart Ensure — automated insurance tracking."` (test greps `/FolioGuard Smart Ensure/i` + `/Coming soon/i`, `residents.module.test.tsx:80-81`) |
| 6 | `ResidentsModule.tsx:191` BlockUpcomingActivities | `reason="Upcoming activities (renewals, inspections, rent changes)."` |
| 7 | `ReportingModule.tsx:430` Surveys tab chip | `<NotYet reason="Create and send tenant satisfaction surveys." />` replacing the accent `Coming Soon` span |
| 8 | `VendorsModule.tsx:253` BlockSurvey | `reason="Survey responses not yet captured."` (test `vendors.module.test.tsx:131`) |
| 9 | `VendorsModule.tsx:295` BlockActivity | `reason="Activity log not yet captured."` (test L146) |
| 10 | `CalendarModule.tsx:355` Outlook card `<p>` | `<NotYet reason="Microsoft Outlook sync — use the .ics download above meanwhile." />`; keep the `Planned` badge |
| 11 | `TemplateGenerator.tsx:411` fake "Browse Files" input | `disabled` the `<input>`, drop the misleading toast, render `<NotYet reason="DOCX variable extraction." />` under the dropzone |
| 12 | `TenantPortalModule.tsx:89-91` `staticModeMessage()` | `return notYetMessage('Send message')` |
| 13 | `CorporateReview.tsx:41-43` `staticModeMessage(action)` | `return notYetMessage(action)` |
| 14 | `SentimentModule.tsx:93` | `setSubmitMsg(notYetMessage('Survey submission'))` |
| 15 | `ProjectsModule.tsx:93` (4th site, not in brief) | `setStatusFeedback(notYetMessage('Status updates'))` |
| 16 | `AstraDashboard.tsx:1036-1062` QuickArbitrage body | `<NotYet reason="90-day arbitrage detection." />` (list is `[]` at L1027) |
| 17 | `AstraDashboard.tsx:1081-…` DomainViews body | `<NotYet reason="Saved domain views." />` (list is `[]` at L1076) |

- Update 4 test regexes to the new copy: `tenant-portal.module.test.tsx:286` → `/send message is not available/i`; `projects.module.test.tsx:242` → `/not available in this read-only preview/i`; `corporate-review.module.test.tsx:212,244` → `/upload is not available/i`, `/triage is not available/i`. Rows 1-10 keep `/Coming soon/i` green because the chip text is literally "Coming soon".
- **Why this shape**: one component, one helper, no CSS file; the chip keeps every existing `/Coming soon/i` assertion passing.
- **Test**: NEW `src/test/notYet.test.tsx` — renders chip text `Coming soon` + reason; `testId` forwarded; `notYetMessage('Upload')` contains `'not available'`. Then `npx vitest run src/test/appfolioParity` must stay green.
- **Verify**: `npx vitest run src/test/notYet.test.tsx src/test/appfolioParity && npx eslint src/components/common/NotYet.tsx src/components/StrataDashboard/modules/{Maintenance,Residents,Reporting,Vendors,Calendar,TenantPortal,Sentiment,Projects}Module.tsx src/components/StrataDashboard/modules/CorporateReview.tsx src/components/DocViewer/TemplateGenerator.tsx`; browser: open a work order → the four stub blocks show the same chip; Template Generator → Browse is disabled with the chip.
- **Overlaps with 045**: none.

### D4. "Labs" sidebar group (collapsed) for dev/deprecated widgets + stale-header fix — S

- Chosen mechanism: **hierarchy.ts dock entries** (smaller and the only one that reaches the sidebar — the sidebar groups `dockItems` by `item.group` (`Sidebar.tsx:816-823`) and the Add-widget gallery reads `dockItems` too (`Sidebar.tsx:1144`); a registry `category` would only re-bucket the FluidOS/HalocronOS catalogs (`FluidOS.tsx:263-265` falls back to `'other'`) and would break `registryWalker.test.tsx:66-68`). Existing dock ids keep their saved group on merge (`WindowContext.tsx:47-63` reconciles label/icon only), so Labs must use **new** ids — the merge appends them for existing users (L60-62).
- `src/data/hierarchy.ts:72` (end of array) append:
  `{ id: 'dock-labs-inbox-zero', label: 'Inbox Zero (deprecated)', icon: 'mail-open', component: 'inbox-zero', pinned: true, group: 'Labs' },` — deprecated 2026-04-19, superseded by `services/emailRouter.ts` (`widgetRegistry.ts:232-248`); today reachable only via ⌘K.
  `{ id: 'dock-labs-autonomous-runs', label: 'Autonomous Runs', icon: 'terminal', component: 'autonomous-runs', pinned: true, group: 'Labs' },` — launcher for repo `launch_*_autorun.sh` harnesses, "honestly labeled" dev tool (`AutonomousRuns.tsx:1-5`; registry L184-191).
  `{ id: 'dock-labs-time-travel', label: 'Time Travel', icon: 'history', component: 'time-travel', pinned: true, group: 'Labs' },` — One Save event-log browser (`TimeTravel.tsx:1-8`; registry L602-610).
  `{ id: 'dock-labs-holocron', label: 'Holocron Library', icon: 'diamond', component: 'holocron-library', pinned: true, group: 'Labs' },` — theme toy, "no backend" (`HolocronLibrary.tsx:1-8`; registry L612-621).
- `src/components/Sidebar/Sidebar.tsx:807-811` `WIDGET_GROUPS` → add `{ name: 'Labs', icon: 'flask-conical' }` as the 4th entry. Collapsed by default for everyone: `sidebarGroupsStore` default set (`Sidebar.tsx:93`) and any saved set lack `'Labs'`. `src/components/Sidebar/iconMap.ts` → import `FlaskConical` from `lucide-react` and add `'flask-conical': FlaskConical` next to `'archive'` (L112).
- `src/components/FileExplorer/FileExplorer.tsx:4-6` comment → *"Cycle 2: scaffold; Cycle 3+: tree fetched via fileExplorerApi.fetchTree() (/api/files/tree)."* No behaviour change; File Explorer stays in Filing Cabinet.
- Not Labs (evidence checked): File Explorer (real fetch), Workspace (reads the same tree, `Workspace.tsx:3-12`), Cloud Browser / Terminal / Georgia Code (real backends), the folded agents (already hidden via `hiddenWidgetsStore.ts:55-56`).
- **Why this shape**: 6 added lines, zero new state; new ids ride the existing dock-merge.
- **Test**: extend `src/test/sidebar.045.test.tsx` — render Sidebar with default stores: `getByText('Labs')` present; `queryByText('Inbox Zero (deprecated)')` is `null` (collapsed); click the Labs header → it appears. Assert `can('widget:inbox-zero')` path renders (if permissions deny it, note and drop the entry).
- **Verify**: `npx vitest run src/test/sidebar.045.test.tsx src/test/registryWalker.test.tsx && npx eslint src/data/hierarchy.ts src/components/Sidebar/Sidebar.tsx src/components/Sidebar/iconMap.ts`; browser: sidebar shows a collapsed "Labs" group at the bottom; existing profile (saved layout) also gets it (merge), Property Management / AI Tools / Filing Cabinet untouched.
- **Overlaps with 045**: **A** — `data/hierarchy.ts` (4 appended lines at the array tail) and `Sidebar.tsx` (1 line in `WIDGET_GROUPS`, L807-811). If A is mid-flight, post these two diffs to A instead of editing; `iconMap.ts` is not in A's list.

**STOP conditions**
- If any Strata/Astra read or write bypasses `strataApi.ts` beyond `AstraDashboard.tsx:934` (grep `API_BASE}/api/dwellium` under `StrataDashboard/` and `AstraDashboard/` before merging) — stop and list them; demo mode must not touch the backend.
- If `VITE_APPFOLIO_SEEDS=true`/`false` builds diverge after deleting `SHOW_SAMPLE_KPIS` — stop; do not re-introduce the fixtures.
- Do not import anything from `fixtures/appfolioDerived/` into demo mode (real-person data). Separate cleanup candidate, not this cluster.
- Do not push/merge without Ilya's go; never delete user data (demo sandbox lives only in `dwellium-changes-*` localStorage keys and is never migrated).

**Ilya decision gates**
1. Demo workspace default for brand-new users: **OFF (proposed)** with the one-click "Try with demo data" in the empty state — or ON behind a portfolio-empty probe (`/stats.totalProperties === 0`, backend online) before first render (+~15 lines, + a test)?
2. Labs membership (proposed): Inbox Zero (deprecated), Autonomous Runs, Time Travel, Holocron Library. Add/remove? (File Explorer stays out — it is real.)
3. OK to delete the six hardcoded "Sample data" KPI cards (`StrataDashboard.tsx:592-638`) rather than re-gate them on the demo flag? (Proposed: delete; prod never shows them today.)

---

## Cluster F — First run that earns a win + one-screen front door  (branch `ux/046-f-first-run`; files: NEW `src/lib/firstRunStore.ts`, NEW `src/components/Shell/FirstRunCard.tsx` + `.css`, NEW `src/components/Sidebar/mottoFor.ts`, `Shell/AdminShell.tsx` (1 import + 1 mount), `lib/perUserIdentity.ts` (+1 holder), `Shell/defaultStack.ts` (+3 lines), `Shell/Desktop.tsx` (3 call sites), `Sidebar/Sidebar.tsx` (−12/+2), `Auth/LoginScreen.tsx` (splash block only) + `LoginScreen.css`)

> **Drift check (run first)**: `git log --oneline -3 -- qualia-shell/src/components/Shell/defaultStack.ts qualia-shell/src/components/Shell/Desktop.tsx qualia-shell/src/components/Sidebar/Sidebar.tsx qualia-shell/src/components/Auth/LoginScreen.tsx`. Anchors below are `main` @ `5e78ca8` (2026-08-19). NOTE: plan 045 Cluster A (`a72a120`) and D2 (`9e1d184`) are **already merged into main** — `LoginScreen.tsx:29` is already `type Stage = 'gate' | 'credential'` (one form), and `defaultStack.ts` already exports `PINNED_WIDGETS`/`DEFAULT_STARTUP_STACK` (L35-44). Overlaps below are therefore with landed code, not in-flight branches; keep each diff to the named lines.

### F1. Guided first run — "Get to your first win" — M

- **Facts verified**: zero onboarding anywhere (`grep -rn "onboarding\|tour\|checklist" src/components` → none). Default stack fires from `Desktop.tsx:664-687`, guarded by `shouldOpenDefaultStack(localStorage.getItem(DEFAULT_STACK_KEY), …)` (`Desktop.tsx:673`), key `'dwellium:default-stack:v1'` (`defaultStack.ts:20`) — per-browser. Strata empty = KPI dashes *"No properties imported yet"* (`StrataDashboard.tsx:545,563,575`); Strata has **no CSV import** — the real entry is `PropertiesModule.tsx:543` `<button …>Add Property</button>` (POST `/properties`, `PropertiesModule.tsx:435`, then `invalidate('properties')` L442). Gmail's only connect UI is Control Panel → `GoogleConnectCard` "Connect Google…" (`ControlPanel/LlmIntegrationsSection.tsx:174-179`, status via `getGoogleOAuthStatus()` `lib/googleOAuthConnect.ts:67`); Inbox Zero has no connect button (`grep -n "Connect" src/components/InboxZero/InboxZero.tsx` → none). ARA prompt bus: `araPromptBus` = `'dwellium:ara-prompt'` (`lib/busChannels.ts:71`), helper `requestAraPrompt(text)` (`lib/llmRouter.ts:218`), consumed by ARA on mount + live (`ARAConsole.tsx:1660-1670`). "ARA replied" signal without touching ARAConsole: both reply paths call `recordAraChat()` (`ARAConsole.tsx:1150` backend, `:1214` LLM fallback) → `hermesLearningStore` (`HonchoHermesPanel/hermesLearningStore.ts:282`, per-user withSync) → `araChatRuns()` (`ARAConsole/araHermes.ts:29`). Deep-link to API keys: `openWidgetBus.emit({ widgetId: 'control-panel' })` (`hooks/useAIAvailability.ts:46-48`; listener `WindowContext.tsx:531-542`). Properties query: `useProperties()` (`StrataDashboard/useStrataQueries.ts:102`, React Query; `QueryProvider` wraps the shell `App.tsx:175-178`; data is `Property[]`, `PropertiesModule.tsx:194`).
- **New `src/lib/firstRunStore.ts`** (morningBriefStore sister shape, `lib/morningBriefStore.ts:34-66`): `export interface FirstRunState { neverShow: boolean; done: Array<'key'|'data'|'ara'> }` (`done` is sticky — a later key removal doesn't un-tick). Add `export const firstRunUserIdHolder = makeHolder()` to `lib/perUserIdentity.ts:47-64` + push into `ALL_HOLDERS` (L67-82) so the single writer namespaces it; `resolveKey()` → `` `firstrun:${uid}` `` / `'firstrun:_anonymous'`; `withSync(createLocalStorageStore({ key: resolveKey, deserializer, defaultValue: { neverShow:false, done:[] } }), { objectType: 'first-run', holder: firstRunUserIdHolder, resolveKey })`. Exports: `markDone(step)`, `setNeverShow()`, `resetFirstRun()` (test reset), `useFirstRun()` (calls `usePerUserIdentity()` then `useSyncExternalStore`). Pure helper `export function deriveSteps(live: { hasLlm: boolean; hasData: boolean; araReplied: boolean }, state: FirstRunState): { done: number; total: 3; steps: Array<{ id; done: boolean }> }` and `export function shouldShowFirstRun(state, sessionDismissed: boolean): boolean` → false when `neverShow`, or 3/3, or `sessionDismissed`. Session dismiss = `sessionStorage['dwellium:first-run:dismissed']='1'` (reappears next session/tab; no per-user persistence needed).
- **New `src/components/Shell/FirstRunCard.tsx`** (+ `FirstRunCard.css`, MorningBriefBanner sister: imports its own CSS, `Shell/MorningBriefBanner.tsx:13`; CSS tokens from `MorningBriefBanner.css:3-20`; position `fixed; right:16px; bottom:16px; width:320px; z-index:97000` — below the banners' 98000). Mount: `AdminShell.tsx:34` add `import FirstRunCard from './FirstRunCard';` and `AdminShell.tsx:162` add `<FirstRunCard />` after `<MorningBriefBanner />`. Live inputs: `hasActiveLlm(integrations.llm)` from `useIntegrations()` (`hooks/useIntegrations.ts:23`) + `hasActiveLlm` (`lib/llmClient.ts:115`); `useProperties().data?.length > 0`; `useSyncExternalStore(hermesLearningStore.subscribe, …)` + `araChatRuns().length > 0` (set `hermesLearningUserIdHolder.current = user?.id ?? null` during render first — same value ARAConsole writes, `ARAConsole.tsx:1366`). Each live-true → `markDone(step)` in an effect. Copy (exact): title **"Get to your first win"**, sub **"Three steps, about five minutes."**; (1) **"Add an AI key"** — "ARA and every AI widget run on your own key." CTA **"Open API Keys"** → `openWidgetBus.emit({ widgetId: 'control-panel' })`; (2) **"Bring your data"** — "Add your first property so Strata has something to show." CTA **"Add a property"** → `openStrataModule('properties')` (`StrataDashboard/strataDeepLink.ts:53`) [alt per gate 2: **"Connect Gmail"** → same control-panel emit; completion = `getGoogleOAuthStatus().status?.connected` polled on `visibilitychange`]; (3) **"Ask ARA"** — "Say hello — ARA answers in seconds." CTA **"Ask ARA"** → `openWidgetBus.emit({ widgetId: 'ara-console' })` + `requestAraPrompt('What can you help me with in Dwellium?')` (question form → non-actionable per `lib/llmRouter.ts:203-204`, so it lands on the chat path that records the reply). Footer: **"Dismiss"** (session) · **"Don't show again"** (`setNeverShow()`); at 3/3 render **"3/3 — you're set."** for one render then `setNeverShow()`.
- **Per-user default-stack flag (3-line diff)**: `defaultStack.ts:20` keep `DEFAULT_STACK_KEY`; add after L23: `/** Per-user key (046-F1): new users only — an existing browser flag stays valid for its user. */ export const defaultStackKey = (userId: string | null): string => userId ? `${DEFAULT_STACK_KEY}:${userId}` : DEFAULT_STACK_KEY;`. `Desktop.tsx:17` import `defaultStackKey` (drop `DEFAULT_STACK_KEY`), import `savedLayoutsUserIdHolder` from `'../../context/WindowContext'` (already set during render by WindowProvider, `WindowContext.tsx:99,181`; Desktop mounts inside it `AdminShell.tsx:172`); replace the three `DEFAULT_STACK_KEY` reads/writes at `Desktop.tsx:673, 677, 678` with `defaultStackKey(savedLayoutsUserIdHolder.current)`. Empty-canvas guard (L673) unchanged, so an existing user with windows open is never stomped.
- **Why this shape**: every completion is derived from stores that already exist (no polling ARA, no new backend); the card is one new file + one mount line.
- **Test**: NEW `src/test/firstRunStore.test.ts` — `deriveSteps` counts; sticky `done` survives `hasLlm:false`; `shouldShowFirstRun` false on `neverShow` / 3-of-3 / session-dismissed; key namespaced by `firstRunUserIdHolder`. NEW `src/test/FirstRunCard.test.tsx` (mock `oneSaveClient` like `src/test/holderIsolation.test.ts:20`) — renders 3 steps "0/3"; `integrationsStore` with an enabled anthropic key → step 1 ticked; click "Don't show again" → returns null + `neverShow===true`; "Ask ARA" click → `araPromptBus.consume()?.text === 'What can you help me with in Dwellium?'`. Extend `src/test/defaultStack.test.ts:14-33` — `defaultStackKey('u1') === 'dwellium:default-stack:v1:u1'`, `defaultStackKey(null) === DEFAULT_STACK_KEY`. Optionally append `firstRunUserIdHolder` to `src/test/holderIsolation.test.ts:38-51` (bump 12→13).
- **Verify**: `cd qualia-shell && npx vitest run src/test/firstRunStore.test.ts src/test/FirstRunCard.test.tsx src/test/defaultStack.test.ts && npx eslint src/lib/firstRunStore.ts src/components/Shell/FirstRunCard.tsx src/components/Shell/defaultStack.ts src/components/Shell/Desktop.tsx`; browser: fresh profile → login → ARA+Strata open, card bottom-right "0/3"; add a key in Control Panel → step 1 ticks without reload; Add Property → step 2 ticks; "Ask ARA" → ARA opens, replies, step 3 ticks → card says 3/3 then disappears; reload → stays gone; second user in same browser → card shows again and the default stack fires for them.
- **Overlaps with 045**: A — `defaultStack.ts` (landed `a72a120`): +3 lines after L23 only. B — `Desktop.tsx` (landed per-branch): L17 import + L673/677/678 key expression only. D — none (AdminShell is not 045-owned).

### F2. Generic data-driven sidebar motto — S

- `Sidebar.tsx:500-511` `GREETING_MESSAGES` is 10 hardcoded first names (Andy/Lisa/…/Marcus Johnson); `Sidebar.tsx:578` falls back to *"Let's get things done"* for everyone else (correction to the brief: it is not blank, it is a one-size fallback). NEW `src/components/Sidebar/mottoFor.ts`: `export function mottoFor(role: string | null | undefined): string` mapping the real roles (`ROLE_HIERARCHY` `context/UserContext.tsx:85`: tenant/agent/maintenance/advisor/management/corporate/god) → e.g. `management: "Your portfolio, one screen"`, `maintenance: "Keep it all running"`, `advisor: "Wise counsel, sharp moves"`, `corporate: "Strategy meets precision"`, `god: "Command the empire"`, `agent: "Leading the way forward"`, `tenant: "Welcome home"`, default `"Let's get things done"`. Sidebar diff: delete L500-511, `L578` → `const personalMessage = mottoFor(user?.role);` (+1 import).
- **Why this shape**: motto keyed on a field every user has; no per-person strings in source.
- **Test**: NEW `src/test/mottoFor.test.ts` — every role returns a non-empty string; unknown → default. Extend `src/test/sidebar.045.test.tsx` — user `{ name: 'Zed', role: 'management' }` renders `mottoFor('management')` in `.sidebar__greeting-msg` (`Sidebar.tsx:622`).
- **Verify**: `npx vitest run src/test/mottoFor.test.ts src/test/sidebar.045.test.tsx`; browser: log in as a non-roster user → motto is role text, not the generic line.
- **Overlaps with 045**: A — `Sidebar.tsx` (landed `a72a120`): −12/+2 lines at L500-511/L578 only; no CSS change.

### F3. Front door in one screen — S

- **Facts verified**: splash overlay `LoginScreen.tsx:120-125` renders only `"Click to Login"` (CSS `.login-start-text` `LoginScreen.css:51-59`, flashing); the card behind it (L132-227) holds the gate form, `GoogleSignInButton` **only when `VITE_GOOGLE_LOGIN==='true'`** (L27, L210-215), and "Resident? Sign in here" (L222-226). Header image is AstraStrata (L134). 045-D2 already landed (L29 `'gate' | 'credential'`); plan 014 owns `GATE_PASSWORD` (L24) / `localAccounts.ts` — **do not touch either**.
- Edit `LoginScreen.tsx:120-125` only — replace the inner `<div className="login-start-text">` with a `.login-front` block: `<h1 className="login-front__name">Dwellium</h1>`, `<p className="login-front__value">{VALUE_STATEMENT}</p>` (const near L27, text per gate 1), `<button type="button" className="login-primary-btn" onClick={() => setHasClicked(true)}>Sign in</button>`, `{GOOGLE_LOGIN_ENABLED && <div className="login-front__google" onClick={e => e.stopPropagation()}><GoogleSignInButton onCredential={loginWithGoogle} /></div>}`, `{onTenantMode && <button type="button" className="login-tenant-link" onClick={e => { e.stopPropagation(); onTenantMode(); }}>Resident? Sign in here</button>}`. Overlay `onClick` (L122) stays so clicking anywhere still enters. Keep the card's own Google block (L210-215) and footer link — unchanged.
- `LoginScreen.css`: replace `.login-start-text` (L51-59) with `.login-front { display:grid; gap:14px; justify-items:center; text-align:center; padding:24px; max-width:420px }`, `.login-front__name { font-size:40px; letter-spacing:6px; text-transform:uppercase; color:#fff }`, `.login-front__value { font-size:15px; color:rgba(255,255,255,.8) }`; reuse `.login-primary-btn` / `.login-tenant-link` (L468).
- **Why this shape**: name + promise + both entry paths are on the first paint; nothing behind the gate moves.
- **Test**: `src/test/LoginScreen.test.tsx:38-52` — change `fireEvent.click(screen.getByText('Click to Login'))` (L50) to `getByRole('button', { name: 'Sign in' })`; add: "Dwellium" + value statement + "Resident? Sign in here" present **before any click**; existing L54-59 (Google hidden by default) still passes; with `vi.stubEnv('VITE_GOOGLE_LOGIN','true')` the mocked "Continue with Google" is present on the splash.
- **Verify**: `npx vitest run src/test/LoginScreen.test.tsx`; `NETLIFY=1 npx react-router build`; browser (cold): first paint shows name, one line, "Sign in", Google (prod flag), resident link; "Sign in" → gate → credential (3 steps incl. splash, unchanged from 045-D2).
- **Overlaps with 045**: D2 — `LoginScreen.tsx(.css)` (landed `9e1d184`): splash block L120-125 + one CSS rule only; gate/credential forms, `GATE_PASSWORD`, `localAccounts.ts` untouched (plan 014).

**Gate per branch**: `cd qualia-shell && npx tsc -b && npx vitest run && NETLIFY=1 npx react-router build` + `npx eslint <touched files>`; paste Verify lines in the commit body. No push/merge without Ilya's go.

### STOP conditions

- F1: if `hermesLearningStore` is mocked/empty in the target env (no `recordAraChat` call observed after a real ARA reply), STOP — do not edit `ARAConsole.tsx` (045-D); report and fall back to completing step 3 on the "Ask ARA" click.
- F1: the default-stack key change must **never** clear or rewrite an existing `dwellium:default-stack:v1` value (no migration, no deletes).
- F3: do not modify `GATE_PASSWORD`, `localAccounts.ts`, or the gate/credential forms (plan 014 lockout risk).

### Ilya decision gates

1. **Value statement (F3) — pick one**: (a) *"Your properties, your inbox, your AI — one screen."* (b) *"Property management with an AI that actually does the work."* Also confirm the product name on the splash is **Dwellium** (sidebar logo) while the AstraStrata image stays in the card.
2. **Step 2 (F1)**: **"Add a property"** first (recommended — no OAuth/server creds needed, auto-completes via `useProperties`) vs **"Connect Gmail"** first (needs backend Google creds configured). Default if unanswered: Add a property.

---

## Cluster B — Real daily loop: server-side morning brief + notifications + badge  (branch `ux/046-b-daily-loop`; files: backend `src/services/morningBriefService.ts` (new), `src/routes/briefRoutes.ts` (new), `src/app.ts` (2 lines), `deploy/cloud-run.sh` (1 line); frontend `src/lib/briefNotifier.ts` (new), `src/hooks/useMorningBriefSync.ts` (new), `Shell/AdminShell.tsx` (1 line), `services/honchoBackgroundRunner.ts` (1 line), `ControlPanel/ActivationCenter.tsx`, `lib/activationStore.ts` (copy), `Shell/MorningBriefBanner.tsx/.css`, `Sidebar/Sidebar.tsx/.css` (≤6 lines))

> **Drift check first**: `git log --oneline -3 -- qualia-shell/src/lib/morningBriefStore.ts qualia-shell/src/services/honchoBackgroundRunner.ts qualia-shell/src/components/Sidebar/Sidebar.tsx` and backend `git log --oneline -3 -- src/routes/objectRoutes.ts src/stores/objectStore.ts`. Anchors below are `main` @ `5e78ca8` (frontend) / `cc5b79c` (backend), 2026-08-19. **Facts verified**: the brief is generated ONLY client-side on a tab-open `setInterval` (`honchoBackgroundRunner.ts:200-201`, `deepCycle` :85-150 → `upsertBrief` :148); `new Notification` / `Notification.requestPermission` appear nowhere in `src/` (grep 2026-08-19) though `activationStore.ts:196` claims it; every input the brief reads is already One-Save-synced (`goals` goalsStore.ts:74, `llm-usage` llmUsageStore.ts:81, `artifacts` artifactStore.ts:69, `honcho-memory` :57, `hermes-learning` :288, `thought-weaver` :62, `activity-log` activityLogStore.ts:80, `morning-brief` morningBriefStore.ts:59) under object id `${objectType}_${user.id}` (`oneSaveStore.ts:91`, holders = raw `user.id` per `perUserIdentity.ts:89-90`); backend One Save is a filesystem store (`stores/objectStore.ts:36-41`) on the GCS mount `/var/dwellium/data` (`deploy/cloud-run.sh:127,237-238`), one warm instance, CPU-throttled by default (`cloud-run.sh:215-218`) → in-process timers are unreliable, Cloud Scheduler → HTTP is the right trigger.

### B1. Backend: `generateMorningBriefs()` + `POST /api/briefs/run` (shared secret) — M

- NEW `src/services/morningBriefService.ts`:
  - `briefDayKey(tz = process.env.BRIEF_TZ || 'America/New_York', d = new Date())` → `new Intl.DateTimeFormat('en-CA', { timeZone: tz, year:'numeric', month:'2-digit', day:'2-digit' }).format(d)` (YYYY-MM-DD; mirrors `dailySynthesis.ts:131-136` but TZ-explicit).
  - `buildDataLines(objs)` — pure port of `honchoBackgroundRunner.ts:91-102`: Goals line (`status !== 'done'`, top 3, `goalProgress` = done/total of `plan.agentActions+userActions`, goalsStore.ts:133-138, ×100 rounded), AI-usage line (`lastNDays(7, ledger)` + `planAdvice(ledger)` — both already take a `ledger` arg, llmUsageStore.ts:159-190; copy verbatim, they are pure given `ledger.days`), Artifacts line (`artifacts.length`, `[0].title`), plus ONE new line from `activity-log`: `While you were away: N actions across M widgets since <yesterday's date>` (entries with `ts` ≥ previous brief's `createdAt` or last 24 h; `ActivityEntry` shape activityLogStore.ts:40-49). **Skip** the cost-advisor lines (:106-122) — `ponytail:` client fallback still produces them; port later if missed.
  - `buildCorpus(objs)` — port of `dailySynthesis.ts:38-91` minus the cost section (same caps: 12 memories / 8 runs / 6 goals / 8 captures / 8 artifacts / usage). `DEEP_DREAM_SYSTEM` + `parseDeepDream` copied **verbatim** from `dailySynthesis.ts:100-128` (pure).
  - `generateMorningBriefs({ date?, userIds? })`: `listUsers(500)` (`authService.ts:128`) → for each user read `getObject('<type>_<id>')` for the 8 types above; skip users with no objects at all; if `OPENAI_API_KEY` and `corpus.sections ≥ 1` call OpenAI exactly as `llmRouterRoutes.ts:62-70` (`fetch chat/completions` + `openaiChatParams(process.env.OPENAI_MODEL || 'gpt-4o-mini', { maxTokens: 700, temperature: 0.6, json: true })`), swallow errors → data-only brief; then `upsertObject({ id: 'morning-brief_'+id, type: 'morning-brief', ownerId: id, payload: [brief, ...existing.filter(b => b.date !== date)].slice(0, 30) })` where `brief = { date, insights, suggestions, dataLines, createdAt: now, seen: false, source: 'server' }` (superset of `MorningBrief`, morningBriefStore.ts:21-30; `isBrief` :39-41 ignores extra keys). Idempotent: if the existing entry for `date` already has `source: 'server'`, skip unless `force`. Returns `{ date, generated, skipped, errors }`. Never logs payloads or the key.
- NEW `src/routes/briefRoutes.ts`: `POST /run` — read `x-brief-secret`; if `!process.env.BRIEF_RUN_SECRET` → `503 {error:'BRIEF_RUN_SECRET not configured'}` (never an open sink; mirrors `meetingRoutes.ts:120-128`); compare with `crypto.timingSafeEqual` on equal-length buffers else `401`; body `{ date?, userIds?, force? }` → `res.json({ success: true, data: await generateMorningBriefs(body) })`. **No `authenticate`** on this route (machine caller). `GET /api/briefs/today` is **not built**: `GET /api/objects/morning-brief_<uid>` (`objectRoutes.ts:49-61`, owner-scoped) already IS that endpoint and the client already calls it via `hydrate()` — add a dedicated route only if a non-One-Save client appears.
- `src/app.ts`: import + `app.use('/api/briefs', briefRoutes);` next to `:409` scheduler mount.
- `deploy/cloud-run.sh:113`: add `upsert_secret_from_env BRIEF_RUN_SECRET dwellium-brief-run-secret` (after the OPENAI line :109).
- Ilya runs (placeholders in `<>`): `export BRIEF_RUN_SECRET="$(openssl rand -hex 32)"` then redeploy via `bash deploy/cloud-run.sh`; then
  `gcloud scheduler jobs create http dwellium-morning-brief --project my-project-57391aion-ethos-api --location us-central1 --schedule "0 7 * * *" --time-zone "America/New_York" --uri "https://<CLOUD_RUN_URL>/api/briefs/run" --http-method POST --headers "x-brief-secret=<BRIEF_RUN_SECRET>,Content-Type=application/json" --message-body '{}' --attempt-deadline 540s` (update: `gcloud scheduler jobs update http dwellium-morning-brief ... --schedule ...`; manual fire: `gcloud scheduler jobs run dwellium-morning-brief --location us-central1`).
- **Why this shape**: the brief is written into the SAME One Save object the client already owns, so zero new read path, zero new table, and the client's existing login hydrate delivers it.
- **Test**: NEW `tests/services/morningBriefService.test.ts` (pattern `tests/objectStore.test.ts:9-16` temp `ONE_SAVE_DATA_DIR`): seed `goals_u1`, `llm-usage_u1`, `artifacts_u1`, `activity-log_u1` objects → `generateMorningBriefs({ date:'2026-08-19', userIds:['u1'] })` with no key → `morning-brief_u1` payload[0] has `date`, 4 `dataLines`, `seen:false`, `source:'server'`; re-run → `skipped` 1 and payload length 1; prior brief for another date preserved and capped at 30; `parseDeepDream` truncations. NEW `tests/briefRoutes.test.ts` (harness `makeApp`): no env → 503; wrong header → 401; right header + `userIds:[]` → 200 `{success:true}`.
- **Verify**: `cd ~/dwellium-backend/ai-dashboard369-file-manager && OPENAI_API_KEY=sk-dummy npx jest --forceExit tests/briefRoutes.test.ts tests/services/morningBriefService.test.ts`; after deploy: `curl -s -o /dev/null -w '%{http_code}' -X POST https://<CLOUD_RUN_URL>/api/briefs/run` → `401`; with header → `200`; then in the app, log out/in → banner "Your morning brief is ready".
- **Overlaps with 045**: none (backend).

### B2. Frontend: pull the server brief on login/resume (client generation stays as fallback) — S

- Login already hydrates `morning-brief` via `oneSaveSync.bootstrap` (`UserContext.tsx:557-558` → `morningBriefStore.hydrate()`, oneSaveStore.ts:143-150). Add resume: NEW `src/hooks/useMorningBriefSync.ts` — `useEffect` registering `window 'focus'` + `document 'visibilitychange'` (visible) → `if (!todaysBrief()) void morningBriefStore.hydrate()` (guard = never clobber a brief the client already has today; bounded calls). After hydrate, diff `todaysBrief()` before/after: if a new unseen brief appeared → `notifyNewBrief(brief)` (B3). Mount in `AdminShell.tsx:43` right after `useHonchoBackgroundRunner();`.
- `honchoBackgroundRunner.ts:86-87`: insert `if (todaysBrief()) return;` before the `deepDayKey` read (import `todaysBrief` alongside `upsertBrief` :31) — server brief wins; client still generates when the backend was unreachable at login.
- **Why this shape**: reuses `hydrate()` — no new fetch code, no new route; one guard line keeps client/server from fighting over today.
- **Test**: NEW `src/test/morningBriefSync.test.tsx` — mock `oneSaveClient.get` (vi.mock `../lib/oneSaveClient`, `ONE_SAVE_ENABLED: true`) to return a payload with today's brief; `renderHook(useMorningBriefSync)`; dispatch `focus` → `todaysBrief()` non-null; second `focus` → `get` not called again (guard). Runner test: with `todaysBrief()` set, `upsertBrief` spy not called (extend `src/test/dailySynthesis.test.ts` beforeEach reset pattern :23-33).
- **Verify**: `cd qualia-shell && npx vitest run src/test/morningBriefSync.test.tsx src/test/dailySynthesis.test.ts`; browser: with a server brief present, blur/focus tab → banner appears without reload.
- **Overlaps with 045**: none (AdminShell.tsx / honchoBackgroundRunner.ts are not 045 files).

### B3. Real Web Notification — S

- NEW `src/lib/briefNotifier.ts`: `canNotify()` = `typeof Notification !== 'undefined'` && `activationStore.getSnapshot().notifications.enabled && .morningBrief` && `Notification.permission === 'granted'`; `requestBriefNotificationPermission()` = `Notification.requestPermission()` (no-op when unsupported); `notifyNewBrief(b)` → `new Notification('Your morning brief is ready', { body: b.insights[0]?.title ?? b.dataLines[0] ?? b.date, tag: 'dwellium-brief-'+b.date })` (tag dedupes), `onclick` → `window.focus(); requestBriefInAra(b.date); markBriefSeen(b.date)` (morningBriefStore.ts:113-115, :80-82) — the banner's open path (`MorningBriefBanner.tsx:21-25`) minus `openWindow`, which needs context; ARA mount-race leg at `ARAConsole.tsx:1704-1712` picks it up when ARA opens.
- `ActivationCenter.tsx:137-141` "Enable notifications" `onChange`: `(v) => { if (v) void requestBriefNotificationPermission(); update(...) }`. Copy `activationStore.ts:196` → `'On — the morning brief arrives as a desktop notification (permission: ' + Notification.permission + ').'` (guard `typeof Notification`); `:197` unchanged.
- **Why this shape**: platform API, ~25 lines, no service worker (needed only for push while the tab is closed — Cloud Scheduler + this covers open/background tabs; Web Push is a separate item).
- **Test**: NEW `src/test/briefNotifier.test.ts` — `vi.stubGlobal('Notification', class { static permission='granted'; static requestPermission=vi.fn(); constructor(public t,public o){ spy(t,o) } })`; toggle off → `notifyNewBrief` constructs nothing; toggle on + granted → constructed with `tag`; `permission='denied'` → nothing; `Notification` undefined → no throw.
- **Verify**: `npx vitest run src/test/briefNotifier.test.ts`; browser: Control Panel → Activation Center → enable both toggles → Chrome permission prompt; blur tab, `gcloud scheduler jobs run …` (or POST with the header) → focus → OS notification appears, click opens ARA with the brief.
- **Overlaps with 045**: none.

### B4. Unread badge — banner + ARA pinned rail — S

- `MorningBriefBanner.tsx:28-31`: add `<span className="mbrief-banner-badge" aria-label="1 unread brief">1</span>` before the text; `.css`: `.mbrief-banner-badge { min-width:16px; height:16px; border-radius:8px; background: var(--accent); color:#000; font-size:10px; font-weight:700; display:inline-grid; place-items:center }`.
- `Sidebar.tsx`: import `useMorningBrief` (morningBriefStore.ts:125-134) and read `const { today: brief } = useMorningBrief(); const briefUnread = !!brief && !brief.seen;` near the other store reads; in the pinned map (`:920-933`) after the label: `{p.component === 'ara-console' && briefUnread && <span className="sidebar-widget__badge" aria-label="1 unread brief">1</span>}`; in the icon-rail map (`:641-653`) same span for `item.component === 'ara-console'`. `Sidebar.css` after `:459`: `.sidebar-widget__badge { margin-left:auto; min-width:16px; height:16px; border-radius:8px; background:var(--accent); color:#000; font-size:10px; font-weight:700; display:inline-grid; place-items:center }` (icon-rail variant `position:absolute; top:4px; right:4px`). Badge clears because `markBriefSeen` runs on Read/Dismiss (`MorningBriefBanner.tsx:24,34`) and on notification click (B3).
- Picked the Sidebar diff over a new badge store: the hook already exists and the store already syncs; a new store would be a second source of truth.
- **Why this shape**: one store, three surfaces (banner, rail, notification) — cannot disagree.
- **Test**: extend `src/test/sidebar.045.test.tsx` — seed `upsertBrief({date: dayKey(), ...})` → `getByLabelText('1 unread brief')` inside `.sidebar-widget--pinned`; `markBriefSeen(date)` → gone.
- **Verify**: `npx vitest run src/test/sidebar.045.test.tsx`; browser: fresh brief → "1" on ARA in the rail and in the banner; Read brief → both clear.
- **Overlaps with 045**: A (Sidebar.tsx ≤6 lines at :641-653/:920-933 + one Sidebar.css rule) — 045 is already merged at `0a80102`, so this is a small diff on landed code, not a parallel branch.

### B5. Email digest — DEFERRED (gated)

- Not buildable in this cluster: `gmailSendRoute.ts:20-22` is explicitly "simulate + log" (writes a comms row, sends nothing) and `gmailService.ts` exports no send function (`:29-316`). If Ilya gates it ON, it is a separate item: real Gmail send (per-user OAuth via `googleOAuthAccountStore.ts`) + `generateMorningBriefs` emitting `formatBrief` markdown as the body. Nothing to do here.
- **Overlaps with 045**: none.

**Gate per branch**: `cd qualia-shell && npx tsc -b && npx vitest run && NETLIFY=1 npx react-router build && npx eslint src/lib/briefNotifier.ts src/hooks/useMorningBriefSync.ts src/components/Sidebar/Sidebar.tsx src/components/Shell/MorningBriefBanner.tsx src/components/ControlPanel/ActivationCenter.tsx src/services/honchoBackgroundRunner.ts`; backend `OPENAI_API_KEY=sk-dummy npx jest --forceExit`. Never `vite build`. No push/merge without Ilya's go.

### STOP conditions
- `getObject('morning-brief_<uid>')` on the live backend returns a payload that is not `MorningBrief[]` (shape drift) → stop, do not write.
- `BRIEF_RUN_SECRET` unset in the deployed service → route must return 503, never run; do not "temporarily" allow unauthenticated runs.
- Any need to touch `context/UserContext.tsx`, `Shell/Desktop.tsx`, or `lib/backendStatus*.ts` → stop and report (045 D/B/A files; not required by this spec).
- Any write path that deletes/soft-deletes One Save objects → stop (never delete user data; this cluster only upserts).
- Backend `listUsers` returns > 500 users → stop and paginate explicitly rather than silently truncating.

### Ilya decision gates
- **Send time + timezone**: default `0 7 * * *` @ `America/New_York` (`--time-zone` on the scheduler job; `BRIEF_TZ` on the service). Confirm or change.
- **Secret plumbing**: OK to add `BRIEF_RUN_SECRET` to Secret Manager via `deploy/cloud-run.sh` (same path as `OPENAI_API_KEY`)? Ilya runs the gcloud lines in B1.
- **Email digest**: ON/OFF — default OFF (B5 not buildable without real Gmail send).
- **Today-ownership rule**: server brief wins when present (client skips); cost-advisor lines therefore move to a follow-up — accept, or ask for the merge now (adds ~40 LoC to B1).
- Everything else: none — proceed.

---

## Cluster A — ARA speaks first + streaming  (branch `ux/046-a-ara-first`; files: NEW `ARAConsole/araStarterPrompts.ts`, NEW `lib/araDailyGlance.ts`, `lib/perUserIdentity.ts` (2 lines), `ARAConsole/ARAConsole.tsx` (3 small diffs), `ARAConsole/ARAConsole.css`, `lib/llmStream.ts`, `lib/readSse.ts`, `lib/araPrefsStore.ts` (1 line); backend `routes/araRoutes.ts`, `agents/araChatEngine.ts`, NEW `services/llm/openaiStream.ts`)

Facts verified 2026-08-19 @ `5e78ca8`: ARA's empty state is the persona card only (`ARAConsole.tsx:2173-2184` — icon/name/lens/"Best for"/2 tags, no prompts). Chips are reactive only (`suggestedHandoffs` L1720-1727 → "Open: X" L2353-2368). Persona catalog is client-side `BUILTIN_MODES` L232-314 (9 ids, `bestFor` per mode; `EXECUTIVE_ASSISTANT_MODE` L215-226). Only proactive path = morning brief (`morningBriefStore.ts:110-123` bus → `ARAConsole.tsx:1704-1715`). Primary chat = `authFetch(\`${API_ARA}/chat\`)` JSON (L1124-1141); `callLlm` is the offline fallback (L1194-1224). `llmStream.ts:40-47` is a single-shot shim (`STREAMING_AVAILABLE=false`); `araPrefsStore.ts:24` `streamTokens:false`; nobody imports `lib/llmStream` except its test — Scribe/Hydra do not stream (grep). PersonaStudio has its own browser-direct SSE client `personaStream.ts:68-100` (anthropic + openai-compat, pre-2026-08-19 body shapes — still sends `temperature` to Opus 5). Backend: no `text/event-stream` anywhere (`grep -rl` in `src/` → none); `/api/ara/chat` at `araRoutes.ts:44-72` → `chatWithARA` (`araChatEngine.ts:157`) → non-stream OpenAI fetch L338-353 with `emptyByLength` retry.

### A1. Starter prompts per persona — S

- NEW `src/components/ARAConsole/araStarterPrompts.ts`: `export const STARTER_PROMPTS: Record<string, string[]>` keyed by `BUILTIN_MODES` ids; `export function starterPromptsFor(modeId: string): string[]` (falls back to `executive-assistant`). Exact copy:
  - `executive-assistant`: "What should I focus on today?", "Which leases end in the next 60 days?", "Open Strata", "Draft a late-rent reminder for unit 4B", "Summarize my open work orders"
  - `chief-of-staff`: "Turn my open goals into a plan for this week", "What's the critical path on my top goal?", "Which tasks have sat the longest on my board?", "Draft a weekly priorities memo"
  - `research-synthesizer`: "Compare Georgia vs Florida security-deposit rules, with sources", "Brief me on this property from my files", "What's changed in rent regulation this year?", "Where are the gaps in our vendor compliance data?"
  - `lead-counsel`: "Review this lease clause for risk: …", "What notice applies to a month-to-month tenant in Georgia?", "Flag compliance risks in our vendor onboarding", "What should a late-fee clause never say?"
  - `clinical-analyst`: "Find outliers in our maintenance response times", "Is our vacancy trend meaningful or noise?", "Occupancy by property, with caveats", "Stress-test this forecast's assumptions"
  - `financial-strategist`: "Model a 5% rent increase across occupied units", "Where is our biggest maintenance spend?", "Best/likely/worst case for a new roof", "Is this vendor quote in range?"
  - `creative-director`: "Name our tenant newsletter", "Rewrite this listing to stand out", "Draft a welcome note for new residents", "3 taglines for the owner portal"
  - `diplomat`: "Tactful late-rent reminder for unit 4B", "Reply to an upset tenant about a delayed repair", "Owner update about a vacancy", "De-escalate a noise complaint between neighbors"
  - `devils-advocate`: "Poke holes in my plan to self-manage maintenance", "What goes wrong if we raise rent 5%?", "Steelman keeping our slowest vendor", "What am I not seeing in my top goal?"
- `ARAConsole.tsx:2182` (after `.ara-hint-tags`): add `<div className="ara-starter-row" role="group" aria-label="Suggested prompts">{starterPromptsFor(currentMode.id).map(p => <button type="button" className="ara-action-btn ara-handoff-btn" onClick={() => void routeUtterance(p)}>{p}</button>)}</div>`. `routeUtterance` (L1510) is the full composer pipeline (commands → chains → LLM), so "Open Strata" runs as a Conductor command (`humanizeCommandAck` L332). Chips vanish after first send for free — the block is already gated `messages.length === 0` (L2173).
- `ARAConsole.css:583` sibling: `.ara-starter-row { display:flex; flex-wrap:wrap; gap:8px; justify-content:center; margin-top:12px; }` — reuses `.ara-handoff-btn` (L2466).
- Why this shape: data file + 4 JSX lines; no new component, no state.
- Test: NEW `src/test/araStarterPrompts.test.ts` — every `BUILTIN_MODES` id has 4–6 prompts; unknown id → EA list. Extend `ARAConsole.test.tsx` ("shows starter chips when empty; clicking one sends it"): render, `findByRole('button',{name:'What should I focus on today?'})`, click → `authFetch` called with `/chat` body containing that text; chips gone after reply.
- Verify: `cd qualia-shell && npx vitest run src/test/araStarterPrompts.test.ts src/test/ARAConsole.test.tsx`; browser: open ARA, switch lens → chips change; click one → it sends; empty-state card gone.
- Overlaps with 045: D (ARAConsole.tsx — disjoint lines, see header).

### A2. "Today at a glance" on open — M

- NEW `src/lib/araDailyGlance.ts`:
  - `araGlanceStore = withSync(createLocalStorageStore<{ lastShownDay: string | null }>({ key: resolveKey, deserializer, defaultValue: { lastShownDay: null } }), { objectType: 'ara-glance', holder: araGlanceUserIdHolder, resolveKey })` — `oneSaveStore.ts:172` + `morningBriefStore.ts:53-60` sister shape; `resolveKey` → `` `araglance:${uid}` `` / `araglance:_anonymous`. Export `.reset()` (repo convention).
  - `lib/perUserIdentity.ts`: add `export const araGlanceUserIdHolder = makeHolder();` (after L64) and push it into `ALL_HOLDERS` (L67-82). That is the sanctioned single-writer path (file header L15-31). Note `src/test/holderIsolation.test.ts:38-66` keeps its own local list — add the new holder there too.
  - `export async function assembleGlance(): Promise<string | null>` — pure assembler over REAL data, each source wrapped in try/catch and skipped on failure:
    1. Leases: `strataGet<{alerts: LeasingAlert[]}>('/leasing/alerts')` (`LeasingModule.tsx:215`; backend `dwelliumRoutes.ts:2268-2295` emits `lease_expired`/`lease_expiring_30`/`notice_period` with `message`+`severity`). Line: `${n} lease${s} need attention — ${top2 messages}`.
    2. Maintenance: `strataGet<Workitem[]>('/workitems', { type: 'work_order' })` (`MaintenanceModule.tsx:340`), keep `status in ('open','pending')` and `priority in ('high','critical')` (field names per `MaintenanceModule.tsx:455-462`). Line: `${n} high-priority work order${s} open`.
    3. Inbox: `fetch('/api/inbox/stats')` → `data.pending` (`InboxWidget.tsx:92-97, 243`; backend `inboxRoutes.ts:60`). Line: `${pending} inbox item${s} waiting for approval`.
    4. Tasks: `taskBoardStore.getSnapshot().cards.filter(c => c.columnId !== 'done')` (`taskBoardModel.ts:58-71, 130`) — set `taskBoardUserIdHolder.current = userId` first (writer today is only `TaskBoard.tsx:71`; same value, no disagreement). Line: `${n} task${s} not done — ${high} high urgency`.
    5. Goals: `goalsStore.getSnapshot().filter(g => g.status !== 'done')` (`dailySynthesis.ts:54`). Line: `${n} active goal${s}: ${first title}`.
    6. Morning brief: `todaysBrief()` (`morningBriefStore.ts:76`) → its first `suggestions[0]` as a line when present.
    Order as listed; take the first 3 non-empty; return null if none. Output (exact): `**Today at a glance — ${k} thing${k===1?'':'s'} worth doing**\n1. …\n2. …\n3. …\n\nAsk me about any of these, or pick a prompt below.`
  - `export async function runDailyGlance(userId: string | null, post: (content: string) => void): Promise<boolean>` — `const today = dayKey()` (`dailySynthesis.ts:131`); if `araGlanceStore.getSnapshot().lastShownDay === today` return false; `const text = await assembleGlance()`; if null return false (silent); `post(text)`; `araGlanceStore.set({ lastShownDay: today })`; return true. Throttle is written AFTER a successful post so a fetch failure retries next open.
- `ARAConsole.tsx`: after the brief effect (L1715) add: `useEffect(() => { if (!isAuthenticated) return; void runDailyGlance(user?.id ?? null, (content) => setMessages(prev => [...prev, createChatMessage({ role: 'assistant', content, mode: 'executive-assistant' })])); }, [isAuthenticated, user?.id]);` (`isAuthenticated`/`user` from L419). Spec says gate additionally on `hasActiveLlm(integrations.llm)` (L1194) — see gate.
- Why this shape: all logic + I/O in a lib with its own tests; ARAConsole gains 1 import + 1 effect; One Save sync via the existing `withSync` wrapper.
- Test: NEW `src/test/araDailyGlance.test.ts` — mock `strataGet`/`fetch`, seed `taskBoardStore`/`goalsStore`: (a) builds 3 lines in priority order with exact heading; (b) returns null with no data and `post` not called; (c) second call same day → false, `post` once; (d) `vi.setSystemTime` next day → posts again; (e) one source throwing → others still assembled. Extend `ARAConsole.test.tsx`: with `runDailyGlance` mocked to post, the glance renders as an assistant message on mount.
- Verify: `npx vitest run src/test/araDailyGlance.test.ts src/test/holderIsolation.test.ts src/test/ARAConsole.test.tsx`; browser: fresh day → ARA opens with the glance; reload → not repeated; DevTools `localStorage['araglance:<uid>']` has today's `dayKey`; Network shows `/api/dwellium/leasing/alerts`, `/api/dwellium/workitems?type=work_order`, `/api/inbox/stats`.
- Overlaps with 045: D (ARAConsole.tsx, disjoint lines); none for perUserIdentity.ts.

### A3. Stream LLM output — browser-direct (phase 1) — M

- `src/lib/readSse.ts`: extract the reader loop (L52-63) into `export async function pumpSseBody(body: ReadableStream<Uint8Array>, onEvent: (e: SseEvent) => void): Promise<void>` (same `\r\n` normalisation + `parseSseBlock` L27); `readSse` calls it. No behaviour change.
- `src/lib/llmStream.ts` — real streaming, same generator signature (L33-36):
  - Pure per-provider event parsers (exported): `parseAnthropicStreamData(data) → { delta?: string; stop?: string }` (`type==='content_block_delta' && delta.type==='text_delta'` → delta; `type==='message_delta'` → `delta.stop_reason`); `parseOpenAiStreamData(data)` (`choices[0].delta.content`; `choices[0].finish_reason`; `[DONE]` → {}); `parseGeminiStreamData(data)` (non-`thought` `candidates[0].content.parts[].text` joined; `candidates[0].finishReason`).
  - Requests reuse `buildAnthropicBody` / `buildOpenAiBody` (`llmClient.ts:375, 323`) + `stream: true`; headers as `llmClient.ts:402-407` / `437`; Gemini URL `…/models/${model}:streamGenerateContent?alt=sse&key=…` mirroring L459 body; local/custom = OpenAI-compatible URL rules from `personaStream.ts:86-97`. Non-OK → `throw new LlmError(provider, status, text)` (`llmClient.ts:41`).
  - Pump with `pumpSseBody`; yield `{ delta, text, done:false }` per delta; track `stop`. On end: if `text===''` and `stop ∈ {'max_tokens','length','MAX_TOKENS'}` → `const res = await callLlm(req, llm)` (which already retries at `retryBudget` — `llmClient.ts:424-428, 447-452, 483-488`) and yield its text as one final event; if `stop==='refusal'` → throw like `llmClient.ts:420-422`. Otherwise yield `{ delta:'', text, done:true }`.
  - Metering: streaming bypasses the `callLlm` chokepoint, so on success call `recordAiSuccess()` + `recordLlmUsage({provider, model, promptChars, responseChars})` (`llmClient.ts:72-80`; exports `aiHealthStore.ts:58,64`, `llmUsageStore.ts:123`), on throw `recordAiFailure`. `STREAMING_AVAILABLE = true`.
- `src/lib/araPrefsStore.ts:24`: `streamTokens: true` (header comment L3-4 "default OFF" → update).
- `ARAConsole.tsx:1196-1224` (offline fallback): when `araPrefsStore.getSnapshot().streamTokens`, replace `callLlm` with: push placeholder `createChatMessage({ role:'assistant', content:'', mode })`, then `for await (const ev of streamLlm(req, integrations.llm)) setMessages(prev => prev.map(m => m.id===id ? {...m, content: ev.text} : m))` (the `setProgress` shape at L1107-1109); after the loop run the existing `recordAraChat`/`recordArtifact`/`escalateIfRefusal` on the final text. Pref off → unchanged `callLlm`.
- `personaStream.ts`: optional 1-commit follow-up — delegate `streamAnthropic`/`streamOpenAiCompat` to the new bodies (fixes its Opus-5 `temperature` 400); keep its exported extractors so `personaStudio.test.ts:27` stays green. Skip if time-boxed.
- Why this shape: one SSE pump for providers + backend; `callLlm`'s existing thinking-retry is the fallback, not a second copy.
- Test: `src/test/llmStream.test.ts` — replace the "not-yet-available" case (L32-34) with `STREAMING_AVAILABLE===true`; per-provider cases with `fetch` mocked to a `ReadableStream` body (`readSse.test.ts:4-12` `streamOf` helper): Anthropic `message_start`→`content_block_delta`×2→`message_delta{stop_reason:'end_turn'}` yields 2 deltas + final text; OpenAI `choices[0].delta.content`×2 + `[DONE]`; Gemini `alt=sse` 2 chunks with a `thought:true` part skipped; "empty + max_tokens → falls back to `callLlm` (spy) once and yields its text"; "refusal → throws LlmError". `readSse.test.ts`: add `pumpSseBody` splits events across chunks. `ARAConsole.test.tsx:12-14` mock gains `'../lib/llmStream'` → `streamLlm` async generator yielding "Offline " then "LLM reply." — existing A1 assertion (L295) still finds the final text; `llmClientNewerModels.test.ts` untouched and must stay green.
- Verify: `cd qualia-shell && npx tsc -b && npx vitest run && NETLIFY=1 npx react-router build && npx eslint src/lib/llmStream.ts src/lib/readSse.ts src/components/ARAConsole/ARAConsole.tsx`; browser: stop the backend, ask ARA → reply grows word-by-word; Control Panel AI-spend ledger still increments.
- Overlaps with 045: D (araPrefsStore.ts 1 line; ARAConsole.tsx L1196-1224 only).

### A4. Backend ARA SSE (phase 2) — M

- Backend NEW `src/services/llm/openaiStream.ts`: `export async function pumpOpenAiSse(body: ReadableStream<Uint8Array>, onDelta: (d: string) => void): Promise<{ text: string; finishReason: string | null }>` — `data:` lines → `choices[0].delta.content` / `finish_reason`, stop at `[DONE]`.
- `src/agents/araChatEngine.ts:132` `ChatWithAraOpts` += `onDelta?: (d: string) => void`; L338-353: when `opts.onDelta`, body gets `stream: true`, call `pumpOpenAiSse(response.body, opts.onDelta)`; if `text===''` and `finishReason==='length'` → fall through to the existing non-stream `callOnce(retryBudget(1500))` (L353) and `onDelta(content)` once. Everything after (history push, Legal Shield footer L378-390, obsStats) unchanged — the returned `ARAResponse.content` is final.
- `src/routes/araRoutes.ts`: NEW `router.post('/chat/stream', …)` (sibling of L44): same validation (L48-56), headers `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `X-Accel-Buffering: no`; `res.flushHeaders()`; `onDelta` → `res.write(\`data: ${JSON.stringify({delta})}\n\n\`)`; on resolve `res.write(\`event: done\ndata: ${JSON.stringify(response)}\n\n\`)`; on throw `event: error` + `{error}`; `res.end()`. Never log `userLlmKey`.
- Frontend `ARAConsole.tsx:1124-1141`: when `streamTokens`, POST the same body to `${API_ARA}/chat/stream` with `Accept: text/event-stream`; `res.status===404` (older Cloud Run revision) → fall back to the existing `/chat` JSON path unchanged; else placeholder message + `pumpSseBody(res.body, …)`: `message` events append `delta`, `done` event supplies `data.data`-shaped `{content, mode, entityGuardianActive, contextSources, observability}` → finalize exactly as L1150-1166 (`recordAraChat`, artifact, `escalateIfRefusal`, avatar/TTS).
- Why this shape: one new route + `onDelta` plumbing; JSON route untouched; client degrades on skew.
- Test: backend `tests/araRoutes.test.ts` — `mockChatWithARA` implementation calls `opts.onDelta('Hel')`, `onDelta('lo')` then resolves `{content:'Hello', mode:'executive-assistant'}`; assert `content-type` text/event-stream and body contains `data: {"delta":"Hel"}` and `event: done`; NEW `tests/openaiStream.test.ts` — `pumpOpenAiSse` over a `ReadableStream` with two chunks split mid-line + `[DONE]` → text + finish_reason; empty+length reported. Frontend `ARAConsole.test.tsx`: `authFetch` for `/chat/stream` returns a streamed body → partial text then final; 404 → JSON path used.
- Verify: backend `OPENAI_API_KEY=sk-dummy npx jest --forceExit` then `curl -N -H 'Authorization: Bearer <dev>' -H 'Content-Type: application/json' -d '{"mode":"executive-assistant","message":"hi"}' http://localhost:3000/api/ara/chat/stream` → `data:` lines arrive before `event: done`; frontend gate as A3; browser with backend up: reply streams; observability drawer still shows `providerUsed`/latency.
- Overlaps with 045: D (ARAConsole.tsx L1124-1166 only).

STOP conditions
- `npx tsc -b`, `npx vitest run` or `NETLIFY=1 npx react-router build` red → stop, do not push. Backend jest red → stop A4.
- A2 posts when `assembleGlance()` returns null, or posts twice on one `dayKey` → stop (throttle bug).
- Any streamed reply ends EMPTY where `callLlm` would have returned text (thinking/length) → stop; fallback in A3 is broken.
- Streaming bypasses `recordLlmUsage` (AI-spend ledger flat after streamed calls) → stop.
- `userLlmKey` or any API key appears in backend logs → stop, scrub.
- Never push/merge without Ilya's go; no widget renames; no deletion of persisted ARA conversations.

Ilya decision gates
- A2: glance for everyone on open (default) vs. only when `hasActiveLlm` (spec wording)?
- A3: flip `streamTokens` default with A3 (streams only the offline fallback until A4) or hold the flip until A4 ships? Default: same branch, flip with A3.
- Otherwise: none — proceed.

---

## Critic notes (cross-cluster — read before dispatch)

- **045 is already merged** (ui/045-integration → 0a80102 ⊂ main 5e78ca8, all of A–E). Ignore every "if 045-X is mid-flight / hand diff to A" line; all 046 work sits on landed code. Still do not touch responsive.css, the 045-C widget CSS files, Desktop.tsx beyond F1's 4 lines, or LayoutContext.tsx.
- Merge order: **S2 → S1 → D → F → B → A**. Each later branch rebases on the previous merge; re-anchor by grep (tag/identifier), never by line number — spot-checked anchors are correct at 5e78ca8 except: `getWidgetMeta` is widgetRegistry.ts:613; D2's sample-KPI block is L593-~640 (delete by the `{SHOW_SAMPLE_KPIS && (` … `)}` pair).
- Shared files & owners: AdminShell.tsx → S2 owns, others append one line each; Sidebar.tsx → F lands its L500-511 deletion first, then S2 (3 `title=`), D (WIDGET_GROUPS), B (badge spans); perUserIdentity.ts + holderIsolation.test.ts → F adds holder #13 (count 13), A adds #14 (count 14); ARAConsole.tsx → A owns, applies S1c's 3 lines re-anchored on `className="ara-input-bar"`; AstraDashboard.tsx → D owns, applies S1b's two hunks; sidebar.045.test.tsx → append-only `it()` blocks per cluster.
- After S1a, `configure()` opens `api-keys`; **F1 step-1 CTA must emit `{ widgetId: 'api-keys' }`** (D2's "Replace with your data" keeps `control-panel` → Activation Center).
- F1 FirstRunCard must not sit on the Assistant FAB / toast corner (bottom-right): use `bottom: 72px` or bottom-left; S1 pill (bottom-centre) and S2 pill (top-centre) are clear.
- Locked-decision fixes: F1 default-stack must honour the legacy `dwellium:default-stack:v1 === 'done'` for existing users (read both keys, write only the per-user key, no migration). D4 Labs: use the registry label for inbox-zero (no "(deprecated)" rename) and gate the dock-merge append for existing saved layouts on Ilya's OK — default-layout changes are for new users only.
- S1c: add one smoke test in ARAConsole.test.tsx (mock useAIAvailability → 'unavailable' → "Open API Keys" visible). A2: add a component-level "no second glance same day" assertion. F1: assert hermesLearningUserIdHolder is set before `araChatRuns()` is read.
- D3 row 15 / D1 write guards: keep `isStaticMode` build-time; confirm `grep "API_BASE}/api/dwellium"` under StrataDashboard/ + AstraDashboard/ returns only AstraDashboard.tsx:934 before merging D.
- A3/A4: streaming must still call `recordAiSuccess`/`recordLlmUsage`; `STREAMING_AVAILABLE` flip and `streamTokens: true` go in the same commit as A3 unless Ilya's gate says otherwise.
- Never push/merge without Ilya's go; `npx tsc -b && npx vitest run && NETLIFY=1 npx react-router build` green per branch; never `vite build`; no DELETE/TRUNCATE anywhere, One Save upsert only.


### File-level overlaps between 046 clusters

- qualia-shell/src/components/Shell/AdminShell.tsx — S1 (SyncStatusPill mount), S2 (CommandPill + ShortcutSheet mount after <CommandPalette/> L159), F (FirstRunCard mount after <MorningBriefBanner/> L162), B (useMorningBriefSync() hook after L43) — all additive one-liners; owner: S2 (merges first); S1/F/B rebase and re-anchor by neighbour tag name, never by line number.
- qualia-shell/src/components/Sidebar/Sidebar.tsx — S2 (title= at L648/849/1151 + withDesc helper), F (delete L500-511 GREETING_MESSAGES, L578 motto), B (badge spans inside icon-rail map L641-653 and pinned map L920-933), D (WIDGET_GROUPS L807-811 + Labs) — S2's L648 sits inside B's L641-653 hunk and F's 12-line deletion shifts every later anchor; owner: F (deletion first), then S2, D, B send diffs anchored by grep (`title={item.label}`, `WIDGET_GROUPS`, `pinnedItems.map`).
- qualia-shell/src/lib/perUserIdentity.ts + src/test/holderIsolation.test.ts — F (firstRunUserIdHolder) and A (araGlanceUserIdHolder) both append to ALL_HOLDERS and both bump `expect(unique.size).toBe(12)` (holderIsolation.test.ts:66) — second to land must set 14, not 13; owner: F, A sends its one-line holder diff + count bump.
- qualia-shell/src/components/ARAConsole/ARAConsole.tsx — S1c (hook after L421, banner above L2536) and A (L1124-1141, L1196-1224, new effect after L1715, chips after L2182) — disjoint hunks but A's inserts shift S1c's L2536 and S1c's L421 insert shifts all of A's; owner: A (largest); S1c is a 3-line diff A's executor applies, re-anchored by `className="ara-input-bar"`.
- qualia-shell/src/components/AstraDashboard/AstraDashboard.tsx — S1b (hook beside L823, nokey block L864-869) and D (guard at L934, NotYet bodies L1036/L1081, DemoBanner mount L1356) — disjoint; owner: D; S1b sends its 2-hunk diff.
- qualia-shell/src/test/sidebar.045.test.tsx — F (motto assertion), B (badge assertion), D (Labs group assertion) all extend the same file — append-only `it()` blocks; owner: F, others add their own describe blocks at the tail.
- Semantic (not file) overlap — S1a retargets useAIAvailability.configure() to widgetId 'api-keys'; F1 step-1 CTA 'Open API Keys' and D2 'Replace with your data' still emit 'control-panel'. F1 must emit 'api-keys' once S1 lands (D2 legitimately wants control-panel → Activation Center).
- Screen-real-estate overlap — F1 FirstRunCard is fixed bottom-right (right:16 bottom:16, 320px) which is where the Assistant FAB and .qualia-toast already live (S1e cites AssistantLauncher.css:4-6, Desktop.css:660-663); S1e SyncStatusPill bottom-centre and S2 CommandPill top-centre are clear. F must offset the card above the FAB (bottom ≈ 72px) or place it bottom-left.


### Merge order (critic)

Correction first: plan 045 is NOT 'not built yet' — all five clusters (A sidebar 54edebb, B desktop 2c1be03, C buttons b81e0c7, D ARA/login bc577f8, E responsive 81bbe24) merged via ui/045-integration → main 0a80102, an ancestor of 5e78ca8 (verified `git log`; Sidebar.tsx:44/47/453 and responsive.css:2 carry 'plan 045' comments). So every 046 cluster lands AFTER all of 045; there is no in-flight 045 branch to sequence against and no diffs to 'hand to A/C'. 046 order: 1) S2 (shallow, widest file fan-out, adds widget descriptions + AdminShell mounts that later clusters anchor next to); 2) S1 (AIDegradedState/useAIAvailability retarget to 'api-keys' — F1 depends on the new target; small ARAConsole/AdminShell diffs); 3) D (Strata/Astra/ControlPanel/hierarchy; its Sidebar WIDGET_GROUPS line and AstraDashboard hunks are isolated); 4) F (its 12-line Sidebar deletion shifts anchors, so land before B's Sidebar badge; adds perUserIdentity holder #13); 5) B (Sidebar badge on top of F's shifted lines; backend route is independent; adds AdminShell hook); 6) A (largest, heaviest ARAConsole edits, backend SSE, two Ilya gates; adds holder #14 and rebases S1c's 3 lines). Relative to 045: S2/S1/D/F/B/A all sit on landed 045-A/B/C/D/E code; 045-E responsive.css and 045-C widget CSS must stay untouched (S2 pill media query in CommandPill.css, S1 pill CSS new file — both comply).


### Locked-decision fixes applied

- F1 default-stack per-user key: reading only `dwellium:default-stack:v1:<uid>` means an EXISTING user whose legacy `dwellium:default-stack:v1`='done' and who logs in with an empty canvas gets the default stack re-fired once — a layout change for existing users. Fix: treat legacy key ==='done' as done for every user (read both keys; write only the new one). No migration/deletion of the legacy key.
- D4 Labs group: the 4 new dock ids are appended to EXISTING users' saved layouts by the WindowContext dock-merge (plan says so explicitly: 'existing profile also gets it (merge)'). Collapsed and additive, but it is a default-layout change for existing users — needs Ilya's explicit OK or gate the merge-append to users whose saved dock lacks any 'Labs' entry AND who have no saved layout (new users only).
- D4 dock label 'Inbox Zero (deprecated)' differs from the registry label for component 'inbox-zero' — borderline rename of a user-visible widget label. Use the registry label verbatim; the Labs group header already conveys status (S2's description row 'Older Inbox Zero entry kept for saved layouts' does the explaining).


### Gaps the executor must close

- S1c — no Test line (explicitly 'none new'); acceptable only because S1a covers the component, but add at least a smoke assertion in ARAConsole.test.tsx (mock useAIAvailability → status 'unavailable' → 'Open API Keys' button present) since ARAConsole.test.tsx exists (cluster A extends it).
- S2-6 — `getWidgetMeta` is at widgetRegistry.ts:613, not 614 (trivial; re-anchor).
- D2 — 'L592-638' is imprecise: the SHOW_SAMPLE_KPIS block is the comment at L593-605 + `{SHOW_SAMPLE_KPIS && (` L606 through `)}` at ~L640 (L592 is the section header comment, L641 starts Move-In/Move-Out). Executor must delete by the `{SHOW_SAMPLE_KPIS && (` … `)}` pair, not by line numbers.
- D4 — FileExplorer.tsx comment fix has no Test/Verify (acceptable, comment-only) but should be a one-line lint check in the Verify list.
- B2 — 'AdminShell.tsx:43' hook mount verified (L43 is useHonchoBackgroundRunner()), but no Verify for the SSR sweep (providerSSRSafety.test.tsx) that S1e mentions — add useMorningBriefSync to the same sweep if it enumerates hooks/stores.
- A2 — 'Extend ARAConsole.test.tsx: glance renders on mount' lacks the assertion that it does NOT render when lastShownDay===today (throttle) at the component level; the lib test covers it but the effect wiring (user?.id dep) is untested.
- F1 — 'araChatRuns().length > 0' completion of step 3 depends on hermesLearningUserIdHolder being set during FirstRunCard render; no test asserts the holder is set before the read (holderIsolation pattern) — add one line to FirstRunCard.test.tsx.
- All clusters — the 'overlaps_045' sections describe 045 branches as in-flight ('hand the diff to A if A is mid-flight', 'land after 045-D or rebase'); all five 045 clusters are already on main (0a80102). Those instructions are dead; replace with the 046-internal ownership above.


## Decision gates — for Ilya (defaults in parentheses; executors proceed with the default)

1. **D** — Demo workspace default for brand-new users: OFF (proposed, with one-click 'Try with demo data' in the empty state) vs ON behind a portfolio-empty probe (/stats.totalProperties === 0 and backend online) — the latter adds ~15 lines + a test
2. **D** — Labs group membership (proposed): Inbox Zero (deprecated), Autonomous Runs, Time Travel, Holocron Library — File Explorer stays out (it is real; stale header only)
3. **D** — Delete the six hardcoded 'Sample data' KPI cards (StrataDashboard.tsx:592-638) outright (proposed; prod already hides them via VITE_APPFOLIO_SEEDS=false) vs re-gate them on the demo flag
4. **F** — F3 value statement: (a) "Your properties, your inbox, your AI — one screen." vs (b) "Property management with an AI that actually does the work." — and confirm splash product name = Dwellium (AstraStrata image stays in the card)
5. **F** — F1 step 2 first action: "Add a property" (recommended; auto-completes via useProperties, no OAuth setup) vs "Connect Gmail" (needs backend Google creds). Default if unanswered: Add a property
6. **B** — B1 send time + timezone: default `0 7 * * *` in `America/New_York` (Cloud Scheduler `--time-zone`; backend computes the brief's `date` in `BRIEF_TZ`, default America/New_York). Confirm or change both values.
7. **B** — B1 secret plumbing: OK to add `BRIEF_RUN_SECRET` via Secret Manager in deploy/cloud-run.sh (mirrors OPENAI_API_KEY) — Ilya runs the gcloud commands in B1.
8. **B** — B5 email digest: ON/OFF — currently NOT buildable: gmailSendRoute.ts:20-22 is 'simulate + log' and gmailService.ts exports no send. Default = OFF / deferred; if ON, it is a separate item (real Gmail send via per-user OAuth creds) outside this cluster.
9. **B** — B2 ownership rule: server brief wins for the day when both exist (client runner skips if `todaysBrief()` is present). Confirm; alternative (merge client cost-advisor lines into the server brief) is a follow-up.
10. **A** — A2 gate: the glance is assembled from local/backend data and needs no model — spec says post it only `hasActiveLlm(...)`. Default in plan: gate on `isAuthenticated` only (everyone gets it). Say 'LLM-only' to keep the spec gate.
11. **A** — A3 gate: flipping `streamTokens` default to true streams the LLM-FALLBACK path only until A4 ships (primary path is backend JSON, ARAConsole.tsx:1124). Ship A3 alone (visible only when backend is down) or hold the flag flip until A4 lands? Default in plan: flip with A3, A4 same branch.

## Sequencing & dispatch

- **Wave 1 (parallel, 6 agents)**: S2, S1, D, F, B, A in worktrees `.advisor-worktrees/046-*` on branches `ux/046-*` off `main` @ `5e78ca8`; shared-file owners per Critic notes (AdminShell.tsx → S2; Sidebar.tsx → F first, then S2/D/B; perUserIdentity.ts → F then A; ARAConsole.tsx → A; AstraDashboard.tsx → D).
- **Merge order**: S2 → S1 → D → F → B → A; each later branch rebases on the previous merge and re-runs the gate.
- **Backend pieces** (B1 route + Cloud Scheduler; A4 SSE): branch `feat/046-brief` / `feat/046-ara-sse` in the backend repo; Ilya runs the `gcloud` commands listed in B1.
- **Per branch**: gate green + the cluster's `Verify` lines pasted in the commit body. No push without Ilya's go.

## STOP conditions

- Any gate above left unanswered: proceed with the stated default and say so in the commit body — never invent a third option.
- D4 / F1: default-layout changes are for **new users only** (locked); the critic's fixes above are mandatory.
- B5 email digest is **not buildable** today (gmailSendRoute is simulate-only) — do not fake it.
- A3/A4: streaming must preserve the empty-reply retry added in `dd7fc89`/`5e78ca8` (reasoning/thinking eats small budgets) and keep `recordAiSuccess`/`recordLlmUsage` calls.
- Never push, reset, or rewrite history on Ilya's behalf; never delete user data.
