.DS_Store
Thumbs.db
*.log
*.swp
.vscode/
.idea/
/node_modules
qualia-shell.pre-subtree-bak/

# Per-project Claude Code settings (session-local)
.claude/

# User-local agent roster config (session-local; commit explicitly via -f if intended for repo)
AGENTS.md

# Per-task scratch baseline directories (session-local probes; canonical baselines are committed at top-level Docs/Baselines/)
Docs/Baselines/phase_*_task_*/

# CDP probe scripts — session-local, NOT committed (per Tasks 3.1 / 3.4 / 6.1 / 6.3 / 6.4 precedent)
qualia-shell/cdp_probe_task_*.cjs

# Alt-dist directory from external build contexts (session-local)
qualia-shell/dist-external/
.vercel

# RR v7 framework-mode build output + cache (Phase-9+ Task 9.5 carry-forward)
qualia-shell/build/
qualia-shell/.react-router/

# Session-local capture configs (preview/debug shell screenshots)
qualia-shell/_debug-*.config.ts
qualia-shell/_preview-capture*.config.ts
qualia-shell/_shell-recapture.config.ts
qualia-shell/e2e/_debug-*.ts
qualia-shell/e2e/_preview-capture*.ts
qualia-shell/e2e/_shell-recapture.ts
Docs/preview-screenshots/
Docs/preview-screenshots-backend/

# Transient PR creation scratch (session-local)
.create-pr.sh
.pr-body.md
.fuse_hidden*

# CodeGraph index (local code-intelligence DB; never committed)
.codegraph/

# Electron packaging artifacts (session-local; DMG outputs + staging + vendored deps)
electron/node_modules/
electron/dist-installer/
electron/staging/

# Local Netlify folder
.netlify
